fastapi>=0.115.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.6
python-multipart>=0.0.22
ccxt>=4.3.20
websockets>=12.0
python-dotenv>=1.0.1
pandas>=2.2.3
numpy>=2.2.4
openpyxl>=3.1.2
schedule>=1.2.2
aiofiles>=23.2.1
httpx>=0.27.0
uvloop>=0.21.0; sys_platform != "win32"
aiosqlite>=0.20.0
cryptography>=42.0.0
yfinance>=0.2.36
redis>=5.0.0
sse-starlette>=2.0.0
markupsafe>=2.1.5
concurrent-log-handler>=0.9.20,<1.0
# IANA tz database for zoneinfo. Windows ships no system tz db, so
# ZoneInfo("America/New_York") — used to place US economic releases on their
# Eastern wall clock (core/news_fetcher) — needs this wheel. It was present
# only TRANSITIVELY via pandas; declared here so a third party's dependency
# change cannot silently break the economic calendar.
tzdata>=2024.1; sys_platform == "win32"
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.1.0,<3.0
fakeredis>=2.20.0
