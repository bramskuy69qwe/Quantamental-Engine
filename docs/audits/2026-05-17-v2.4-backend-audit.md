# v2.4 Backend Audit — 2026-05-17

## Overall Verdict

The v2.4 codebase is **operationally solid for a single-user deployment**. The
core trading loop (fill processing, position lifecycle, risk calculations) works
correctly under normal conditions. 1247 tests pass. The history page redesign
(Tasks 68-82) shipped cleanly with consistent patterns.

The primary risk areas are: (1) a logic bug in exec_link where TP/SL matching
always returns True (self-comparison), (2) data export always uses account_id=1
regardless of active account, (3) missing authentication on all API endpoints,
(4) N+1 query pattern in the fill drawer, (5) division-by-zero paths in edge
cases, and (6) WebSocket reconnect can stack overflow on persistent auth failures.
Items 1, 2, and 4 are v2.4-era regressions; the rest are pre-existing. None are
show-stoppers for current single-user operation but several affect multi-account
correctness.

Architecture is well-layered (core + adapter ring, DB mixins, pub/sub abstraction)
but shows coupling creep via 24 direct `db._conn` access points that bypass the
DatabaseManager API. Test coverage by module count is 30% (18/59 core modules have
dedicated tests), though integration coverage is higher via cross-cutting test files.

## Stat Summary

| Severity | Count |
|----------|-------|
| Critical | 0 |
| High | 7 |
| Medium | 39 |
| Low | 21 |
| **Total** | **67** |

(Pass 1: 28. Pass 2: +15. Pass 3: +12. Pass 4: +14. Pass 5: +8. Pass 6: +10. Pass 7: +5.)
Discovered during fix phase: +13 (LOW-022, MED-043, HIGH-026, CRIT-008, MED-044, HIGH-027, HIGH-028, HIGH-029, LOW-023, HIGH-030, HIGH-031, MED-045, MED-046).
Verified false during fix phase: −8 (CRIT-003, CRIT-005, HIGH-004, HIGH-005, HIGH-006, HIGH-019, HIGH-025, HIGH-012).
Resolved during fix phase: CRIT-002 (Task 85, slippage guard — count correction applied in Task 89), CRIT-004 (Task 87, short-term 1/1; full fix Phase 9), LOW-022 + MED-043 (Task 85b), CRIT-001 (Task 88, batch-fetch refactor), CRIT-008 (Task 88.1, missing column ALTER), HIGH-003 (Task 88.1, two indexes inline), CRIT-006 (Task 89, active-account export scoping), HIGH-009 (Task 92, overfill cap on fee allocation), HIGH-020 (Task 93, fetch_account null-check), HIGH-021 (Task 94, TTL reduction + admin force-refresh), HIGH-026 (Task 95, silent-swallow engine_event emission), HIGH-022 + HIGH-023 + MED-036 (Task 97, route input validation bundle), HIGH-017 + HIGH-018 (Task 98, DD gate fail-closed + pubsub except narrowing), HIGH-007 + HIGH-014 + HIGH-015 + LOW-019 (Task 99, adapter observability/reliability bundle), HIGH-024 (Task 100, SensitiveStr wrapper — narrow scope: routes_connections only; HIGH-029 filed for parallel exchange-credential paths), HIGH-011 + MED-003 + MED-022 (Task 101, SQL injection defense bundle — filter whitelist + route-level sort validator + LIKE parameterization), HIGH-013 + MED-018 (Task 102, trust-but-verify exchange responses — critical-field validation + negative-equity clamp), HIGH-008 (Task 103, scheduler AuthenticationError handling + account-level auth_failed_accounts state), MED-045 (Task 103.5, ConcurrentRotatingFileHandler swap — filed + resolved same commit), HIGH-027 (Tasks 104a + 104b, calc-to-fill matching window + UI).
Duplicates consolidated: −2 (HIGH-016 → HIGH-010, LOW-018 → MED-011) (Task 90.5e cleanup).
Net findings active: 67 (0 CRIT / 7 HIGH / 39 MED / 21 LOW).
Framing-note correction (Task 99.1): HIGH-007 + HIGH-014 were paraphrased as "race-framed" in earlier Top-5 commentary, but the audit text itself is not race-framed for either (HIGH-007 = observability; HIGH-014 = drift / missing reconciliation). Neither finding counts toward the race-framing false-positive calibration. Count below remains 8/15 (53%).

**Race-framing calibration (as of Task 96):** 8 of ~15 audit findings using "race", "concurrent", or similar framing have verified false (53% false-positive rate in this category). The audit consistently assumed Python runtime semantics permit concurrent mutation where local-variable binding, GIL serialization, and cooperative asyncio actually provide structural protection. The correct pattern for closure-capturing mutable data already exists in the codebase: `lambda f=fill: ...` default-arg snapshots are the right shape and are used in places where mutation is a genuine concern.

Implication for future audit-vs-codebase work: race-framed findings should default to verify-before-assume — start with await-analysis of the loop body and trace of the mutation paths before accepting the audit's described mechanism.

**Audit-impact-imprecision pattern (as of Task 103, four examples):** Distinct from race-framing. The audit has repeatedly described the *mechanism* or *direction of impact* of a finding incorrectly, even when the underlying bug is real. Four observed examples:
- HIGH-019 (Task 93): audit assumed the engine actively places TP/SL orders. Engine is a passive observer; Quantower plugin places. Wrong architecture — finding verified false and reframed as HIGH-027 (calc-to-fill window).
- HIGH-026 (Task 95): audit described a silent-swallow try/except. Logs already existed via `log.exception`. The real gap was missing structured engine_event emission, not silence — finding redrawn during fix.
- HIGH-013 (Task 102): audit framed downstream impact as "risk engine allows excessive position sizing". Tracing the consumers showed equity=0 actually collapses via `risk_engine.py:351`'s `total_equity if > 0 else 1.0` fallback to 1.0 USDT phantom equity → micro-positions, not excessive. Wrong direction. Real harm is silent corruption of known-good prior state (BOD/SOW freeze, etc.).
- HIGH-008 (Task 103): audit said the scheduler "crashes silently" on AuthenticationError. The existing broad `except Exception` already caught it — the task did not crash; every 15-30 s tick hammered the exchange with invalid creds, generating log noise + IP-ban risk + no operator signal. Wrong mechanism.

Implication for future audit-vs-codebase work: don't anchor on the audit's described impact mechanism. Trace the downstream consumers independently when investigating a finding. The bug may be real but with a different harm shape than the audit predicted — fix the real harm, not the described one. The fix-task report should explicitly note when the audit's mechanism/direction was wrong, so the calibration record grows.

## Sweep Classification (Task 90.5 — multi-batch)

Batch 1a (Task 90.5a, Sections 1-3, 9 in-scope findings):
| Classification | Count | IDs |
|---|---|---|
| VERIFIED REAL | 8 | MED-001, MED-002, HIGH-001, MED-003, MED-004, LOW-001, MED-005, MED-006 |
| VERIFIED FALSE | 0 | — |
| SCOPE-SHIFTED | 1 | MED-007 (audit said no symbol-cleanup; cleanup exists via evict_symbol_caches — only max-age eviction is missing) |
| NEEDS DEEP READ | 0 | — |
| **Batch 1a total** | **9** | |

Batch 1b (Task 90.5b, Sections 4-11, 26 in-scope findings):
| Classification | Count | IDs |
|---|---|---|
| VERIFIED REAL | 19 | HIGH-002, MED-008, LOW-002, MED-009, LOW-003, LOW-004, LOW-005, LOW-007, LOW-009, LOW-010, LOW-011, HIGH-007, HIGH-008, MED-010, MED-011, MED-012, MED-013, MED-014, LOW-015 |
| VERIFIED FALSE | 0 | — |
| SCOPE-SHIFTED | 1 | LOW-012 (.env.example doesn't exist) |
| NEEDS DEEP READ | 6 | LOW-006, LOW-008, HIGH-006, MED-015, LOW-013, LOW-014 |
| **Batch 1b total** | **26** | |

Batch 1c (Task 90.5c, Sections 12-21, 27 in-scope findings):
| Classification | Count | IDs |
|---|---|---|
| VERIFIED REAL | 24 | HIGH-009, HIGH-010, HIGH-011, MED-016, MED-017, MED-018, MED-019, MED-020, MED-021, MED-022, LOW-016, LOW-017, MED-023, MED-024, MED-025, MED-026, HIGH-013, HIGH-014, HIGH-015, HIGH-016, MED-027, MED-028, MED-029, LOW-019 |
| VERIFIED FALSE | 0 | — |
| SCOPE-SHIFTED | 2 | LOW-018 (explicit dup of MED-011 — audit doc self-flags), MED-030 (KeyError mechanism wrong; silent-stale is the real failure mode) |
| NEEDS DEEP READ | 1 | HIGH-012 (closure-over-account_id race description doesn't match Python parameter binding semantics; needs deeper trace) |
| **Batch 1c total** | **27** | |

Batch 1d (Task 90.5d, Sections 22-28, 18 in-scope findings):
| Classification | Count | IDs |
|---|---|---|
| VERIFIED REAL | 18 | HIGH-017, HIGH-018, HIGH-019, HIGH-020, HIGH-021, MED-031, MED-032, MED-033, HIGH-022, HIGH-023, HIGH-024, HIGH-025, MED-034, MED-035, MED-036, MED-037, MED-038, MED-039 |
| VERIFIED FALSE | 0 | — |
| SCOPE-SHIFTED | 0 | — |
| NEEDS DEEP READ | 0 | — |
| **Batch 1d total** | **18** | |

Batch 1e (Task 90.5e, Section 29, 5 in-scope findings):
| Classification | Count | IDs |
|---|---|---|
| VERIFIED REAL | 5 | MED-040, MED-041, MED-042, LOW-020, LOW-021 |
| VERIFIED FALSE | 0 | — |
| SCOPE-SHIFTED | 0 | — |
| NEEDS DEEP READ | 0 | — |
| **Batch 1e total** | **5** | |

**Combined Sweep Total (batches 1a + 1b + 1c + 1d + 1e):**
| Classification | Count | Percentage |
|---|---|---|
| VERIFIED REAL | 74 | 87% |
| VERIFIED FALSE | 0 | 0% |
| SCOPE-SHIFTED | 4 | 5% |
| NEEDS DEEP READ | 7 | 8% |
| **Sweep total** | **85** | 100% |
| Duplicates consolidated | 2 (HIGH-016 → HIGH-010, LOW-018 → MED-011) | — |

Note: duplicate findings (HIGH-016, LOW-018) are counted in their VERIFIED REAL / SCOPE-SHIFTED categories for sweep math. Post-consolidation in Task 90.5e they are marked Status: DUPLICATE and excluded from active-finding totals. Actual unique open-finding count after dedup: **83 unique findings** (85 sweep total − 2 duplicates).

Batch 1a pattern observation: stark contrast with Sections 8-9 / Tasks 86-90 where ~5 of 14 examined findings were already-guarded. Sections 1-3 are 89% VERIFIED REAL. The earlier false-positive cluster was concentrated in the CRIT-level findings and "near-miss" guard cases; the broader Section 1-3 finding pile appears solidly anchored in real code state. Implication for Phase 3+: budget for real-fix tasks rather than pin-only tasks in these sections.

Batch 1b pattern observation: 73% VERIFIED REAL (19/26), 0% VERIFIED FALSE — confirms the "no near-miss guards in this band" pattern from batch 1a. The 6 NEEDS DEEP READ findings (LOW-006, LOW-008, HIGH-006, MED-015, LOW-013, LOW-014) cluster around concerns that need either per-call judgment (log levels), per-file inspection (pagination styles), runtime/concurrency verification (HIGH-006, MED-015), or browser-observable UI behavior (LOW-013, LOW-014). None look like "audit was wrong" candidates. The single SCOPE-SHIFTED (LOW-012) is the .env.example absence — bigger fix surface than audit's 1-line add. Across batches 1a+1b combined: 35 swept, 27 VERIFIED REAL (77%), 0 VERIFIED FALSE, 2 SCOPE-SHIFTED, 6 NEEDS DEEP READ.

Batch 1c pattern observation: 89% VERIFIED REAL (24/27), 0% VERIFIED FALSE — continues the established trend. Two SCOPE-SHIFTED (LOW-018 = explicit dup, MED-030 = wrong failure mechanism), one NEEDS DEEP READ (HIGH-012 closure-over-account_id race description that doesn't match Python parameter binding semantics). New observation: audit contains duplicate findings — HIGH-010 and HIGH-016 are the same crypto.py:54-58 finding filed twice across different sections. Audit's reported total severity counts may overstate by the duplicate count. Across batches 1a+1b+1c combined: 62 swept, 51 VERIFIED REAL (82%), 0 VERIFIED FALSE, 4 SCOPE-SHIFTED, 7 NEEDS DEEP READ.

Batch 1d pattern observation: 100% VERIFIED REAL (18/18), 0% VERIFIED FALSE, 0 SCOPE-SHIFTED, 0 NEEDS DEEP READ. Cleanest batch in the sweep — every finding in Sections 22-28 confirmed at the cited file with the cited failure mode. Line drift on 4 findings (HIGH-022, HIGH-024, MED-032 substantial; HIGH-021 minor) but underlying claims unchanged. No new duplicates found in this batch. Sweep complete across Sections 1-28 (80 findings, 78 unique post-dedup).

Batch 1e pattern observation: 100% VERIFIED REAL (5/5). Section 29 frontend-security findings (SRI, CSP, DB timeout, Cache-Control, SSE limit) all confirmed by absence — features not present that should be. No new duplicates, no CRIT severity escalations. Combined across all five batches: **85 swept (83 unique post-dedup), 74 VERIFIED REAL (87%), 0 VERIFIED FALSE, 4 SCOPE-SHIFTED, 7 NEEDS DEEP READ.** Audit's reported severity counts before dedup overstated by 2 (HIGH-016, LOW-018). The full audit-vs-current-code ledger is now consolidated and ready as Phase 3+ planning input.

## Top 5 Highest-Priority Fixes

1. **[HIGH-001]** No authentication on any API endpoint (full unauthorized access)
2. **[HIGH-010]** `core/crypto.decrypt` returns "" on InvalidToken / generic Exception — callers get empty credentials and silently attempt exchange auth with no key, leaving the engine running credential-less with no user-visible error
3. **[HIGH-028]** Bybit weight-tracker reconciliation missing — parallel to the Binance fix in Task 99 (HIGH-014). Bybit's `core/adapters/bybit/rest_adapter.py` has no `_reconcile_from_response()` override, so tracker state drifts from exchange-reported state under load (over- or under-throttling)
4. **[HIGH-029]** Parallel credential traceback leak — exchange creds (api/routes_accounts.py 5 entry points + core/account_registry.py decrypt sites) are not wrapped in SensitiveStr; same audit-cited shape as HIGH-024 (which Task 100 fixed for the narrower routes_connections path). Operator-credential leakage in tracebacks during account create/update flows
5. **[HIGH-030]** Parallel to HIGH-013 — Bybit's `core/adapters/bybit/rest_adapter.py:fetch_account` uses the same `info.get(field, 0) or 0` shape on five critical balance/equity fields, plus a structural nesting concern (info → result → list[0]). Silent equity-state corruption that downstream consumers can't distinguish from legitimate zero. Sister-finding to HIGH-028 (both Bybit-only follow-ups to Binance fixes).

Slot reshuffle rationale (Task 104b): HIGH-027 RESOLVED across Tasks 104a + 104b (calc-to-fill window backend + UI). All remaining slots shift up by one; new slot 5 is HIGH-030 — concrete Bybit hardening follow-up parallel to HIGH-028 (the two together form a future "Bybit hardening" bundle). Considered alternatives: HIGH-002 (architectural coupling, not money-at-risk); HIGH-031 (MEXC parallel) is read-only-adapter scope so lower priority than HIGH-030's order-placing Bybit path. HIGH-030 wins on operational impact — Bybit accounts placing trades on phantom-zero equity is the same failure mode HIGH-013 (Binance) closed in Task 102, just unfinished for Bybit.

Slot reshuffle rationale (Task 103): HIGH-008 RESOLVED this task (narrow AuthenticationError catch + auth_failed_accounts state). HIGH-010 / HIGH-028 / HIGH-029 each promote one slot. New slot 5: HIGH-029 — parallel credential traceback leak for the exchange-credential path, filed during Task 100 but explicitly deferred per narrow-scope decision. Concrete next-task target with a fix shape already prototyped (SensitiveStr class lives in core/security.py since Task 100; HIGH-029 is the "apply same pattern to the second credential pathway" follow-up). Considered alternatives: HIGH-002 (architectural coupling, not active money-at-risk) — pushed off Top-5; HIGH-030 / HIGH-031 (Bybit/MEXC account-field parallels filed Task 102.1) are smaller follow-ups; HIGH-011 already RESOLVED in Task 101. HIGH-029 wins because credential leakage in tracebacks is a security finding with concrete victims (operator credentials shown in support tickets / error logs), and the fix is a pure parallel of an already-shipped pattern.

## Top 5 Spaghetti Zones

| File | LOC | Concern |
|------|-----|---------|
| `core/order_manager.py` | ~864 | Mixed state mgmt, risk logic, order lifecycle, event emission |
| `core/db_orders.py` | ~842 | 20+ query methods, read/write/cache mixed |
| `core/data_cache.py` | ~820 | Position state + equity tracking + cache eviction |
| `core/platform_bridge.py` | ~810 | Adapter + enrichment + reconciliation |
| `core/database.py` | ~763 | Migration logic + DDL + data access + backfill |

---

## 1. Correctness & Bug Risks

### [CRIT-002] [Correctness] core/risk_engine.py:159-161
  Description: Division by zero in slippage calculation when `best_price` is 0
  Evidence: `est_slippage = max(0.0, (est_fill_price - best_price) / best_price)` — only `entry_price <= 0` is guarded (line 147), not `best_price`
  Impact: ZeroDivisionError crashes the risk calculator mid-trade
  Recommended fix: Add guard `if best_price <= 0: return 0.0, entry_price`
  Effort: small

### [CRIT-003] [Correctness] core/order_manager.py:654-668
  Description: Division by zero risk in proportional entry fee calculation; `max()` on potentially empty list
  Evidence: `exit_time = max(f["timestamp_ms"] for f in close_fills)` — can raise ValueError if close_fills empty after filtering. Fee ratio `total_close_qty / total_open_qty` guarded by ternary but expression evaluated before selection in some Python versions.
  Impact: Crash during position close, leaving inconsistent state
  Recommended fix: Guard `max()`/`sum()` with empty-list checks; use explicit if-else instead of ternary for division
  Effort: small
  Status: VERIFIED FALSE in commit 0068a8e (Task 86) — no production change required
  Verification note (Task 86):
    Claim 1 (max() on empty close_fills): The audit cited lines 654-668 but missed the early-return guard at lines 650-652:
        if not close_fills:
            log.warning("No closing fills for %s %s — skipping", symbol, direction)
            return
    Line 659's max() is unreachable with empty close_fills. Failure-before-fix verification confirmed: removing the line-650 guard produces `ValueError: max() iterable argument is empty` at line 655 (swallowed by the outer try/except at line 737). No bug exists in current code.

    Claim 2 (ternary div-by-zero "in some Python versions"): Python ternary expressions (PEP 308, 2003) have always short-circuited by selection. The expression `a / b if b > 0 else 0.0` does not evaluate `a / b` when `b == 0`, in any Python version. Operands here are plain floats; no comparison overload or NaN trickery applies. Failure-before-fix verification confirmed: removing the ternary `if total_open_qty else 0.0` produces `ZeroDivisionError` at the division site (also swallowed by the outer try/except). The ternary IS load-bearing — but the construction is correct as written. No bug exists in current code.

    Action taken: Three regression-pin tests added in tests/test_order_manager.py::TestClosePositionGuards to protect against future removal of the line-650 guard, the prop_entry ternary (lines 665-668), and the exit_price ternary (lines 655-658). No production code modified.

### [HIGH-004] [Correctness] core/data_cache.py:516
  Description: Inconsistent null-checks across portfolio recalculation
  Evidence: `rolling_peak` from `_fetch_rolling_peak_equity()` could return None; `max_eq` guarded at line 474 but downstream fields like `individual_margin_used` on positions may be uninitialized
  Impact: Incorrect drawdown metrics or crash on None arithmetic
  Recommended fix: Validate all position fields initialized; ensure rolling peak returns non-zero
  Effort: medium
  Status: VERIFIED FALSE in Task 90 — both halves already guarded in current code.
  Verification note (Task 90):
    Half 1 (rolling_peak may be None): _fetch_rolling_peak_equity at core/data_cache.py:75-106
    has return type `float`, not Optional. All three return paths produce a float — the
    `row[0] if row and row[0] is not None else 0.0` at line 102 explicitly handles the None
    case from MAX() over an empty set. Consumer at line 516 is also already guarded:
    `rolling_dd = (rolling_peak - total_equity) / rolling_peak if rolling_peak > 0 else 0.0`.

    Half 2 (individual_margin_used may be uninitialized): Field defaulted to 0.0 via
    @dataclass field at core/state.py:102. Explicit construction-site init at
    core/platform_bridge.py:629 (`prev.individual_margin_used if prev else 0.0`).
    SQLite column declared `REAL NOT NULL DEFAULT 0` at core/database.py:114. Never None
    on any construction path.

    Action: Four regression-pin tests added in tests/test_task90_high004_high005_pins.py
    to document the guards and catch future removal. No production change. Non-vacuous
    proof verified: removing the dataclass default OR the line-516 ternary causes the
    corresponding pin to fail.

### [HIGH-005] [Correctness] core/order_manager.py:526-562
  Description: TP/SL enrichment proceeds when mark price is 0 (uninitialized)
  Evidence: Guard at line 527 checks `mark`, sets flags, but lines 534-561 still process orders even if `mark == 0` — `min()` with distance-to-zero picks arbitrarily among equidistant orders
  Impact: Wrong TP/SL price displayed on open positions
  Recommended fix: Return early from enrichment if mark price is 0
  Effort: small
  Status: VERIFIED FALSE in Task 90 — early-return guard at lines 527-532 already short-circuits.
  Verification note (Task 90):
    Audit claimed lines 534-561 still process orders when mark == 0. Read of current code
    shows the early-return guard at lines 527-532:
        if not mark:
            pos.individual_tp_price = 0.0
            pos.individual_sl_price = 0.0
            pos.individual_tpsl = False
            continue
    fires for both None and 0/0.0 (via `if not mark`), zeroes the display fields, clears
    the flag, and `continue`s to the next position. The min() calls at lines 550/556 are
    unreachable when mark is falsy. The guard is the second statement in the loop body.

    Action: Three regression-pin tests added (zero, None defensive, and a sanity counter-
    test for valid marks that protects against future over-widening of the guard).
    No production change. Non-vacuous proof verified: removing the `if not mark` block
    causes the zero/None pins to fail by selecting the TP order at 110.0 against a
    mark=0 position (the exact failure mode the audit described — except the guard
    was already preventing it).

### [MED-001] [Correctness] core/order_manager.py:632-635
  Description: Position lookup falls back to entry_price=0.0 when position not in memory
  Evidence: `pos = next((p for p in app_state.positions if ...), None); entry_price = pos.average if pos else 0.0`
  Impact: Corrupted closed_position records with 0 entry price
  Recommended fix: Log warning; attempt DB lookup as fallback
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5a)
  Sweep evidence:
      pos = next(
          (p for p in app_state.positions if p.position_id == pos_id),
          None,
      )
      entry_price    = pos.average if pos else 0.0
  Sweep note: lines 632-636 confirm the unguarded fallback. No warning, no DB lookup. Real correctness bug — fires whenever the in-memory positions list is stale or empty at close-row build time. Trivially reachable post-restart with open positions.

### [MED-002] [Correctness] core/risk_engine.py:419
  Description: R:R ratio can overflow when est_loss approaches zero
  Evidence: `est_r = est_profit / est_loss if est_loss > 0 else 0.0` — no cap on result
  Impact: Extremely large R:R values causing display/analytics distortion
  Recommended fix: Cap at reasonable maximum (e.g., 100.0)
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5a)
  Sweep evidence (line 436):
      est_r      = est_profit / est_loss if est_loss > 0 else 0.0
  Sweep note: Ternary guards zero only; very-small-positive est_loss produces unbounded R:R. Confirmed at risk_engine.py:436 (audit cited :419, the actual statement is at :436 — minor line drift, semantic unchanged).

### [MED-043] [Correctness] core/risk_engine.py (estimate_vwap_fill level loop)
  Description: Missing qty <= 0 guard in level iteration — negative qty corrupts total_qty and est_fill_price
  Evidence: Loop computes `avail_usdt = price * qty` → `fill_usdt = min(remaining, avail)` → `fill_qty = fill_usdt / price`. Negative qty cascades through all three steps silently, accumulating negative total_qty and inflating remaining_usdt.
  Impact: If qty ever goes negative (upstream parsing bug or exchange glitch), est_fill_price is corrupted and feeds garbage into slippage estimation and position sizing.
  Recommended fix: Symmetric `qty <= 0` skip with log.warning, placed adjacent to existing price guard at line 117.
  Effort: small
  Status: RESOLVED in commit d21c58b (Task 85b)
  Discovered-during: Task 85b investigation
  Note: Task 85's discovery report described `price <= 0` as unguarded in this function; that claim was incorrect — the price guard exists at line 117-118. The actual defensive gap was on qty, addressed here. This note is in the permanent record to prevent future maintainers trusting the original misattribution.

### [LOW-022] [Observability] core/risk_engine.py:117-118
  Description: Silent skip of invalid orderbook levels in estimate_vwap_fill — no log emitted on corrupt exchange data
  Evidence: Pre-fix code was `if price <= 0: continue` with no log statement.
  Impact: Corrupt exchange data dropped with no telemetry. Position sizing anomalies downstream have no diagnostic trail back to the source orderbook glitch.
  Recommended fix: Add log.warning to existing skip; mirror with symmetric qty guard (see MED-043).
  Effort: small
  Status: RESOLVED in commit d21c58b (Task 85b)
  Discovered-during: Task 85b investigation

### [HIGH-026] [Reliability] core/order_manager.py:737-741
  Description: Outer try/except in _build_close_row_for_fill silently swallows any close-row build failure
  Evidence: `try: ... except Exception: log.exception(...)` at the end of the function catches every exception from the close-row build (e.g., empty close_fills bypassing the line-650 guard, ternary failures, schema/DB errors during insert_closed_position). On catch, the function returns without surfacing the failure to the caller, without emitting an engine_event, and without marking the position-close attempt as failed in any persisted state.
  Impact: A failed position close vanishes silently. Engine state diverges from reality: caller assumes close attempted (possibly succeeded), no signal to user, no forensic trail beyond the log line. In trading code, fail-quiet at the position-close boundary is the wrong default — caller cannot retry, reconcile, or surface to operator.
  Recommended fix: Surface the exception to caller (re-raise after logging) OR emit an engine_event("close_row_build_failed", ...) with position context AND return a structured failure indicator the caller can check. Decide which based on the existing caller-contract; either way the silent-swallow path must end. Coordinate with the broader "fail-loud in async paths" pattern (HIGH-007, HIGH-008).
  Effort: medium
  Status: RESOLVED in Task 95 — approach (a) enhanced: kept existing log.exception, added structured engine_event emission.
  Discovered-during: Task 86 failure-before-fix verification (commit 0068a8e)
  Implementation note (Task 95):
    Investigation found the current code already has `log.exception(...)` inside
    the except (at order_manager.py:751 pre-edit). The audit's "vanishes silently"
    framing overstated the silence — logs ARE emitted. But the real gap held:
    no structured engine_event, no UI surface, no queryable forensic trail
    beyond raw log scrollback.

    Caller analysis (both fire-and-forget):
      - process_fill at order_manager.py:599 uses asyncio.ensure_future(...);
        result not awaited, can't react to exceptions.
      - build_final_close_row at order_manager.py:782 awaits but discards return.
    Re-raise (approach c) buys nothing — neither caller can act on the exception.
    Narrowing the except (approach b) would require enumerating 5+ exception
    types across DB ops + dict access + arithmetic — smell.

    Fix: kept `log.exception` (preserves existing forensic trail), added
    inner `log_event(account_id, "close_row_build_failed", {...}, source="order_manager")`
    capturing symbol, direction, terminal_position_id, exchange_fill_id,
    exchange_order_id, error_type, error_msg. Inner try/except prevents
    event emission failure from masking the original exception.

    Schema: added "close_row_build_failed" to core/event_log.py EventType
    Literal whitelist.

    Tests (3 new, TestClosePositionSilentSwallow class — avoids the duplicate
    TestClosePositionGuards collision documented in Task 92.5):
      - test_silent_swallow_emits_close_row_build_failed_event: load-bearing
        pin. Forces RuntimeError via mocked get_position_fills; asserts both
        log.exception fires AND the engine event is emitted with full
        context payload. Failure-before-fix verified by reverting the
        event-emission block; pin failed with `len(failure_events) == 1`
        assertion error (0 events emitted). Restored.
      - test_normal_path_no_failure_event: anti-over-correction.
      - test_close_row_build_failed_event_type_registered: schema pin
        against future removal of the EventType whitelist entry.

### [CRIT-008] [Correctness] core/database.py:initialize() — missing pre_trade_log.calc_id inline ALTER
  Description: Legacy DB's pre_trade_log table was missing the calc_id column on fresh installs. The column is referenced by ≥5 production query sites including the v2.4.1-shipping frag_position_fills batch path (Task 88) and frag_exec_link.
  Evidence: No `ALTER TABLE pre_trade_log ADD COLUMN calc_id` in core/database.py's inline migrations list. Migration 005 adds the column only to per-account DBs, where pre_trade_log doesn't actually exist. Self-acknowledged at core/database.py:547-560 where a closed_positions backfill is wrapped in try/except OperationalError for this exact reason.
  Impact: frag_position_fills and frag_exec_link history-page routes raise OperationalError ("no such column: calc_id") on fresh installs. Task 88's batch refactor (CRIT-001 RESOLVED) inherits this gap — perf win is unreachable until the column lands. v2.4.1-blocking.
  Recommended fix: Add `ALTER TABLE pre_trade_log ADD COLUMN calc_id TEXT` to the inline migrations list at core/database.py:497-513, alongside the existing orders.calc_id / fills.calc_id / closed_positions.calc_id ALTERs. The existing try/except shell at lines 517-526 handles re-run idempotency via the "duplicate column name" swallow.
  Effort: small (1 LOC + regression tests)
  Status: RESOLVED in Task 88.1 — ALTER added alongside sibling calc_id ALTERs; regression test pins post-init column existence + round-trip on a fresh DatabaseManager.
  Discovered-during: Task 88.1 Step 1 column-existence gate (after Task 88 batch refactor surfaced the dependency)

### [HIGH-027] [Correctness] core/exec_link.py — calc-to-fill matching has no time window
  Description: The engine matches incoming fills to pre_trade_log entries implicitly via price/symbol proximity in compute_exec_match. Fills carry no explicit calc_id; the engine assigns one post-hoc by finding the best-matching pretrade. The matching function has no time-window check, and pre_trade_log entries persist indefinitely with no expiration. A stale pretrade from hours or days ago can match a fresh fill if price and symbol coincidentally align, retroactively tagging the new fill with the wrong calc_id.
  Evidence:
    - compute_exec_match (core/exec_link.py) parameters are fill + pretrade + order_type. No fill_ts or pretrade_ts in the matching logic; no MAX_LINK_WINDOW constant or config knob.
    - pre_trade_log table has no expiration column; no cleanup or soft-expiration job exists.
    - Fills arrive via WS without explicit calc_id propagation. Matching is fully implicit (engine is passive observer + post-hoc linker).
  Impact: Stale plan-to-fill linkages contaminate intent-vs-execution analytics. R:R metrics, drawdown attribution, and "did the plan work?" measurements compute against the wrong intended parameters. A discretionary trader who calculated an idea earlier, abandoned it, and traded a different setup later can have the new fill incorrectly linked to the old plan if prices align — silently corrupting all downstream metrics for that trade.
  Recommended fix (UI + backend):
    Backend:
      1. Add per-account `link_window_seconds` field to accounts table (default 6h = 21600s; operator-tunable).
      2. Add per-pretrade `link_window_seconds_override` (nullable) on pre_trade_log; falls back to account default if NULL.
      3. compute_exec_match accepts fill_ts and pretrade_ts; computes effective window as (override ?? account_default); rejects candidates where (fill_ts - pretrade_ts) > effective_window.
      4. Update caller(s) of compute_exec_match to pass timestamps.
      5. Optional defense-in-depth: soft-expire pre_trade_log entries past their effective window (set expired=True); UI surfaces "expired plan, no link possible" badge.
    Frontend (HTMX):
      6. Account settings page: add link-window editor for the active account's default. Hours/minutes input. Standard save flow.
      7. Calculator UI: add per-calculation override input near trade-copyable setups area. Default to "use account default"; allow override per calc.
      8. Live countdown element near the calculator showing time remaining until link eligibility expires. Format like "Linkable for: 5h 23m" or "EXPIRED — recalculate to link new fills." Updates via HTMX polling (or client-side timer if polling cost matters).
    Tests:
      9. Pin: in-window match succeeds; past-window match rejected.
      10. Pin: account-level default applies when no override.
      11. Pin: per-calc override takes precedence over account default.
      12. Pin: UI countdown reflects effective window (could be a screenshot/HTML-snapshot test or a backend test asserting the rendered template carries correct values).
  Open design decisions:
    - Account-default value (suggest 6h for mixed crypto futures; tunable).
    - Override granularity: free-form seconds vs preset bins (30m / 1h / 4h / 24h)?
    - Countdown display format: precise countdown (HH:MM:SS) or coarse ("expires in ~4h")? Precise is more informative but more visual noise.
    - Expired-state UI: show stale calc in greyed-out state vs hide it vs separate "expired ideas" log?
    - Countdown UI refresh mechanism: HTMX polling (cleaner, server-rendered) vs client-side timer (lower server load, more JS). For sub-minute precision, client-side is probably necessary.
    - DB schema migration for the new accounts column + pre_trade_log column — needs to be defensive of existing data (default to 6h for existing rows).
  Effort: medium-large. ~40-60 LOC core/exec_link.py + ~20 LOC callers + ~30 LOC backend settings persistence + ~80-150 LOC frontend (settings editor + calc override + countdown element) + ~40 LOC migration + tests. Roughly 200-300 LOC total + tests.
  Status: RESOLVED in Tasks 104a + 104b — backend (schema, compute_exec_match window logic, caller updates) in 104a; frontend (account settings editor, per-calc override input, live countdown with 4-state UI) in 104b.
  Discovered-during: Task 93 HIGH-019 verification (architecture reframe based on user workflow clarification); refined in Task 93.1.
  Refined-during: Task 94.1 (operator UX requirements clarification)
  Target phase: Phase 4 (reliability + analytics correctness)
  Backend status (Task 104a): RESOLVED — DB schema + compute_exec_match window logic + caller updates landed.
    Implementation note:
      - core/database.py: `accounts.link_window_seconds INTEGER NOT NULL DEFAULT 21600` (6 h) added to CREATE TABLE + ALTER migration. `pre_trade_log.link_window_seconds_override INTEGER DEFAULT NULL` added to CREATE TABLE + ALTER migration. Legacy DBs catch the ALTERs on startup; idempotent via the existing `duplicate column name` swallow.
      - core/exec_link.py: `DEFAULT_LINK_WINDOW_SECONDS = 21600` module constant. compute_exec_match signature extended with fill_ts_ms, pretrade_ts_ms, account_link_window_seconds (kwargs with backwards-compatible defaults). Effective window = `pretrade['link_window_seconds_override']` (if non-NULL) else `account_link_window_seconds`. Hard-reject (return None) when `(fill_ts_ms - pretrade_ts_ms) / 1000 > effective_window`. Boundary: strict `>` so elapsed == window still matches. get_exec_link_status forwards the new args and reports `None` from compute_exec_match as `('unlinked', 0)`. exec_link_confirmed fast-path preserved — confirmed links bypass the window check (an operator confirmation is authoritative).
      - api/routes_orders.py: 2 call sites updated. frag_position_fills (line ~190) — one-shot `_account_link_window_seconds(aid)` lookup before the per-fill loop; per-fill `_pretrade_ts_to_ms(ptl)` ISO→ms conversion; passes timestamps + window to get_exec_link_status. frag_exec_link (line ~255) — same pattern at the single-fill site, passes to compute_exec_match.
      - Two new helpers in routes_orders.py: `_account_link_window_seconds(account_id)` async one-shot SELECT (defensive: falls back to DEFAULT_LINK_WINDOW_SECONDS on missing-row), `_pretrade_ts_to_ms(pretrade)` ISO parse with timezone-naïve → UTC fallback (defensive: returns None on parse failure, which skips the window check rather than crashing).
    Tests (tests/test_task104a_exec_link_window.py, 16 cases):
      - TestComputeExecMatchWindow (8): within-window match; past-window reject; per-calc override precedence; zero-window rejects all; window boundary strict `>`; missing timestamps skips check; partial-args (only one ts) skips check; DEFAULT_LINK_WINDOW_SECONDS == 21600 pin.
      - TestGetExecLinkStatusWindow (3): past-window reports 'unlinked'; confirmed-fill bypasses window; within-window reports 'linked'.
      - TestSchemaPins (3): fresh-init has accounts.link_window_seconds with default 21600; fresh-init has pre_trade_log.link_window_seconds_override nullable; legacy-shape DB picks up both columns via ALTER migration.
      - TestCallerWiring (2): source pin asserts both call sites pass fill_ts_ms / pretrade_ts_ms / account_link_window_seconds kwargs; helper-function presence pin.
    Frontend status: RESOLVED in Task 104b.
    Overall HIGH-027 status: RESOLVED (Task 104a backend + Task 104b frontend).
    Drift footgun captured: core/database.py:`_CREATE_STATEMENTS.strip().split(";")` naively splits on every `;`, including ones inside SQL line comments. Initial Task 104a commit hit `sqlite3.OperationalError: incomplete input` from a literal `;` inside a `-- ...` comment block. Resolved by rewriting the comments without semicolons. Filed as MED-046 in Task 104b for a separate cleanup.

  Frontend implementation note (Task 104b):
    - core/exec_link.py: new compute_link_window_status(pretrade_ts_ms, account_link_window_seconds, *, override_seconds, exec_link_confirmed, now_ms) → {status, effective_window_s, remaining_s, expires_at_ms}. Four-state machine: LINKABLE / EXPIRING_SOON (< 300 s remaining) / EXPIRED / LINKED_CONFIRMED. LINKED_CONFIRMED takes precedence over EXPIRED so an operator-confirmed past-expiry link renders as "Linked (confirmed)" not "Expired" (the Task 104a latent nuance, now load-bearing in the UI). EXPIRING_SOON_THRESHOLD_S = 300 s and MAX_LINK_WINDOW_SECONDS = 86400 s constants pinned in tests.
    - core/account_registry.py: update_account gains link_window_seconds kwarg; load_all reads the column from DB into cache; _account_meta exposes it to UI fragments.
    - core/db_trades.py:insert_pre_trade_log: INSERT column list + values dict now include link_window_seconds_override (NULL when blank/zero from UI). Forwarded by handle_risk_calculated from the calc dict.
    - api/routes_accounts.py:update_account_detail: new link_window_seconds Form param. Server-side validation rejects < 1 with "must be at least 1 second" and > 86400 with "must be ≤ 86400 s (24 h)" — both return 400 with an inline error fragment so the operator sees the rejection on the existing #acct-msg target.
    - api/routes_calculator.py:calculate_risk: new link_window_seconds_override Form param (string, blank-tolerant). Server-side parse: blank/non-integer → 400; ≤ 0 → treated as "no override" (None); > MAX → 400. Parsed int (or None) attached to calc["link_window_seconds_override"] before publish.
    - api/routes_calculator.py: new endpoint GET /calculator/link-window-status/{calc_id} returns the countdown fragment. Looks up pre_trade_log row (timestamp + override) + accounts.link_window_seconds + exec_link_confirmed via 3 small reads; passes to compute_link_window_status; renders link_window_countdown.html.
    - templates/fragments/account_detail.html: new "Calc Link Window" section with preset dropdown (30 m / 1 h / 4 h / 6 h default / 12 h / 24 h max) + automatic "Custom (Ns)" entry when the persisted value isn't a preset. Submits as the existing form's link_window_seconds field.
    - templates/calculator.html: per-calc link_window_seconds_override <select> dropdown in the form, same preset shape, blank-option for "use account default". Submits with the calc POST.
    - templates/fragments/calc_result.html: countdown container with hx-trigger="load, every 5s", initial GET to /calculator/link-window-status/{calc_id}, swap=outerHTML. Renders only when calc.calc_id and calc.eligible are set.
    - templates/fragments/link_window_countdown.html: new fragment, four state branches with distinct border colors (green / amber / red / green). Hybrid display format — precise (Mm Ss) when remaining < 1 h or in EXPIRING_SOON, coarse (~Hh Mm) when > 1 h LINKABLE. Active states (LINKABLE, EXPIRING_SOON) carry their own hx-trigger="every 5s" so the polling continues; terminal states (EXPIRED, LINKED_CONFIRMED) intentionally stop polling.

  Tests (tests/test_task104b_link_window_ui.py, 20 cases):
    - TestComputeLinkWindowStatus (8): LINKABLE / EXPIRING_SOON / EXPIRED / LINKED_CONFIRMED state pins; LINKED_CONFIRMED precedence over EXPIRED; per-calc override beats account default; missing pretrade_ts returns EXPIRED defensively; zero window returns EXPIRED; constants pinned.
    - TestAccountLinkWindowEndpoint (4): handler accepts the new Form param; validates lower bound (< 1 → 400); validates upper bound via MAX_LINK_WINDOW_SECONDS; end-to-end registry update persists to DB + cache.
    - TestCalculatorOverrideWiring (5): handler accepts override; attaches to calc dict; insert_pre_trade_log persists the column; calculator template includes the input; endpoint registered.
    - TestTemplateWiring (3): calc_result.html includes countdown polling element; account_detail.html has the link-window section; countdown fragment renders all four states with correct color cues.

  Design decisions (per task spec):
    1. Account settings input format: **Option C (preset bins + automatic custom display).** Operators with different trading styles (scalp / swing / discretionary) covered by presets; custom appears automatically when the persisted value is non-preset, so existing custom values round-trip without surprise.
    2. Per-calc override input format: **same preset shape as Option C.** Consistency with the account settings UI. Empty option submits as blank → server treats as "use account default."
    3. Refresh mechanism: **HTMX polling at 5 s.** Operator typically has 1-3 active calcs at most; 5 s gives "I have N hours/minutes left" precision without polling load. Active states carry hx-trigger inside the fragment so the polling self-perpetuates; terminal states (EXPIRED, LINKED_CONFIRMED) stop polling deliberately — no point pinging the server when nothing changes.
    4. Display format: **hybrid.** Precise (Mm Ss) when remaining < 1 h or in EXPIRING_SOON; coarse (~Hh Mm) when > 1 h LINKABLE. Operators glance for awareness, focus when expiry is near.
    5. State transitions: **four-state model.** LINKED_CONFIRMED takes precedence over EXPIRED — confirmed-past-expiry link is real, not stale.
    6. Upper bound: **MAX_LINK_WINDOW_SECONDS = 86400 (24 h).** Anything beyond a day stretches the "linkable" semantics past operator-plausible workflows; better to force recalc than allow stale matches.

### [HIGH-028] [Reliability] core/adapters/bybit/rest_adapter.py — weight-tracker reconciliation missing (Bybit)
  Description: Parallel to HIGH-014's Binance fix. The Bybit adapter does not parse Bybit's response headers to reconcile the in-memory weight-tracker against exchange-reported usage. Tracker state drifts from actual exchange state under load, leading to potential rate-limit misjudgments — either over-throttling (cost: missed trades) or under-throttling (cost: API ban / disconnect).
  Evidence: Task 99 wired `_reconcile_from_response()` for Binance (HIGH-014 fix); Bybit override was explicitly deferred ("different header set"). `core/adapters/bybit/rest_adapter.py` has no `_reconcile_from_response()` implementation. The base-class default in `core/adapters/base.py:164-173` returns None (no-op), so every Bybit call leaves the tracker estimate uncorrected.
  Impact: Bybit-side rate-limit decisions made against drifted local state. Under load, the adapter could either over-throttle (missed trades) or under-throttle (API ban / disconnect). Symmetric in shape to HIGH-014's Binance impact before fix.
  Recommended fix: Implement Bybit-specific `_reconcile_from_response()` override parsing Bybit's response headers (likely `X-Bapi-Limit-Status` / `X-Bapi-Limit` / `X-Bapi-Limit-Reset-Timestamp`; confirm against current Bybit API docs before implementation). Match the structural shape of Binance's override in `core/adapters/binance/rest_adapter.py`: read from `self._ex.last_response_headers`, case-insensitive lookup, bounds-check parsed integer, call `tracker.reconcile(used, reset_time_ms)`.
  Effort: small. ~20-30 LOC + tests, parallel to Binance's HIGH-014 fix.
  Status: OPEN — deferred from Task 99 (HIGH-014 was Binance-only). Confirm Bybit response-header schema before implementation.
  Discovered-during: Task 99 HIGH-014 implementation (Binance fix scoped explicitly to one adapter; Bybit parallel work needed but different headers required separate task).
  Cross-reference: HIGH-014 (parent finding; Binance fix).
  Target phase: Phase 4 (slot as follow-up to HIGH-014) or defer to Phase 7 if Bybit traffic is light enough to not warrant near-term.
  Filed-during: Task 99.1

### [HIGH-029] [Security] api/routes_accounts.py + core/account_registry.py — parallel credential traceback leak (exchange creds)
  Description: Parallel to HIGH-024's connections-credentials fix. The exchange-credential pathway (api_key + api_secret for the exchange adapter, distinct from the 3rd-party data provider keys covered by HIGH-024) is unwrapped: raw `str` flows from form handlers into `account_registry.add_account` / `update_account` and into the cache without SensitiveStr protection. The same traceback-leak failure mode as HIGH-024 applies — an exception in `encrypt()`, `db.add_account()`, or downstream initialization renders the raw `api_key` / `api_secret` in stack-frame locals.
  Evidence (entry points):
    - `api/routes_accounts.py:44-45` (create_account) — api_key + api_secret from Form
    - `api/routes_accounts.py:60-61` (update_account) — api_key + api_secret from Form
    - `api/routes_accounts.py:198-199` (add_account_modal) — api_key + api_secret from Form
    - `api/routes_accounts.py:229-230` (test_account_preview) — api_key + api_secret from Form
    - `api/routes_accounts.py:306-307` (update_account_detail) — api_key + api_secret from Form
    - `core/account_registry.py:61-62` — decrypt() output for both fields, cached at :75-76, :166-167, :205-207
    Plus `core/crypto.decrypt` is the single chokepoint for ALL credential decryption (connections and accounts).
  Impact: Identical to HIGH-024 (credential exposure in error logs / tracebacks) but with broader surface — exchange credentials gate the actual trading path, so leakage is operationally more severe than 3rd-party data keys.
  Recommended fix: Choose approach (a) wrap at each form-handler entry point (5 sites) + ensure account_registry preserves wrapping through `add_account` / `update_account` then unwraps at the cache boundary; OR (b) wrap at the `core/crypto.decrypt` return + at each form-handler entry, then explicitly `.unwrap()` at every site that builds httpx/CCXT URLs and headers (broader change; defense-in-depth across both connection paths and exchange paths).
  Effort: medium. Approach (a) ≈ 50-80 LOC across routes_accounts.py + account_registry.py. Approach (b) ≈ 100-150 LOC plus discovery work for every unwrap site.
  CRITICAL caveat for implementer: urllib.parse.urlencode / httpx URL builders serialize SensitiveStr via the masked `__str__` override (verified: `urlencode({"k": SensitiveStr("v")})` → `k=%3Cmasked%3E`). Anywhere a SensitiveStr propagates into URL params or HTTP headers must `.unwrap()` first, or the live HTTP request will carry the literal `<masked>` string and authentication will fail. The narrow Task 100 fix avoided this by unwrapping at the cache boundary in connections_manager.upsert.
  Status: OPEN — narrow audit scope at HIGH-024 explicitly cited only `api/routes_connections.py`; the parallel exchange-credential leak surface was not in HIGH-024's text. Filed as separate finding rather than scope-shifting Task 100.
  Discovered-during: Task 100 HIGH-024 investigation (entry-point inventory revealed the parallel exchange-credential surface).
  Cross-reference: HIGH-024 (parent finding; same wrap technique applies). The `SensitiveStr` class in `core/security.py` is already in place — implementer reuses, doesn't redesign.
  Target phase: Phase 4 follow-up or Phase 5. Approach (a) preferred (smaller blast radius, parallel to Task 100's narrow-scope pattern).
  Filed-during: Task 100

### [HIGH-030] [Correctness] core/adapters/bybit/rest_adapter.py — Bybit account-field validation missing (parallel to HIGH-013)
  Description: Parallel to HIGH-013's Binance fix. Bybit's V5 fetch_account uses the same `info.get(field, 0) or 0` pattern for critical balance/equity fields without validation. If Bybit's response omits a field or returns null, the account state silently corrupts to 0, producing the same downstream consequences identified in HIGH-013's downstream analysis: risk_engine.py:351 falls back to 1.0 phantom equity (micro-positions), or BOD/SOW init freezes at 0 (data_cache.py:670).
  Evidence (core/adapters/bybit/rest_adapter.py fetch_account, lines 86-99):
      total_equity = float(account.get("totalEquity", 0) or 0)
      available = float(account.get("totalAvailableBalance", 0) or 0)
      unrealized = float(account.get("totalPerpUPL", 0) or 0)
      initial_margin = float(account.get("totalInitialMargin", 0) or 0)
      maint_margin = float(account.get("totalMaintenanceMargin", 0) or 0)
      ...
      for coin in coins:
          if coin.get("coin") == "USDT":
              total_equity = float(coin.get("equity", total_equity) or total_equity)
              available = float(coin.get("availableToWithdraw", available) or available)
              unrealized = float(coin.get("unrealisedPnl", unrealized) or unrealized)
  Additional structural concern unique to Bybit: the response is deeply nested (`info → result → list[0]`). Survey confirmed at lines 78-84 — if `info` is missing, OR `result` is missing, OR `list` is empty, the `account` dict is `{}` and ALL fields cascade-default to 0 silently. The fix must validate the structural path as well as the leaf fields.
  Critical-field set (parallel to Binance's totalWalletBalance + availableBalance): totalEquity (or per-coin USDT equity) + totalAvailableBalance (or per-coin availableToWithdraw). Non-critical (warn + default): totalPerpUPL, totalInitialMargin, totalMaintenanceMargin.
  Impact: Identical to HIGH-013 — silent equity-state corruption that downstream consumers can't distinguish from legitimate zero state. Risk-engine sizing-collapse OR frozen BOD/SOW baseline.
  Recommended fix: Apply HIGH-013's Shape A (raise ValueError on missing critical fields) for totalEquity + totalAvailableBalance, plus a structural-path check that raises if `info["result"]["list"][0]` is missing/empty (Bybit-specific addition). Apply Shape B (default + log) for the three non-critical fields. Match the structure of Binance's fix in core/adapters/binance/rest_adapter.py:113-150.
  Effort: small. ~25-35 LOC (slightly more than HIGH-013's Binance fix due to the structural-path validation); ~6-7 tests parallel to TestBinanceFetchAccountCriticalFieldValidation.
  Status: OPEN — deferred from Task 102 (HIGH-013 was Binance-only). Critical-field set + structural-path validator confirmed in Task 102.1 survey.
  Discovered-during: Task 102 HIGH-013 implementation (Binance fix surfaced parallel concern); confirmed in Task 102.1 source survey.
  Cross-reference: HIGH-013 (parent finding; Binance fix). HIGH-028 (sibling Bybit-deferred work from Task 99 — pairs naturally into a "Bybit hardening" bundle task).
  Target phase: Phase 4 (alongside HIGH-028) or Phase 5. Bundling HIGH-028 + HIGH-030 is reasonable — both are Bybit-only follow-ups to Binance fixes from the same audit pass.
  Filed-during: Task 102.1

### [HIGH-031] [Correctness] core/adapters/mexc/rest_adapter.py — MEXC account-field validation missing (parallel to HIGH-013)
  Description: Same shape as HIGH-013 and HIGH-030. MEXC's fetch_account uses `usdt.get(field, 0) or 0` for the two critical fields (total / free). Plus a structural-path concern parallel to Bybit's: `usdt = raw.get("USDT", raw.get("total", {}))` falls back to an empty dict if both keys are missing, cascading all fields to 0 silently.
  Evidence (core/adapters/mexc/rest_adapter.py fetch_account, lines 111-119):
      raw = await self._run(self._ex.fetch_balance)
      usdt = raw.get("USDT", raw.get("total", {}))
      return NormalizedAccount(
          currency="USDT",
          total_equity=float(usdt.get("total", 0) or 0),
          available_margin=float(usdt.get("free", 0) or 0),
          unrealized_pnl=0.0,  # CCXT doesn't split this for MEXC
      )
  Critical-field set: usdt["total"] (→ total_equity), usdt["free"] (→ available_margin). Plus the structural-path: `raw.get("USDT")` must succeed.
  Impact: Reduced relative to HIGH-013 / HIGH-030 because the MEXC adapter is read-only (capabilities.orders=False at adapter init — Task 9). Phantom-zero equity does not gate position placement for MEXC accounts; the harm is bounded to display, analytics, BOD/SOW init, and any downstream calculations that read MEXC-account total_equity (e.g., aggregate portfolio reporting). Still a real silent-corruption vector for the dashboards that DO consume MEXC equity.
  Recommended fix: Apply HIGH-013's Shape A for both fields (total + free) plus a structural-path check for the USDT/total fallback. Smaller footprint than HIGH-030 because there are no non-critical fields to handle (MEXC only exposes the two; unrealized_pnl is hardcoded to 0.0).
  Effort: small. ~15-20 LOC + ~4 tests.
  Status: OPEN — deferred from Task 102 (HIGH-013 was Binance-only). Survey confirmed in Task 102.1.
  Discovered-during: Task 102 HIGH-013 implementation; confirmed in Task 102.1 source survey.
  Cross-reference: HIGH-013 (parent finding); HIGH-030 (sibling Bybit fix; HIGH-013 + HIGH-030 + HIGH-031 are the three adapters in the "adapter response sanity" family). The "more than 2 additional adapters" stop-and-report criterion does not apply — adapter count is 3 total (binance, bybit, mexc) and HIGH-013 covered the first.
  Target phase: Phase 5 or later. Lower priority than HIGH-030 due to read-only adapter scope (no direct trading impact).
  Filed-during: Task 102.1

### [MED-044] [Maintainability] core/ws_manager.py:_reconnect_user (recursion vs iteration)
  Description: _reconnect_user uses async tail-recursion for retry loop. Bounded by config.WS_RECONNECT_ATTEMPTS=15 today, so safe, but stack depth scales linearly with the config value.
  Evidence: `await _reconnect_user(attempt + 1)` at line 496; bound via line-477 guard. If WS_RECONNECT_ATTEMPTS were ever raised to several hundred (e.g., to "be more generous about retries"), Python's default recursion limit (1000 frames, plus async coroutine overhead) could become a real risk.
  Impact: Defense-in-depth concern only at current config. Real failure mode if config is bumped.
  Recommended fix: Convert tail-recursion to a `while` loop in _reconnect_user. ~10 LOC. Preserves all existing behavior; eliminates the recursion-depth dependency on a config value.
  Effort: small
  Status: OPEN — target Phase 4 alongside other reliability hardening
  Discovered-during: Task 89 (CRIT-005 verification surfaced the recursion-vs-iteration concern)

### [MED-046] [Reliability] core/database.py — _CREATE_STATEMENTS.split(";") fragile against SQL line-comment semicolons
  Description: core/database.py:465 splits the CREATE TABLE blob on ";" to execute each statement separately. The split is naive — it includes semicolons that appear inside SQL line comments (`-- ... ;`), producing incomplete statement fragments that sqlite3 rejects with "incomplete input".
  Evidence: Task 104a hit `sqlite3.OperationalError: incomplete input` during the test run because two added `-- ...` comments contained semicolons ("...link_window_seconds; NULL → use account default" and "...crypto-futures swing/intraday workflows; tunable via..."). Resolved inline by rewriting the comments without semicolons, but the underlying fragility persists for any future contributor adding multi-sentence SQL comments.
  Impact: Loud failure mode (test errors during init, fresh-install fails to come up) rather than silent data corruption. Practical impact is wasted developer time + a debugging side-quest the first time someone hits it.
  Recommended fix: Either (a) switch from split-and-execute-each to sqlite3's `executescript()`, which handles full SQL blobs with comments correctly, OR (b) strip line comments before splitting. Option (a) is simpler and idiomatic. The migration list is already iterated as a Python list rather than a SQL blob, so this only affects the CREATE TABLE bootstrap.
  Effort: small. ~5-10 LOC + a regression test that exercises a SQL blob containing a comment with a semicolon.
  Status: OPEN — captured for future cleanup; non-blocking but compounding.
  Discovered-during: Task 104a HIGH-027 backend implementation.
  Filed-during: Task 104b
  Target phase: Phase 5 or any future task that touches database.py.

### [MED-045] [Reliability] main.py:50 — log rotation fails on Windows under multi-process
  Description: Python's stdlib RotatingFileHandler uses os.rename for rotation, which fails on Windows when another process holds the file open. Multi-worker uvicorn (or any subprocess that opens the log file) triggers PermissionError [WinError 32] on every rotation attempt, causing the rotation to silently fail. Log file grows unbounded; rotation-attempt error spam appears in stderr.
  Evidence: Runtime stack trace from engine startup, captured 2026-05-18 after commit 1cb8787 (Task 103). PermissionError at logging/handlers.py inside os.rename(source, dest) — source file held by a uvicorn worker subprocess (visible in call stack: multiprocessing.spawn → uvicorn server → engine startup).
  Impact:
    - Logfile grows unbounded; disk-space exhaustion possible over long-running deployments.
    - stderr / console noise on every rotation attempt after the maxBytes threshold is reached.
    - Historical log integrity degrades — rotation discipline is broken, so investigating past incidents requires manually managing the growing logfile.
    - Affects both development (cmd-window noise) and production (audit-trail reliability).
  Recommended fix: Replace logging.handlers.RotatingFileHandler with ConcurrentRotatingFileHandler from the concurrent-log-handler PyPI package. Drop-in replacement that uses OS-level file locks (msvcrt on Windows, fcntl on Unix) to coordinate rotation across processes. Single-line config change in main.py + add concurrent-log-handler to requirements.txt.
  Effort: small. ~5 LOC + dependency addition.
  Status: RESOLVED in Task 103.5 — ConcurrentRotatingFileHandler swap.
  Implementation note: main.py:46-65 — `from concurrent_log_handler import ConcurrentRotatingFileHandler` replaces the stdlib import; handler instantiation passes the same maxBytes (10 MiB), backupCount (5), encoding (utf-8) plus `use_gzip=False` to match prior behavior. core/log_formatter.py docstring updated to reference the new handler. requirements.txt pinned to `concurrent-log-handler>=0.9.20,<1.0` (installed at 0.9.29). Single handler — no other RotatingFileHandler instances in the codebase.
  Tests (tests/test_task103_5_log_handler.py, 8 cases):
    - TestLogHandlerClass (3): source-pin asserts main.py imports ConcurrentRotatingFileHandler; anti-revert pin asserts the old stdlib import is NOT present; instance check asserts main._json_handler is a ConcurrentRotatingFileHandler.
    - TestLogHandlerConfigPreserved (3): maxBytes, backupCount, encoding all unchanged after the swap.
    - TestSingleProcessRotationSmoke (2): real ConcurrentRotatingFileHandler with maxBytes=200 + 10 log records → rotated .1 file appears; handler.close() does not raise on empty-write path (lifespan-shutdown safety).
  Manual smoke: cross-process rotation can't be unit-tested cheaply; verify post-commit by starting the engine and confirming no PermissionError [WinError 32] in the console after log rotation should have triggered.
  Discovered-during: Runtime observation by operator after Task 103 commit (1cb8787).
  Target phase: Phase 4 (infrastructure follow-up, addressed same commit).

---

## 2. Security

### [HIGH-001] [Security] main.py / api/router.py
  Description: No authentication or authorization on any API endpoint
  Evidence: No middleware, no `Depends(verify_token)`, no session validation. All routes — including account creation, trading history, and admin — are fully open.
  Impact: Any network user can access all accounts, balances, positions, and trading endpoints
  Recommended fix: Add FastAPI security middleware (JWT or session-based) before all routes
  Effort: large
  Sweep classification: VERIFIED REAL (Task 90.5a)
  Sweep evidence: grep of `Depends|HTTPBearer|verify_token|JWT|authenticate` across main.py: 0 hits. Across api/: only api/routes_platform.py has any auth-adjacent code (and that's platform identification, not request auth).
  Sweep note: Real for the as-written claim. Note: this is a single-user-deployment app per the Overall Verdict ("operationally solid for a single-user deployment"); the audit's severity weighting may treat this differently when multi-user is a target. Status quo of "fully open" is accurate either way.

### [MED-003] [Security] api/routes_orders.py:46-50, api/routes_history.py:70-75
  Description: `sort_by` and `sort_dir` parameters passed to DB layer without whitelist validation in some routes
  Evidence: Query helpers do whitelist `sort_by` against `_ALLOWED_SORT_COLS` sets — but the route handlers don't validate before passing. Defense-in-depth gap.
  Impact: SQL injection if whitelist is bypassed or new routes skip it
  Recommended fix: Validate at route level; add `sort_dir in ("ASC", "DESC")` check in routes
  Effort: small
  Status: RESOLVED in Task 101 — shared validator at core/sql_safety.py:validate_sort_params; wired into all 7 sortable route handlers in routes_orders.py (4) + routes_history.py (3).
  Implementation note: validate_sort_params(sort_by, sort_dir, allowed_cols) raises HTTPException(400) on unknown column or invalid direction; upper-cases the direction so the DB-layer match against ("ASC", "DESC") doesn't need its own normalization. Routes call it with the appropriate DB-layer sort-cols set (db._ORDERS_SORT_COLS / db._FILLS_SORT_COLS / db._CLOSED_POS_SORT_COLS / db._EXCHANGE_HISTORY_SORT_COLS / db._PRE_TRADE_SORT_COLS / db._TRADE_HISTORY_SORT_COLS). Visible 400 replaces the prior silent fallback to a default — operator can now see when bad input is rejected. routes_analytics.py:296-317 was inspected but does in-Python sorting (not SQL ORDER BY) and already has an inline whitelist; out of MED-003 scope.
  Tests (tests/test_task101_sql_injection_defense.py): TestValidateSortParams (7 cases: valid/invalid combos, lowercase upper-casing, injection in sort_by, injection in sort_dir, empty-string sort_dir), TestRouteWiring (4 cases: import + call-count pins for both modules), TestRoutesRejectInjection (3 e2e cases via TestClient: injection payloads return 400, valid request returns 200).
  Sweep classification: VERIFIED REAL (Task 90.5a)
  Sweep evidence (routes_orders.py:43-50):
      sort_by: str = "created_at_ms", sort_dir: str = "DESC",
      ...
      rows, total = await db.query_open_orders(
          account_id=app_state.active_account_id,
          sort_by=sort_by, sort_dir=sort_dir, ...
      )
  Sweep note: Route accepts sort_by/sort_dir as raw strings and forwards them unchanged. DB layer at core/db_orders.py uses _ORDERS_SORT_COLS / _FILLS_SORT_COLS sets to whitelist, so today the path is safe end-to-end. But the route-level gap is real defense-in-depth — any future query helper that forgets the whitelist becomes injectable from this route. Audit's framing is accurate.

### [MED-004] [Security] core/crypto.py:37-38
  Description: Key derivation uses plain SHA256 hash, not a proper KDF (PBKDF2/scrypt)
  Evidence: `key_bytes = hashlib.sha256(master.encode()).digest()` — no salt, no iteration count
  Impact: If master key is leaked, Fernet key derivable in microseconds
  Recommended fix: Use PBKDF2 with random salt; acceptable for current single-user deployment but should improve before multi-user
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5a)
  Sweep evidence (crypto.py:37):
      key_bytes = hashlib.sha256(master.encode()).digest()
      return Fernet(base64.urlsafe_b64encode(key_bytes))
  Sweep note: Bare SHA256, no salt, no iteration count. Confirmed at line 37. Caveat the audit itself raised applies — single-user deployment makes the offline-crack-cost trade-off acceptable for now, but the recommendation to upgrade is correct on its merits.

### [LOW-001] [Security] api/routes_history.py:41
  Description: Ticker input uppercased but not validated (could contain special chars)
  Evidence: `"ticker": ticker.upper()` — no format check
  Impact: Minor; parameterized queries prevent injection, but good practice to validate
  Recommended fix: Validate against `[A-Z0-9]+` pattern
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5a)
  Sweep evidence (routes_history.py:41):
      "ticker": ticker.upper(), "side": side,
  Sweep note: No regex/format validation on ticker. Confirmed. Audit's own severity assessment is right — parameterized queries downstream prevent injection, so this is hygiene rather than exploitable. Low-priority.

---

## 3. Performance

### [CRIT-001] [Performance] api/routes_orders.py:159-184
  Description: N+1 query pattern in position_fills endpoint
  Evidence: For each fill in the loop, 2 individual queries fire: `SELECT * FROM pre_trade_log WHERE calc_id=?` and `SELECT order_type FROM orders WHERE exchange_order_id=?`. A position with 50 fills = 100 extra queries.
  Impact: Endpoint latency scales linearly with fill count; visible delay on positions with many fills
  Recommended fix: Batch-fetch pre_trade_log and orders by calc_id/order_id lists, then join in memory
  Effort: medium
  Status: RESOLVED in Task 88 — batch-fetch via IN-clause replaces per-fill queries
  Implementation note (Task 88):
    Added db.get_pretrade_logs_by_calc_ids() and db.get_order_types_by_ids() in
    core/db_orders.py. routes_orders.frag_position_fills now executes 3 queries
    total (fills fetch + 2 batched enrichment queries) regardless of fill count,
    down from 2N + 1 where N = entry-fill count.
    Benchmark on a 50-fill position (in-memory SQLite, 15-iter median):
      N+1 form:   10.6 ms / 100 queries
      Batch form: 0.34 ms / 2 queries  (excluding the prior fills fetch)
      Speedup:    31× / 50× fewer queries
    Real disk-backed DB will show larger absolute deltas because each per-fill
    query previously paid a roundtrip cost.
    Helpers dedupe input ID lists; SQLite default param limit is 32766
    (since 3.32, 2020) so no chunking needed at realistic fill counts.
    Phase 6 (db._conn refactor, Task 92) will systematize similar helpers
    across routes_orders.py — this task seeds the pattern.

### [HIGH-003] [Performance] core/database.py (missing indexes)
  Description: No index on `pre_trade_log.calc_id` or `orders.calc_id` despite frequent lookup by calc_id
  Evidence: `SELECT * FROM pre_trade_log WHERE calc_id=?` appears 6+ times across routes_orders.py — full table scan each time
  Impact: 10-100x slower calc_id lookups as tables grow
  Recommended fix: Add `CREATE INDEX IF NOT EXISTS idx_pretrade_calc_id ON pre_trade_log (calc_id)` and same for orders
  Effort: small
  Status: RESOLVED in Task 88.1 — two indexes added inline in core/database.py:initialize() after the ALTER block, alongside existing idx_pretrade_ts / idx_orders_* declarations.
  Implementation note:
    Scope verified against actual schema before patching. The audit's original column inventory (pre_trade_log.calc_id, orders.calc_id) was correct.
    A speculative third index (orders.exchange_order_id) was investigated and dropped — that column is already covered by SQLite's auto-index for `UNIQUE(account_id, exchange_order_id)`. Every production caller prefixes account_id, so the composite auto-index handles them. The third index would have been write-cost duplicate.
    Audit's query-site count for orders.calc_id was inflated (claimed "6+", actual ~3 equality + 2 aggregate sites) but the index is still warranted by calc_correlation.py's hot path.
    Indexes installed via inline DDL in initialize() (not as a new migration file in core/migrations/) because pre_trade_log and orders live in the legacy single-shared DB, which the per-account migration runner does not target. Pattern matches existing idx_pretrade_ts, idx_orders_* declarations.
    EXPLAIN QUERY PLAN verified pre/post:
      Before: `SCAN pre_trade_log` (single-key and IN-clause), `SCAN orders`.
      After:  `SEARCH ... USING INDEX idx_pretrade_calc_id (calc_id=?)` for both single and IN forms; `SEARCH orders USING INDEX idx_orders_calc_id (calc_id=?)`.
  Depended on: CRIT-008 — the pretrade index could not be added until the underlying column existed. Both ALTER and indexes landed in the same commit.

### [MED-005] [Performance] core/trade_event_log.py:99+, core/calc_correlation.py:75+
  Description: Sync sqlite3 calls in modules called from async handlers
  Evidence: `conn = sqlite3.connect(db_path)` used directly; trade_events endpoint wraps in `asyncio.to_thread()` (correct) but other callers may not
  Impact: Event loop blocking for 1-5ms per call
  Recommended fix: Ensure all async call paths wrap sync DB calls in `to_thread()`
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5a)
  Sweep evidence: trade_event_log.py:99 uses sync `sqlite3.connect(db_path)` inside a sync `log_trade_event` function. Callers in core/order_manager.py (an async file) invoke `log_trade_event(...)` directly at lines 343, 353, 420, 452, 474, 494, 723 — no `asyncio.to_thread` wrap.
  Sweep note: Inside async coroutines, sync DB I/O blocks the event loop. Per-call cost may be small for INSERTs into a separate trade_events DB, but the pattern is real and the audit is correct. The trade_events READ endpoint wraps in to_thread (correct path); writes from order_manager don't.
  Sweep scope-expansion note (batch 1b add): Audit's recommended fix mentions to_thread() wrapping but did not enumerate primary caller-violation sources. Grep across core/order_manager.py during sweep 1a found ≥7 sync log_trade_event call sites in order_manager.py alone. Fix task should treat the to_thread() wrapping as a wider refactor than the audit's text implies — likely a multi-file pass across order_manager.py, trade_event_log.py, calc_correlation.py.

### [MED-006] [Performance] templates (HTMX polling)
  Description: 5+ HTMX elements poll at `every 30s` concurrently (including hidden tabs); 3 elements at `every 1s`
  Evidence: All tabs fire `hx-trigger="load, every 30s"` regardless of visibility
  Impact: 8+ concurrent requests per 30s cycle; 3 RPS from 1s pollers
  Recommended fix: Use HTMX `intersect` trigger or visibility API to pause hidden-tab polling
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5a)
  Sweep evidence: grep of `every 30s|every 1s|hx-trigger` across templates/: 31 occurrences spread across 10 files. history.html and dashboard.html dominate (7 each). No visibility-aware gating in the grep results.
  Sweep note: Exact counts of "5+ at 30s, 3 at 1s" not individually verified, but the structural claim (no intersect/visibility gating, all-tabs-poll-always pattern) is accurate. Frontend Phase work — defer.

### [MED-007] [Performance] core/data_cache.py:777-789
  Description: OHLCV cache grows without hard memory bound
  Evidence: `cache.append(candle)` — soft trim at `ATR_FETCH_LIMIT + 10` but no max-age eviction or symbol cleanup
  Impact: Memory growth on long-running sessions with many symbols
  Recommended fix: Add per-symbol max-age eviction; clean up disconnected symbols
  Effort: medium
  Sweep classification: SCOPE-SHIFTED (Task 90.5a)
  Sweep evidence: lines 781-783 confirm the soft trim at ATR_FETCH_LIMIT+10. BUT lines 791-800 contain `evict_symbol_caches(self, active_tickers: set)` which DOES remove cache entries for symbols no longer in any active position (across ohlcv_cache, orderbook_cache, and mark_price_cache):
      def evict_symbol_caches(self, active_tickers: set) -> None:
          """Remove cache entries for symbols no longer in any active position."""
          for sym in list(app_state.ohlcv_cache.keys()):
              if sym not in active_tickers:
                  del app_state.ohlcv_cache[sym]
  Sweep note: Audit said "no max-age eviction OR symbol cleanup". Symbol cleanup exists (evict_symbol_caches). Only the max-age eviction half of the finding is real. Severity may also be lower (MED → LOW?) — long-running sessions with stable symbol set would have bounded per-symbol cache, just with stale data. Scope: focus a fix on max-age eviction within an active symbol's cache; symbol-set growth is already handled.

---

## 4. Architecture & Coupling

### [HIGH-002] [Architecture] 24 locations across codebase
  Description: Direct access to `db._conn` (private attribute) bypassing DatabaseManager API
  Evidence: `async with db._conn.execute(...)` in api/routes_orders.py (8x), core/monitoring.py (3x), core/platform_bridge.py (8x), core/reconciler.py (1x), plus others
  Impact: Encapsulation violation; code breaks if DatabaseManager refactors connection management
  Recommended fix: Create public helper methods (`db.get_closed_position()`, `db.batch_get_pretrade_logs()`, etc.)
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence: grep `db\._conn\.` across production code (api/, core/) shows 6 hits in api/routes_orders.py (was 8 pre-Task-88; the batch refactor removed 2), 8 in core/platform_bridge.py, 3 in core/monitoring.py, 1 in core/reconciler.py — production total ~18 sites (audit said "24 locations" likely including some other modules I didn't enumerate; magnitude matches).
  Sweep note: Encapsulation violation as described. Task 88 already seeded the pattern of moving these to public helpers (get_pretrade_logs_by_calc_ids / get_order_types_by_ids). Phase 6 work to systematize.

### [MED-008] [Architecture] core/event_bus.py vs core/pubsub/bus.py
  Description: Two parallel pub/sub systems without clear usage boundary
  Evidence: `EventBus` (asyncio.Queue-based, internal) coexists with `PubSubBus` (Protocol with InProcess/Redis backends). Some modules use one, some the other.
  Impact: Confusion for contributors; risk of events not reaching all subscribers
  Recommended fix: Document when to use each; long-term consolidate onto PubSubBus
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence: both files exist (core/event_bus.py and core/pubsub/bus.py per the file structure). The "two parallel systems" claim is structural — confirmed by file presence and the audit doc's adjacent HIGH-018 / LOW-017 entries that also reference both. Mixing of subscribers across the two buses is the documented concern.
  Sweep note: Architectural finding; not a bug per se. Fix is documentation + long-term consolidation. Phase 6 territory.

### [LOW-002] [Architecture] 40+ function-level imports across core/
  Description: Import-inside-function anti-pattern to work around circular dependencies
  Evidence: `from core.state import DEFAULT_PARAMS` inside function bodies in account_registry, data_cache, order_manager, etc.
  Impact: Hides dependencies; delays ImportError to first invocation
  Recommended fix: Restructure module boundaries to eliminate circulars; acceptable for now
  Effort: large
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence: grep `^\s+from core\.` (indented core imports — function-scoped) in core/ returns 113 occurrences across 30 files. Audit's "40+" is conservative; actual count is ~2.8× higher.
  Sweep note: Real anti-pattern, well above audit's stated count. Fix scope is genuinely large; correct as "acceptable for now" until module-boundary refactor (Phase 6).

---

## 5. Test Coverage & Quality

### [MED-009] [Testing] 41/59 core modules have no dedicated test file
  Description: 69% of core modules lack dedicated unit tests
  Evidence: Modules without test files include: exec_link.py, crypto.py, db_orders.py, db_trades.py, exchange.py, handlers.py, monitoring.py, order_enrichment.py, reconciler.py, ws_manager.py, and 31 others
  Impact: Regressions in these modules caught only by integration tests (if at all)
  Recommended fix: Prioritize tests for exec_link.py (new, untested), order_enrichment.py (critical path), and crypto.py (security)
  Effort: large (ongoing)
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence: Audit's accurate count of 41/59 — verifiable but tedious. Trust the audit's enumeration; partial spot-check confirms exec_link.py and crypto.py lack dedicated tests, though Tasks 85b/87/88/88.1/89/90 have since added coverage for risk_engine.py / order_manager.py paths and a small exec_link.py test for CRIT-004 (test_exec_link_crit_004_semantics.py).
  Sweep note: Real coverage gap. Recent fix-phase work nibbled at exec_link.py and risk_engine.py but the bulk (39+ modules) is still untested. Phase 11 owns this — Task 120 explicitly scoped to expand coverage there.

### [LOW-003] [Testing] tests/test_sr7_step4_price_extremes.py:134-140
  Description: `__new__` hack bypasses adapter `__init__` in test fixture
  Evidence: `adapter = BinanceUSDMAdapter.__new__(BinanceUSDMAdapter)` then manually sets internal attrs
  Impact: Test may pass even if `__init__` is broken; low risk since intentional and isolated
  Recommended fix: Acceptable for adapter isolation testing; document the pattern
  Effort: n/a
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence (test_sr7_step4_price_extremes.py:136):
      adapter = BinanceUSDMAdapter.__new__(BinanceUSDMAdapter)
      adapter._ex = mock_ex or MagicMock()
      adapter._markets_loaded = True
      adapter._weight_tracker = None
  Sweep note: Confirmed at line 136 with the manually-set attrs at 137-140. Audit's own assessment is "acceptable for adapter isolation testing" — pattern stands as documented.

---

## 6. Code Quality

### [LOW-004] [Quality] core/data_logger.py
  Description: `load_recent_history()` defined but never called from any non-deleted code
  Evidence: Only references are in the function definition itself (line 201) and the deleted routes (Task 70)
  Impact: Dead code; minor confusion for readers
  Recommended fix: Delete or mark as deprecated
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence: grep `load_recent_history` across repo shows only the definition (core/data_logger.py:187) and references in audit/plan docs. No production caller.
  Sweep note: Trivial deletion candidate. ~10 LOC including the function body.

### [LOW-005] [Quality] core/ohlcv_fetcher.py:233-238
  Description: Print statements in CLI utility (not logging module)
  Evidence: `print(f" [{pct:5.1f}%] {msg}")` — 3 occurrences in `__main__` block
  Impact: Cosmetic; acceptable for CLI tool context
  Recommended fix: Replace with `log.info()` for consistency
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence: grep `^\s*print\(` in core/ohlcv_fetcher.py returns 3 hits at lines 233, 236, 238 — matches audit exactly.
  Sweep note: Confirmed. Audit's own framing ("Cosmetic; acceptable for CLI tool") sets correct priority.

### [LOW-006] [Quality] core/ — log level distribution
  Description: 38% of log calls are `log.warning` (112/296) — some are informational
  Evidence: e.g., `log.warning("Unmapped Binance order status: %s", raw_status)` — handled gracefully, should be debug
  Impact: Warning fatigue; genuine warnings harder to spot
  Recommended fix: Downgrade ~20-30 warnings to info/debug
  Effort: medium
  Sweep classification: NEEDS DEEP READ (Task 90.5b)
  Sweep note: The audit's 112/296 ratio claim is specific and verifiable but would require counting all log.warning vs all log.* calls AND case-by-case judgment on which warnings should downgrade. Per-call judgment is the deep-read portion. Defer to its fix task for the actual triage.

### [LOW-007] [Quality] 0 TODO/FIXME/HACK/XXX markers in core/ and api/
  Description: Clean codebase — no inline technical debt markers
  Evidence: `grep -rn "TODO\|FIXME\|HACK\|XXX" core/ api/` returns 0 results
  Impact: Positive finding
  Recommended fix: n/a
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence: grep `TODO|FIXME|HACK|XXX` in core/ returns 0 matches. Audit's positive finding holds.
  Sweep note: No action needed.

### [LOW-008] [Quality] Inconsistent pagination patterns
  Description: 3 separate paginated query implementations (db_orders, db_trades, db_analytics)
  Evidence: Each implements LIMIT/OFFSET with slightly different parameter handling
  Impact: Minor duplication; not worth a helper until a 4th consumer appears
  Recommended fix: Defer; create shared helper if pattern spreads further
  Effort: small (when needed)
  Sweep classification: NEEDS DEEP READ (Task 90.5b)
  Sweep note: Audit's own recommendation is "defer — not worth a helper yet". To classify VERIFIED REAL needs reading all three files' pagination handlers. Audit's framing is plausible (3 separate impls is common in codebases this size) but I'd want to read them to confirm the divergence is real before tagging. Defer to fix-task.

---

## 7. Documentation & Operational

### [LOW-009] [Docs] README.md — updated in Task 83
  Description: README reflects current v2.4 state (test count, architecture, status)
  Evidence: Verified accurate as of commit a100a28
  Impact: Positive finding
  Recommended fix: n/a
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep note: Audit's own positive finding; no action needed. README.md test count of 1247 is now stale (current: 1291) — minor drift not worth a fix in this sweep.

### [LOW-010] [Docs] All core modules have module-level docstrings
  Description: 59/59 checked modules have `"""..."""` header documentation
  Evidence: Verified via grep
  Impact: Positive finding
  Recommended fix: n/a
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep note: Positive finding; trusting audit's enumeration (n=59 cross-checks with MED-009's 41/59-untested count consistent).

### [LOW-011] [Docs] Migration 008 has misleading comment
  Description: `008_v2_4_closed_positions_calc_id.sql` says "Placeholder — closed_positions is in legacy DB, ALTER via database.py" but the migration still runs on per-account DBs (no-op SELECT 1)
  Evidence: File contains only `SELECT 1;` — harmless but confusing
  Impact: Minor confusion for migration readers
  Recommended fix: Delete the placeholder file or add clarifying comment
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence: core/migrations/008_v2_4_closed_positions_calc_id.sql is 6 lines: scope `per_account`, description "Placeholder — closed_positions is in legacy DB, ALTER via database.py", body `SELECT 1;`. Migration runner records it as applied; no-op against per-account DBs (which don't have closed_positions).
  Sweep note: Confirmed verbatim. Related to the migration-005-vs-legacy-DB documentation gap noted as Phase 12 cleanup in Tasks 88.1 and 89 reports. Same family of doc-cleanup work.

### [LOW-012] [Docs] exec_link.py EXEC_LINK_PRICE_TOL not in .env.example
  Description: New env var `EXEC_LINK_PRICE_TOL` added in Task 82 but `.env.example` not updated
  Evidence: `config.py` references `os.getenv("EXEC_LINK_PRICE_TOL", "0.0005")` — not in .env.example
  Impact: New deployments may not discover the config knob
  Recommended fix: Add to .env.example with comment
  Effort: small
  Sweep classification: SCOPE-SHIFTED (Task 90.5b)
  Sweep evidence: glob `**/.env*` returns only `.env` — there is no `.env.example` file in the repo at all.
  Sweep note: Audit assumed `.env.example` exists; it doesn't. The real concern ("new deployments don't discover the config knob") is valid but the fix surface is bigger — either create a `.env.example` from scratch (and audit which env vars to include), or document EXEC_LINK_PRICE_TOL in README/CLAUDE.md instead. Re-spec needed before tasking.

---

## Second Pass (2026-05-17, same session)

Additional findings from deeper code review of adapters, WebSocket, state
management, schedulers, and template/JS interactions.

---

## 8. Exec Link Logic Bug

### [CRIT-004] [Correctness] core/exec_link.py:55-56
  Description: TP and SL match criteria compare pretrade values against themselves — always True
  Evidence: `tp_match = price_near(pretrade.get("tp_price"), pretrade.get("tp_price"))` — both args are the same dict key. Same for sl_match on line 56.
  Impact: Exec link badge shows 3/3 LINKED for all limit orders regardless of actual TP/SL accuracy. Partial (2/3) can never appear for TP/SL mismatches — only entry price slippage triggers partial.
  Recommended fix: Compare pretrade TP/SL against actual TP/SL orders on the exchange. Short-term: document that TP/SL criteria are plan-vs-plan (always pass) and the real value is entry price comparison. Long-term: query orders table for actual TP/SL placed.
  Effort: medium (to fix properly); small (to document limitation)
  Status: RESOLVED in Task 87 (short-term 1/1 semantics) — full plan-vs-actual deferred to Phase 9 (TP/SL modification tracking)
  Implementation note (Task 87):
    The typo on lines 55-56 (price_near self-comparing pretrade values) was
    NOT fixed in the literal sense. A real plan-vs-actual comparison requires
    threading actual placed TP/SL order data through compute_exec_match's
    callers, which is Phase 9 scope. Short-term fix: dropped TP/SL contributions
    from match_count and auto_link, shifting semantics from "x/3" to "x/1".
    tp_match/sl_match remain on ExecMatchResult as informational stubs (still
    self-comparing) with explicit comments documenting the limitation. Badge
    now reads honestly: 1/1 LINKED when entry matches, 0/1 UNLINKED otherwise.
    Test `test_tp_sl_remain_informational_stubs_post_CRIT_004` pins the stub
    to prevent accidental half-fix without Phase 9 caller threading.
    Templates updated: templates/fragments/history/exec_link_panel.html (badge
    /3 → /1, TP/SL rows marked "(plan-vs-plan stub)" with footer note);
    templates/fragments/history/position_fills.html (badge /3 → /1).

---

## 9. WebSocket & Adapter Risks

### [CRIT-005] [Correctness] core/ws_manager.py:496
  Description: WS reconnect recurses without stack depth limit on persistent auth failure
  Evidence: `_reconnect_user(attempt + 1)` called recursively. Max-attempt guard at line 477 protects the outer path, but `create_listen_key()` failure at line 496 recurses before hitting that guard.
  Impact: Stack overflow crash if Binance persistently returns 401/403 on listen key refresh
  Recommended fix: Add `if attempt >= max_attempts: return` before the recursive call at line 496
  Effort: small
  Status: VERIFIED FALSE in Task 89 — line-477 max-attempt guard runs at top of every recursive entry; recursion bounded at config.WS_RECONNECT_ATTEMPTS=15, well below Python's default 1000-frame limit.
  Verification note (Task 89):
    The audit's "stack overflow" framing was incorrect. Read of core/ws_manager.py:470-504
    shows `if attempt >= config.WS_RECONNECT_ATTEMPTS: return` at line 477 fires for every
    recursive call entry. With WS_RECONNECT_ATTEMPTS=15 (config.py:63), max stack depth
    from this recursion is 15 frames. The audit's claim that the recursion fires "without
    first checking the max-attempt guard" was wrong — the guard is the second statement
    in the function body (after the _stopping check).
    Regression pin test_reconnect_user_bounded_by_max_attempts_no_recursion_error
    asserts the bound holds; failure-before-fix confirmed by removing the guard, which
    caused unbounded recursion (~947 attempts before Python's recursion-limit RecursionError).
    Related concern recorded as MED-044 (recursion-vs-iteration as a defense-in-depth
    smell if WS_RECONNECT_ATTEMPTS is ever bumped) — separate finding, not a CRIT.

### [HIGH-006] [Correctness] core/ws_manager.py:159-195
  Description: Position list iterated while concurrent WS handlers modify it in-place
  Evidence: TP/SL enrichment loops over `app_state.positions` while `DataCache.apply_position_update_incremental()` can add/remove positions concurrently
  Impact: Positions skipped or duplicated during enrichment; risk state miscalculated
  Recommended fix: Copy position list before iteration: `for p in list(app_state.positions):`
  Effort: small
  Sweep classification: NEEDS DEEP READ (Task 90.5b) — resolved in Task 91 (see below)
  Sweep evidence: ws_manager.py:159 confirms `for pos in app_state.positions`. The loop body (lines 159-195) has no `await` statements — pure attribute assignment — so within a single coroutine no other coroutine can interleave. Reachability depends on whether `apply_position_update_incremental` ever runs in a *different OS thread* (e.g., via asyncio.to_thread or a thread-pool callback) concurrently with this loop.
  Sweep note: The recommended fix (`list(app_state.positions)`) is a trivially safe defensive copy regardless of whether the bug is triggerable today, so the fix is cheap. But the "is this actually reachable" question requires tracing apply_position_update_incremental's caller chain. Defer to fix-task for that verification; apply the copy regardless.
  Status: VERIFIED FALSE in Task 91 — loop body is await-free; concurrent modification cannot interleave
  Verification note (Task 91):
    Deep-read traced the loop body (core/ws_manager.py:159-195) and confirmed
    zero `await` statements: pure attribute assignments, conditionals, and a
    `break` after the first match. Within asyncio's cooperative single-threaded
    event loop, no other coroutine can run during this loop.
    
    Modifier audit: grep `self\._positions\.|self\._positions =` in
    core/data_cache.py finds 4 sites — all inside async methods on the same
    event loop, three of them additionally gated by `async with self._lock`
    (data_cache.py:266 inside apply_position_snapshot, :366 + :386 inside
    apply_position_update_incremental, :813 init/reset). No `to_thread` or
    `run_in_executor` path mutates `_positions` from a separate OS thread.
    
    Conclusion: the audit's concurrent-modification failure mode is
    structurally unreachable. Combined with the `break` on first match
    (line 195), even if a mutation could interleave, this loop processes at
    most one position before exiting — the audit's "skip/duplicate" framing
    doesn't apply.
    
    Action taken: No production change. Regression pin
    test_apply_order_update_tpsl_loop_body_has_no_awaits in
    tests/test_task91_high006_position_iter_pin.py uses ast parsing to
    assert the loop body remains await-free. If a future contributor adds
    an `await` to the loop body, the pin fails with an explicit message
    pointing back to HIGH-006 and the audit's snapshot fix recommendation.
    Non-vacuous proof: temporarily inserted `await asyncio.sleep(0)` into
    the loop body, pin failed with "loop gained 1 await statement(s)";
    restored.

### [HIGH-007] [Reliability] core/adapters/base.py:109-110
  Description: Weight tracker exceptions caught and swallowed silently
  Evidence: `except Exception: pass` around weight tracking — requests proceed without cost estimation
  Impact: 429 rate-limit bans when budget was believed sufficient; no observability
  Recommended fix: Log at WARNING level on weight tracker failure
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence (adapters/base.py:107-110):
      except RateLimitError:
          raise
      except Exception:
          pass  # tracker failure must not block requests
  Sweep note: Confirmed verbatim. Comment "tracker failure must not block requests" justifies pass-not-raise; audit's fix is just to add a log.warning before pass. Trivial.
  Status: RESOLVED in Task 99 — observability fix matches the audit description directly. log.exception added at both swallow sites in core/adapters/base.py (reserve()'s except at lines 117-128 and _get_weight_tracker()'s lazy-init except at lines 66-75); preserves original "tracker failure must not block requests" control flow. AST regression pins in tests/test_task99_adapter_bundle.py::TestWeightTrackerSwallowLogs cover both branches.
  Framing note (Task 99.1): The Task 98 Top-5 slot-5 rationale that promoted HIGH-007 described it as "race-framed in the audit text" — that paraphrase was inaccurate. Re-reading the audit description above ("exceptions caught and swallowed silently" / "no observability") and the recommended fix ("Log at WARNING level"), the audit was never race-framed for this finding. The fix shape (log + preserve control flow) matches the audit recommendation exactly. No race-claim verification was needed; this finding does NOT count toward the race-framing false-positive calibration.

### [HIGH-008] [Reliability] core/schedulers.py:134-141
  Description: AuthenticationError not caught in background refresh tasks
  Evidence: `_account_refresh_loop` catches `RateLimitError` but not `AuthenticationError`
  Impact: Invalid API keys crash background tasks silently; positions not refreshed
  Recommended fix: Catch `AuthenticationError`; log CRITICAL and disable periodic refresh for affected account
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence: grep `RateLimitError|AuthenticationError|except.*Error` in core/schedulers.py shows `from core.adapters.errors import RateLimitError` (line 17) and `except RateLimitError as e:` (lines 219, 516). No AuthenticationError anywhere. Background tasks at lines 125-140 fetch_account / fetch_positions without auth-specific catching.
  Sweep note: Confirmed. If invalid API key → auth error propagates → task crashes → MED-012's "log but don't restart" leaves the account stale forever. Pairs naturally with MED-012 (resurrection) as a bundled fix task.
  Status: RESOLVED in Task 103 — narrow AuthenticationError catch + per-account auth_failed_accounts state + credential-update clears the flag.
  Audit-mechanism correction: the sweep note above describes the pre-fix path as "auth error propagates → task crashes." Tracing the actual code, the broad `except Exception` at line 222 ALREADY caught the AuthenticationError — the loop did not crash; it just logged a WARNING and retried on the next 15-30 s tick. The real harm pattern (closer to the audit's intent) is: every tick hammers the exchange with invalid credentials, which (a) generates log noise, (b) risks exchange-side IP-level bans for repeated auth failures, (c) gives the operator no visible signal that "credentials are wrong — fix them." Pattern matches the Task 102 calibration note: bug real, mechanism the audit described was off. Fix addresses the real harm (terminal-until-fix + visible-to-operator) rather than the described one (crashing loop).
  Implementation note:
    - core/state.py:285-291 — new `auth_failed_accounts: Set[int]` field on AppState. Mirrors the `dd_manually_unblocked` set pattern. NOT reset on account-switch (auth_failed is account-specific, not session-specific).
    - core/schedulers.py — imports `AuthenticationError` alongside `RateLimitError` (line 17); skip-guard at top of loop body checks `app_state.active_account_id in app_state.auth_failed_accounts` and `continue`s if true; new `except AuthenticationError as e:` clause between the RateLimitError and the broad Exception handlers logs.critical with account context, adds account_id to the set, and appends a WS log entry "AUTH FAILED for account {aid}: {e} — update credentials in Config." surfacing to the operator on next page load.
    - core/account_registry.py:update_account — when api_key OR api_secret is updated (non-None), the account_id is discarded from app_state.auth_failed_accounts so the scheduler resumes refresh on the next tick. Inside try/except so import failures in test paths don't propagate.
  Design choices:
    - Exception class: core.adapters.errors.AuthenticationError — adapter-neutral type already used at core/adapters/base.py:_run() to translate ccxt.AuthenticationError. Clean. No exchange-specific catching needed.
    - UI surface: minimal — app_state.ws_status.add_log() entry. Visible on the existing WS log fragment without new UI work. A dedicated badge / banner is deferred (out of scope per task spec).
    - Recovery: auth flag clears only on credential update via account_registry.update_account. No automatic retry. Matches audit intent ("disable periodic refresh until manual reconnect").
  Light arch check: scheduler is read-side. `_account_refresh_loop` calls `fetch_account`, `fetch_positions`, and `fetch_user_trades` — all read endpoints. Does not place / modify / cancel orders. Active-side concern does not apply.
  Tests (tests/test_task103_scheduler_auth.py, 10 cases): TestAuthFailedAccountsField (2) — field exists, fresh init empty; TestSchedulerCatchesAuthError (3) — AST pin verifies `except AuthenticationError` precedes `except Exception` in the loop's try chain, skip-guard references auth_failed_accounts, module imports AuthenticationError; TestCredentialUpdateClearsAuthFailedFlag (3) — api_key update clears, api_secret update clears, name-only update does NOT clear (anti-over-correction); TestAuthExceptionHandlerBehavior (2) — AuthenticationError sets flag + CRITICAL log + WS-log entry; ConnectionError (transient) does NOT set the flag.

---

## 10. State & Concurrency

### [MED-010] [Concurrency] core/order_manager.py (~process_fill)
  Description: No mutex prevents concurrent processing of same exchange_fill_id
  Evidence: Duplicate fills from WS + REST fallback can both enter `process_fill` simultaneously
  Impact: Duplicate closed_position rows (mitigated by UNIQUE constraint) but snapshot data may double-count
  Recommended fix: Add asyncio.Lock keyed by (account_id, exchange_fill_id) or rely on UNIQUE + upsert
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence: grep `async def process_fill|asyncio\.Lock|_fill_lock` in core/order_manager.py returns only `async def process_fill(` at line 565. No locking primitive in the file.
  Sweep note: The audit's note about UNIQUE constraint preventing duplicate closed_position rows is accurate — checked in Task 88.1 context (orders table UNIQUE(account_id, exchange_order_id)). Snapshot double-counting concern (in-memory state updates twice) is the real residual risk. Bounded blast radius; medium severity correct.

### [MED-011] [Correctness] core/db_orders.py:562
  Description: Fill ordering by timestamp_ms without tiebreaker — fills at same millisecond appear in arbitrary order
  Evidence: `ORDER BY timestamp_ms ASC` — no secondary sort
  Impact: Entry vs close fill order could be inverted for ultra-fast trades (same ms)
  Recommended fix: Add `ORDER BY timestamp_ms ASC, ROWID ASC` for deterministic ordering
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence (db_orders.py:562): `sql += " ORDER BY timestamp_ms ASC"` — confirmed verbatim. No secondary sort.
  Sweep note: get_position_fills exact line. Same `ORDER BY timestamp_ms ASC` pattern likely exists elsewhere in the file (e.g., get_fills_by_order at line 573, get_unrecorded_closing_fills at line 599) — fix task should sweep all fill-ordering sites in db_orders.py, not just this one line.

### [MED-012] [Reliability] core/schedulers.py:54-56
  Description: Background task crash logs exception but does not restart task
  Evidence: `_on_done` callback logs but doesn't signal lifespan or attempt restart
  Impact: Crashed scheduler (regime_fetcher, etc.) runs once and never recovers
  Recommended fix: Signal event_bus on crash; implement task resurrection for critical loops
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence (schedulers.py:54-58):
      def _on_done(t: asyncio.Task) -> None:
          if not t.cancelled() and t.exception() is not None:
              log.error("Background task '%s' crashed: %r", name, t.exception())
  Sweep note: Confirmed verbatim. No restart, no event_bus signal. Pairs naturally with HIGH-008 (auth-error not caught) since the joint failure mode is: auth-error crashes scheduler → schedulers.py:_on_done logs but doesn't restart → account stays unrefreshed indefinitely.

---

## 11. Template & JS Issues (History Page)

### [MED-013] [UI] templates/fragments/history/trade_events_table.html:34
  Description: Event type dropdown filter loses search state when changed
  Evidence: Select's `hx-get` URL manually lists params (`search={{ search }}`) using server-rendered value, not current input value. If user types new search then changes event_type, old search is sent.
  Impact: Search clears when changing event type filter
  Recommended fix: Add `hx-include="closest div"` to the select element
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence (trade_events_table.html:34): `hx-get="{{ ep }}?page=1&per_page={{ per_page }}&search={{ search }}&date_from={{ df }}&date_to={{ dt }}"` — server-rendered search value, no `hx-include` on the select. Adjacent input at line 30 uses `hx-include="closest div"`.
  Sweep note: Confirmed. Trivial template fix.

### [MED-014] [UI] templates/fragments/history/trade_events_table.html:99-121
  Description: Trade Events uses custom prev/next pagination instead of standard `{{ pagination() }}` macro
  Evidence: Only `«` and `»` buttons rendered; no numbered page buttons
  Impact: Users cannot jump to specific pages; must click through sequentially
  Recommended fix: Replace with `{{ pagination(ep, tid, page, per_page, total, total_pages, qs) }}`
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence (trade_events_table.html:99-119): the custom block renders `«` and `»` buttons (lines 111-118) with no numbered page buttons. The standard `{{ pagination(...) }}` macro is not used here.
  Sweep note: Confirmed. Trivial template swap.

### [MED-015] [UI] templates/history.html (histMainTab)
  Description: Tab switch immediate fetch can race with the 30s auto-poll
  Evidence: `histMainTab()` fires `htmx.ajax()` immediately; the panel's `hx-trigger="every 30s"` continues independently. If user switches at second 29, two requests fire within 1 second.
  Impact: Minor: duplicate request, possible flicker if responses arrive out of order
  Recommended fix: Acceptable for v2.4; if problematic, cancel pending poll on tab switch
  Effort: medium
  Sweep classification: NEEDS DEEP READ (Task 90.5b)
  Sweep note: Audit's own recommendation is "Acceptable for v2.4". To classify VERIFIED REAL would require reading histMainTab() JS, the panel's hx-trigger config, and tracing the actual response-arrival ordering. Audit's framing is plausible but verifying the precise race window needs runtime reasoning. Defer to fix-task; severity is already acknowledged as minor.

### [LOW-013] [UI] Open positions subtab flicker on 1s refresh
  Description: `hx-trigger="every 1s"` replaces entire open_positions fragment including subtab buttons; subtab state reapplied via htmx:afterSettle but may cause brief visual flicker
  Impact: User on Open Orders subtab may see momentary snap to Positions subtab
  Recommended fix: Extend interval to 5s or use hx-preserve on subtab buttons
  Effort: small
  Sweep classification: NEEDS DEEP READ (Task 90.5b)
  Sweep note: Audit describes an observable runtime UI behavior. Static template inspection can't reproduce flicker; needs a browser session. Defer to fix-task (or to a frontend Phase pass that batches UI hygiene).

### [LOW-014] [UI] Exec link panel caching prevents refresh after confirm
  Description: `toggleExecLink()` sets `data-loaded="1"` after first fetch; re-opening never re-fetches
  Evidence: After confirming a link, closing and re-opening the panel shows cached (pre-confirm) content until page reload
  Recommended fix: Clear `data-loaded` after confirm POST completes, or re-fetch on every open
  Effort: small
  Sweep classification: NEEDS DEEP READ (Task 90.5b)
  Sweep note: Audit describes observable JS behavior; verification requires reading toggleExecLink() implementation and the confirm POST handler. Plausible per static read of exec_link_panel.html but the confirm-POST cache-clear flow needs trace. Defer.

### [LOW-015] [Correctness] core/migrations/runner.py:115
  Description: `executescript()` auto-commits DDL; partial migration cannot rollback
  Evidence: If migration fails mid-way (e.g., after CREATE TABLE but before CREATE INDEX), schema is half-applied
  Impact: Recovery requires manual intervention; mitigated by IF NOT EXISTS patterns
  Recommended fix: Document recovery procedure; existing IF NOT EXISTS guards provide idempotency
  Effort: small (doc only)
  Sweep classification: VERIFIED REAL (Task 90.5b)
  Sweep evidence (runner.py:112-115):
      # executescript() handles multi-statement SQL.  DDL is auto-committed
      # by SQLite so true rollback of CREATE TABLE is not possible —
      # all migrations MUST be written with IF NOT EXISTS / OR IGNORE.
      conn.executescript(migration.sql)
  Sweep note: The runner's own comment block (112-114) acknowledges the limitation and the IF-NOT-EXISTS workaround. Audit's "doc only" recommended fix is correct — already documented in code; needs to be documented in runbook for operator recovery.

---

## Third Pass (2026-05-17, same session)

Deeper code review of order lifecycle edge cases, data cache math, risk engine
sizing, crypto module, shutdown behavior, and DB layer defense-in-depth.

---

## 12. Order Lifecycle Edge Cases

### [HIGH-009] [Correctness] core/order_manager.py:654-666
  Description: No validation that total_close_qty <= total_open_qty (overfill)
  Evidence: `total_close_qty = sum(f["quantity"] for f in close_fills)` with no cap. Proportional entry fee `entry_fees * (total_close_qty / total_open_qty)` exceeds entry_fees if overfilled.
  Impact: Fee calculation inflated, net_pnl understated for overfilled positions
  Recommended fix: `total_close_qty = min(total_close_qty, total_open_qty)` before fee calc
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence: lines 654 and 666 unchanged from the audit:
      total_close_qty = sum(f["quantity"] for f in close_fills)
      ...
      prop_entry  = (
          entry_fees * (total_close_qty / total_open_qty)
          if total_open_qty else 0.0
      )
  Sweep note: Confirmed. If total_close_qty > total_open_qty (overfill scenario), the ratio exceeds 1.0 and proportional entry fee inflates. Trivial cap fix per audit recommendation.
  Status: RESOLVED in Task 92 — overfill cap added before fee-proportional calc.
  Implementation note (Task 92):
    Light verification confirmed the audit's claim. `total_close_qty` is used
    downstream at two sites with semantic meaning that depends on the
    uncapped value:
      - core/order_manager.py:656 — denominator for exit_price (VWAP of close fills)
      - core/order_manager.py:699 — persisted `"quantity"` field on closed_positions
        (audit record must reflect actual close size, not capped value)
    The audit's literal recommendation (`total_close_qty = min(...)`) would
    corrupt both. Chose the separate-variable form instead:
      close_qty_for_fees = total_close_qty
      if total_open_qty and total_close_qty > total_open_qty:
          log.warning(...overfill...)
          close_qty_for_fees = total_open_qty
      prop_entry = entry_fees * (close_qty_for_fees / total_open_qty) ...
    Cap fires only on overfill (close > open). Normal close and partial close
    paths produce identical output to pre-fix.
    Tests in tests/test_order_manager.py::TestClosePositionOverfillCap:
      - test_overfill_caps_prop_entry_at_100pct (failure-before-fix verified:
        pre-fix total_fees=7.5 vs post-fix 5.0 with close_qty=1.5, open_qty=1.0,
        entry_fees=5.0)
      - test_underfill_no_cap_no_warning (anti-over-correction)
      - test_exact_fill_no_cap_no_warning (boundary)

### [HIGH-010] [Security] core/crypto.py:54-58
  Description: Decryption silently returns "" on failure (wrong master key, corruption)
  Evidence: `except (InvalidToken, Exception): return ""` — callers get empty credentials. WS auth sends empty API key, silently fails to connect.
  Impact: Engine runs with no exchange connectivity after key rotation; no user-visible error
  Recommended fix: Raise on decryption failure or log CRITICAL + set app_state flag to disable trading
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (crypto.py:48-56):
      def decrypt(ciphertext: str) -> str:
          if not ciphertext: return ""
          try:
              return _fernet().decrypt(ciphertext.encode()).decode()
          except (InvalidToken, Exception) as e:
              log.error("Decryption failed (key mismatch or corruption): %s", type(e).__name__)
              return ""
  Sweep note: The `return ""` silent fallback is present. A `log.error` was added (line 55) that partially mitigates the "no user-visible error" half of the audit's claim — but callers still receive "" and proceed with empty credentials. Recommended fix (raise OR set app_state.disable_trading flag) is still warranted. Duplicate finding: HIGH-016 cites the same lines verbatim. Audit should consolidate.

### [HIGH-011] [Correctness] core/db_trades.py:57-59
  Description: Filter column names in `_paginated_query()` are f-string interpolated without whitelist
  Evidence: `clauses.append(f"{col} = ?")` where `col` comes from `filters` dict keys. Currently callers pass hardcoded keys ("ticker", "direction") but no enforcement.
  Impact: SQL injection if future code passes user-controlled filter keys. Defense-in-depth gap.
  Recommended fix: Add `_ALLOWED_FILTER_COLS` set and validate `col in allowed` before interpolation
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (db_trades.py:57-60):
      for col, val in filters.items():
          if val:
              clauses.append(f"{col} = ?")
              params.append(val)
  Sweep note: Confirmed verbatim. No whitelist on `col`. Pairs with MED-003 (sort whitelist at route level) and MED-022 (LIKE prefix not parameterized) as the "defense-in-depth SQL-injection-hygiene" bundle.
  Status: RESOLVED in Task 101 — class-level _ALLOWED_FILTER_COLS frozenset on TradesMixin + per-iteration validation in _paginated_query.
  Implementation note: Added `_ALLOWED_FILTER_COLS = frozenset({"ticker", "side", "direction"})` at core/db_trades.py:27-34 (covers all current callers — query_pre_trade_log uses ticker+side, query_execution_log uses ticker+side, query_trade_history uses ticker+direction). The filter loop at db_trades.py:67-77 now raises ValueError when a filter key is not in the whitelist, before any f-string interpolation. Existing callers pass only hardcoded keys, so the raise never fires in practice — it's a fail-fast guard against a future caller that might forward user-controlled filter dict keys.
  Tests (tests/test_task101_sql_injection_defense.py::TestPaginatedQueryFilterWhitelist, 4 cases): _ALLOWED_FILTER_COLS constant existence + required-keys check; concrete injection payload ("ticker; DROP TABLE pre_trade_log--") rejected; known-safe filter ("ticker") passes; empty value short-circuits without spurious raise.

---

## 13. Risk Engine & Equity

### [MED-016] [Correctness] core/risk_engine.py:285
  Description: Position size can be negative when est_slippage >= 1.0 (100%+ slippage)
  Evidence: `est_size = base_size * (1.0 - est_slippage)` — if est_slippage = 1.2, est_size = base_size * -0.2
  Impact: Negative size passed to exchange API — rejected or interpreted as opposite-side order
  Recommended fix: `est_size = max(0.0, base_size * (1.0 - est_slippage))`
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (risk_engine.py:302):
      est_size = base_size * (1.0 - est_slippage)
  Sweep note: Line drift — audit cited :285, actual site is :302. Formula identical. Confirmed reachable: est_slippage >= 1.0 produces negative est_size, no clamp. (Note: est_slippage from estimate_vwap_fill is computed as `|est_fill_price - average| / average` — ≥1.0 means fill deviated more than 100% from entry, which would indicate catastrophic book conditions. Rare but the audit's defensive fix is still warranted.)

### [MED-017] [Correctness] core/data_cache.py:747
  Description: Mark price cache stores price without timestamp; stale prices used indefinitely
  Evidence: `app_state.mark_price_cache[symbol] = mark` — no age tracking. If WS disconnects, equity computed from minutes-old prices.
  Impact: Stale equity → incorrect risk sizing after WS outage
  Recommended fix: Store `(mark, timestamp_ms)` tuple; skip update if age > threshold
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (data_cache.py:744): `app_state.mark_price_cache[symbol] = mark` — bare scalar, no timestamp.
  Sweep note: Line drift — audit cited :747, actual :744. Claim accurate. Pairs with HIGH-021 (cache freshness) for a freshness-hardening fix-task in Phase 3.

### [MED-018] [Correctness] core/data_cache.py:778
  Description: Negative equity not clamped; can cause downstream issues
  Evidence: `acc.total_equity = acc.balance_usdt + acc.total_unrealized` — if unrealized losses exceed balance, equity is negative. Downstream exposure calc divides by equity.
  Impact: Undefined risk metrics when equity is negative (near-liquidation scenarios)
  Recommended fix: Clamp to `max(0.0, ...)` or trigger liquidation warning
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (data_cache.py:767-768):
      if acc.balance_usdt > 0:
          acc.total_equity = acc.balance_usdt + acc.total_unrealized
  Sweep note: Line drift — audit cited :778, actual :768. Computation only runs when balance > 0 (so equity ≥ unrealized but unrealized can be negative). Audit's claim of negative-equity reachability is real. The `if acc.balance_usdt > 0` guard prevents the path on zero-balance but doesn't prevent negative total_equity.
  Status: RESOLVED in Task 102 — clamp negative computed equity (and derived available_margin) to 0 at the apply_mark_price boundary.
  Implementation note: In core/data_cache.py apply_mark_price (current line ~767), raw_equity = balance + unrealized is computed once, checked, and clamped: if raw_equity < 0, log.warning + acc.total_equity = 0.0; else apply raw. available_margin is then computed as max(0.0, total_equity - total_margin_used) — the prior `equity - margin_used` would have produced a negative value whenever margin_used > 0 and equity was clamped. Both clamps are at the same boundary so internal-state invariants stay coherent.
  Downstream-consumer analysis: pre-fix, negative equity produced (a) `pf.drawdown = (max_eq - total_equity) / max_eq` reading > 100 % drawdown display; (b) `pf.total_exposure` skipped via the `if total_equity > 0` guard, leaving stale exposure data; (c) risk_engine.py:351's `total_equity if > 0 else 1.0` fallback gave 1.0 USDT phantom equity for sizing. The audit's "undefined risk metrics" is real but mostly cosmetic (display + sizing fallback) — no division-by-zero crash. Clamping makes the state coherent and explicit (near-liquidation surfaces as equity=0 + warning rather than as silent oddities downstream).
  Tests (tests/test_task102_trust_but_verify.py::TestApplyMarkPriceClampsNegativeEquity, 5 cases): equity clamp branch + warning fires; equity clamp via end-to-end mark_price drop on a real position; available_margin also clamps to 0; positive equity passes through; raw_equity == 0 exactly does NOT trigger warning (boundary condition).

### [MED-019] [Correctness] core/risk_engine.py:429,487
  Description: max_position_count is advisory only — setting eligible=False doesn't prevent order submission
  Evidence: `at_max_positions` flag sets eligible=False but no barrier at the signal/order layer blocks new orders
  Impact: Position limit can be exceeded by rapid signals that bypass eligibility check
  Recommended fix: Add enforcement at order submission layer (check before sending to exchange)
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence: risk_engine.py:446 `at_max_positions = len(app_state.positions) >= prm["max_position_count"]`. Line 504: `"eligible": sizing["eligible"] and not at_max_positions`. The flag is computed and combined into `eligible`, but the audit's claim is that the order-submission layer doesn't honor it.
  Sweep note: The sweep can confirm the flag exists in the risk-engine result; cross-layer enforcement check (whether order_manager / submit paths gate on `eligible`) is a deeper trace the fix-task should perform. Audit's framing as "advisory only" is consistent with the structure.

---

## 14. Shutdown & Lifecycle

### [MED-020] [Reliability] main.py:111-113
  Description: Background tasks not cancelled on shutdown; WS cleanup not called
  Evidence: Shutdown only calls `event_bus.close()` (no-op) and `db.close()`. 15+ scheduler tasks and WS connections left to die from event loop cancellation.
  Impact: Orphaned tasks may complete after DB close; unclean WS disconnect
  Recommended fix: Call `ws_manager.stop()`, cancel `_bg_tasks` in shutdown
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (main.py:111-113):
      log.info(f"Shutting down {config.PROJECT_NAME}...")
      await event_bus.close()
      await db.close()
  Sweep note: Confirmed verbatim. No ws_manager.stop(), no _bg_tasks cancel. Pairs with LOW-017 (event_bus.close() is no-op) for a single "graceful shutdown" fix-task.

### [MED-021] [Reliability] config.py:97,101
  Description: Config values EXCHANGE_REFRESH_HZ and EXEC_LINK_PRICE_TOL parsed without validation
  Evidence: `float(os.getenv(...))` — no range check. Values of 0, negative, or NaN cause division-by-zero, logic inversion, or crash.
  Impact: Misconfigured env causes runtime crash or silent logic errors
  Recommended fix: Add `assert val > 0` or `max(0.001, val)` after parsing
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (config.py:97 and :101):
      EXEC_LINK_PRICE_TOL = float(os.getenv("EXEC_LINK_PRICE_TOL", "0.0005"))
      EXCHANGE_REFRESH_HZ = float(os.getenv("EXCHANGE_REFRESH_HZ", "1.0"))
  Sweep note: Confirmed verbatim. Bare float() parse, no clamps. Trivial defensive fix.

---

## 15. DB Layer Defense-in-Depth

### [MED-022] [Security] core/db_orders.py:mark_stale_orders_canceled
  Description: LIKE clause prefix built via f-string, not parameterized
  Evidence: `scope_clause = f" AND exchange_order_id NOT LIKE '{exclude_prefix}%'"` — single quotes not escaped
  Impact: SQL injection if prefix params are ever user-controlled. Currently called with hardcoded prefixes only.
  Recommended fix: Use `?` placeholder: `scope_clause = " AND exchange_order_id NOT LIKE ?"` with `params.append(f"{prefix}%")`
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (db_orders.py:362,364):
      scope_clause = f" AND exchange_order_id NOT LIKE '{exclude_prefix}%'"
      ...
      scope_clause = f" AND exchange_order_id LIKE '{only_prefix}%'"
  Sweep note: Confirmed at both lines. Both f-string interpolated with single-quote literal wrapping. Bundles with MED-003 and HIGH-011 as the SQL-injection-hygiene defense-in-depth task.
  Status: RESOLVED in Task 101 — both LIKE-clause branches use `?` placeholder; prefix value passed in params list.
  Implementation note: scope_clause now sets " AND exchange_order_id NOT LIKE ?" (or " LIKE ?") and appends `f"{prefix}%"` to a `scope_params` list, which is concatenated into both UPDATE-execute params tuples. SQL string no longer contains the raw prefix value. LIKE wildcard escaping decision: % and _ in the prefix are LEFT UNESCAPED (wildcards remain LIKE-feature, not literal-match). Today's callers pass only "algo:" (no wildcards); any future caller wanting literal-match semantics for user-supplied prefixes must escape themselves — documented in the function's body comment.
  Tests (tests/test_task101_sql_injection_defense.py::TestStaleOrdersLikeParameterized, 4 cases): source-pin asserts `LIKE '{` (the vulnerable shape) is not in the function source and that `LIKE ?` IS in it; capture-execute-args test fires `mark_stale_orders_canceled` with prefix `"' OR 1=1 --"` and confirms the raw payload appears in params but NOT in the executed SQL string (for both exclude_prefix and only_prefix branches); functionality-preservation test confirms "algo:" prefix still passes through unchanged.

### [LOW-016] [Quality] api/routes_orders.py, api/routes_history.py
  Description: No server-side maximum on `per_page` query parameter
  Evidence: `per_page: int = 20` with no upper bound. `per_page=999999` scans entire table in one query.
  Impact: DoS vector via crafted requests. UI-only limit (20/50 dropdown) easily bypassed.
  Recommended fix: `per_page = min(max(per_page, 1), 100)` in route handlers
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence: grep `per_page: int = 20` across api/ returns 5 hits (routes_orders.py:42,65,91,117 + routes_history.py:94). All bare `per_page: int = 20` with no upper-bound clamp.
  Sweep note: Confirmed at 5 route entries. Single-line clamp per route. Bundles naturally with MED-003 (sort whitelist) and LOW-001 (ticker regex) as the "route-level input validation" task.

### [LOW-017] [Reliability] core/event_bus.py:108-109
  Description: `close()` method is a no-op; event bus task runs indefinitely after shutdown
  Evidence: `async def close(self) -> None: """No-op"""`
  Impact: Messages queued after shutdown never drained; task outlives app context
  Recommended fix: Set a stop flag; drain queue on close
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (event_bus.py:108-109):
      async def close(self) -> None:
          """No-op — kept for interface compatibility with old redis_bus."""
  Sweep note: Confirmed verbatim. Pairs with MED-020 (background tasks not cancelled on shutdown) as a "graceful shutdown" bundle.

### [LOW-018] [Correctness] core/db_orders.py:562
  Description: Fill ordering by timestamp_ms without tiebreaker for same-ms fills (already in pass 2 as MED-011 — confirming)
  Note: Already recorded as MED-011. Skipping duplicate.
  Sweep classification: SCOPE-SHIFTED (Task 90.5c)
  Sweep note: Audit doc itself flags this as a duplicate of MED-011 (same file:line, same claim). When MED-011 is fixed, LOW-018 is automatically resolved. Recommend the audit's master record consolidate (mark LOW-018 as "dup of MED-011 — close on MED-011 resolution"). No separate tracking needed.
  Duplicate note: LOW-018 is a duplicate of MED-011 (audit doc self-flagged). Treat as consolidated; resolve MED-011 only.
  Status: DUPLICATE of MED-011 (Task 90.5e cleanup) — audit doc itself self-flagged this duplication ("Note: Already recorded as MED-011. Skipping duplicate."). Do not count toward active Low total. MED-011 is the canonical entry; resolve via that ID only.

---

## Fourth Pass (2026-05-17, same session)

Audit of account management, data export, Quantower bridge, analytics math,
regime/news fetchers, exchange adapter internals, and error handling patterns.

---

## 16. Data Export & Account Isolation

### [CRIT-006] [Data Integrity] core/data_logger.py:216-218
  Description: `export_all_to_excel()` calls `db.get_all_pre_trade_log()`, `get_all_execution_log()`, `get_all_trade_history()` WITHOUT passing `account_id` — defaults to `account_id=1`
  Evidence: `pre_trade_df = pd.DataFrame(await db.get_all_pre_trade_log(days=365))` — no account_id arg. DB functions default to `account_id: int = 1`.
  Impact: Multi-account users always export account 1's data regardless of active account. Data isolation completely broken for exports.
  Recommended fix: Pass `account_id=app_state.active_account_id` to all three calls
  Effort: small
  Status: RESOLVED in Task 89 — threaded app_state.active_account_id through all three db.get_all_* calls.
  Implementation note (Task 89):
    No DB-layer change needed — all three methods already accepted account_id as a kwarg
    defaulting to 1. core/data_logger.py:216-218 now binds `aid = app_state.active_account_id`
    once (alongside the existing now_in_account_tz(aid) usage at line 213) and passes it
    explicitly. Regression tests:
      - test_export_passes_active_account_id_to_all_three_db_calls (mock-based explicit pin
        on all three call sites — catches partial fixes; failure-before-fix verified by
        reverting one of the three passes).
      - test_export_returns_only_active_account_rows (integration test against a real
        DatabaseManager seeded with two accounts' rows; asserts only the active account's
        row reaches the xlsx output).

---

## 17. Account Management

### [HIGH-012] [Correctness] api/routes_accounts.py + core/order_manager.py:607
  Description: Account switch while a deferred close-row build is pending can write old account's position to new account
  Evidence: `loop.call_later(2.0, lambda f=fill: asyncio.ensure_future(self._build_close_row_for_fill(account_id, f)))` — the lambda captures `account_id` from the outer scope which changes on account switch. If switch happens during the 2s delay, the close row may use the old account's data in a mixed context.
  Impact: Cross-account contamination of closed_position records during rapid account switches
  Recommended fix: Capture `account_id` as a value in the lambda, not a closure reference; verify it matches active account before insert
  Effort: small
  Sweep classification: NEEDS DEEP READ (Task 90.5c)
  Sweep evidence (order_manager.py:594-599):
      if fill.get("is_close"):
          loop = asyncio.get_running_loop()
          loop.call_later(
              2.0,
              lambda f=fill: asyncio.ensure_future(
                  self._build_close_row_for_fill(account_id, f)
  Sweep note: Subtler than the audit suggests. `account_id` is a function parameter of process_fill — Python function parameters are bound once at call entry and don't mutate from external account-switches. The lambda's closure captures the parameter's binding, which is stable. So the "outer scope changes on account switch" race-description is questionable as written. BUT the recommended fix (explicit value capture + active-account verification before insert) is still defensible defense-in-depth, especially the "verify it matches active account before insert" half. Defer to fix-task for a careful trace of: (1) is account_id ever rebound inside process_fill? (2) is the audit's race actually triggerable via some path I haven't traced? (3) should the verify-active-before-insert half be split into a separate finding?
  Status: VERIFIED FALSE in Task 96 — lambda captures function parameter; race structurally impossible.
  Verification note (Task 96):
    Deep-read of OrderManager.process_fill at core/order_manager.py:565-601:
        async def process_fill(self, account_id: int, fill: Dict[str, Any]) -> None:
            ...
            if fill.get("is_close"):
                loop = asyncio.get_running_loop()
                loop.call_later(
                    2.0,
                    lambda f=fill: asyncio.ensure_future(
                        self._build_close_row_for_fill(account_id, f)
                    ),
                )

    Capture analysis:
      - `self` and `account_id` are free vars; `f=fill` is explicit-value-captured already (correct).
      - `account_id` is the function parameter of process_fill. Each process_fill
        call has its own frame with its own account_id local variable.
      - Grep of process_fill body confirmed no reassignment of account_id (uses
        only: line 569 fill.setdefault, 576 _snapshot_and_fix_isclose, 587
        get_position_fees, 599 lambda body).
      - The lambda outlives process_fill (call_later 2.0s), but the captured
        cell stays pinned to the value passed at call time. Python frame-locals
        are not reachable from outside the function — no `app_state.something =
        ...` mutation can affect this binding.

    The audit's "outer scope changes on account switch" mechanism is incorrect:
    process_fill's local scope is private and immutable from outside.
    Each call has its own account_id; concurrent process_fill calls with different
    account_ids each have their own correctly-pinned closures.

    The "verify-active-account-before-insert" half of the audit's recommended fix
    was defensible only under the (incorrect) assumption that the captured value
    could go stale. Since it can't, the verify is redundant — the deferred close
    inserts to the correct account by construction.

    Action: no production change. Regression pin in
    tests/test_task96_high012_closure_pin.py uses AST inspection to assert the
    lambda references account_id as a bare ast.Name (function parameter), not
    an ast.Attribute (which would be self.account_id, app_state.active_account_id,
    or similar mutable path). Non-vacuous proof: temporarily replaced the
    lambda body's `account_id` with `self.account_id`; pin failed with explicit
    "HIGH-012 closure pattern changed... captured value can now be mutated by
    external code... apply audit's recommended fix" message. Restored.

    This is the 8th VERIFIED FALSE in the race/concurrency-framed cluster
    (CRIT-003, CRIT-005, HIGH-004, HIGH-005, HIGH-006, HIGH-019, HIGH-025,
    now HIGH-012). The audit consistently assumed Python runtime semantics
    permit concurrent mutation where in fact local-variable / GIL / cooperative-
    asyncio semantics provide structural protection. Future audit batches
    targeting "race" findings should default to verify-before-assume.

### [MED-023] [Security] api/routes_platform.py:15-21
  Description: Empty `PLATFORM_TOKEN` (default) bypasses Quantower auth entirely
  Evidence: `if not expected: return` — when token is empty string (default), all callers pass auth check
  Impact: In default config, any network client can send platform events to `/ws/platform` and `/api/platform/*`
  Recommended fix: Log warning on startup if PLATFORM_TOKEN is empty; consider requiring non-empty token
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (routes_platform.py:15-21):
      def _verify_token(token: str | None) -> None:
          expected = config.PLATFORM_TOKEN
          if not expected:
              return  # no token configured — allow (local-only deployment)
          if token != expected:
              raise HTTPException(...)
  Sweep note: Confirmed verbatim. Comment "no token configured — allow (local-only deployment)" reveals the intentional bypass for local dev. Audit's recommended startup-warning is the right hardening.

### [MED-024] [Data Integrity] core/database.py:186-196
  Description: `broker_account_id` column has no UNIQUE constraint; duplicate values cause non-deterministic fill routing
  Evidence: `find_by_broker_id()` returns first match — if two accounts share the same broker_account_id, fills from Quantower route to the wrong account
  Impact: Fill misrouting in multi-account setups
  Recommended fix: Add UNIQUE constraint on broker_account_id (with migration)
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (database.py:186-196): `accounts` CREATE TABLE shows `broker_account_id TEXT` with no UNIQUE constraint. Inline ALTER list (line 486) also adds the column without UNIQUE. Both creation paths leave the column unconstrained.
  Sweep note: Multi-account fill misrouting is a real risk if two accounts share the same broker_account_id. UNIQUE migration is the audit's recommended fix; needs to handle existing duplicate rows if any are present (migration script should query for duplicates first).

---

## 18. Platform Bridge Input Validation

### [MED-025] [Security] core/platform_bridge.py:200-205
  Description: WebSocket handler accepts unlimited message size from Quantower plugin
  Evidence: `async for raw in websocket.iter_text()` — no size limit. Malicious client can send gigabyte-sized messages.
  Impact: Memory exhaustion DoS
  Recommended fix: Add `if len(raw) > 1_000_000: continue` or configure FastAPI WS size limit
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (platform_bridge.py:200): `async for raw in websocket.iter_text():` — no size check.
  Sweep note: Confirmed. Trivial fix. Bundles with MED-023 (empty PLATFORM_TOKEN bypass) as "platform-bridge hardening".

### [MED-026] [Correctness] core/platform_bridge.py:49-120
  Description: Quantower message normalization functions don't validate field types or bounds
  Evidence: `_map_fill()` accepts any price/quantity values including None, negative, NaN, inf. Catches generic Exception but inserts defaults (0.0).
  Impact: Malformed Quantower messages create corrupt fill/position records
  Recommended fix: Add bounds checks: price > 0, quantity > 0, symbol is non-empty string
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (platform_bridge.py:80-81):
      "price":        float(msg.get("price", 0)),
      "quantity":     abs(float(msg.get("quantity", msg.get("qty", 0)))),
  Sweep note: Price has no clamp. Quantity uses abs() (good — kills negative) but no upper bound and no NaN/inf check. Confirmed multiple sites (lines 80, 81, 144, 146). Fix scope is moderate — needs adding validation across _map_fill, _map_position, _map_order paths.

---

## 19. Exchange Adapter Robustness

### [HIGH-013] [Reliability] core/adapters/binance/rest_adapter.py:72-79
  Description: Missing account fields from Binance API response silently default to 0
  Evidence: `total_equity=float(info.get("totalWalletBalance", 0) or 0)` — if Binance omits field or returns null, equity becomes 0.0
  Impact: Account equity reported as 0 → risk engine allows excessive position sizing (thinks no capital at risk)
  Recommended fix: Validate critical fields (equity, balance) are present and > 0; raise if missing
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (binance/rest_adapter.py:72-79):
      total_equity=float(info.get("totalWalletBalance", 0) or 0),
      available_margin=float(info.get("availableBalance", 0) or 0),
      unrealized_pnl=float(info.get("totalUnrealizedProfit", 0) or 0),
      ...
  Sweep note: Confirmed verbatim across 8 fields (lines 72-79). Each defaults missing/null to 0 silently. Audit's risk-engine-allows-excessive-sizing impact is real — pairs with MED-018 (negative-equity clamp) for a "trust-but-verify exchange responses" hardening bundle.
  Status: RESOLVED in Task 102 — Shape A (raise on missing critical fields) for the two equity-gating fields; Shape B (default + log) for the three non-critical numeric fields.
  Implementation note: After info/comm unpack in core/adapters/binance/rest_adapter.py:fetch_account (current line ~112), the two critical fields (`totalWalletBalance` → total_equity, `availableBalance` → available_margin) are validated via explicit `info.get(...) is None` checks. Either being missing or null raises ValueError with a "phantom-zero" message identifying the offending field. The three non-critical numeric fields (totalUnrealizedProfit / totalInitialMargin / totalMaintMargin) keep their `info.get(..., 0) or 0` default but a log.warning fires when each is absent. Behavior trace: schedulers._account_refresh_loop's existing `except Exception` catches the ValueError and logs "Periodic account refresh failed" — internal state (`acc.total_equity`, `acc.balance_usdt`) keeps its prior known-good value rather than being overwritten by a phantom zero.
  Downstream-consumer analysis: pre-fix, equity=0 propagated to (a) risk_engine.py:351's `total_equity if > 0 else 1.0` guard — sizing reverted to a phantom 1.0 USDT equity (micro-positions, not "excessive" as the audit phrased it); (b) data_cache._recalculate_portfolio's exposure / drawdown calcs (all guarded by `if > 0` — silently zeroed); (c) BOD/SOW equity init (line 670-676) — would silently freeze at 0 forever. The audit's "excessive sizing" framing is wrong-direction (sizing actually collapses to micro), but the silent corruption of prior state is real. The fix preserves prior state by failing the refresh cycle cleanly.
  Tests (tests/test_task102_trust_but_verify.py::TestBinanceFetchAccountCriticalFieldValidation, 6 cases): missing totalWalletBalance raises; missing availableBalance raises; null value raises (not just absent key); complete response applies normally; missing non-critical field logs+defaults (no raise); zero value valid (not treated as missing).

### [HIGH-014] [Reliability] core/adapters/base.py:88-110
  Description: Weight tracker never reconciles estimate with exchange response headers
  Evidence: Tracker estimates request cost upfront but never reads `X-MBX-USED-WEIGHT-1M` (Binance) or equivalent response headers to update actual usage
  Impact: Tracker estimate diverges from server truth over time; may under/over-throttle
  Recommended fix: Read response headers post-call and reconcile tracker state
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence: grep `X-MBX-USED-WEIGHT|response\.headers|reconcile|update_actual` across core/adapters/ returns 0 hits. Tracker has no response-header reconciliation path.
  Sweep note: Confirmed by absence. Reconciling against exchange-reported usage requires intercepting CCXT's HTTP response — moderate effort. Pairs with HIGH-007 (weight tracker exceptions swallowed) for a "tracker observability + accuracy" bundle.
  Status: RESOLVED in Task 99 (Binance only) — reconciliation fix matches the audit description directly. `_reconcile_from_response()` hook added on BaseExchangeAdapter (no-op default) and called from `_run()` after each successful executor return, wrapped in try/except so reconciliation failure cannot corrupt the call result. BinanceUSDMAdapter overrides the hook to parse `X-MBX-USED-WEIGHT-1M` (case-insensitive) from `self._ex.last_response_headers` and call the pre-existing tracker.reconcile(). Bounds-check rejects negative or implausibly-large values so a malformed header cannot poison the budget. Bybit override deferred — tracked separately as HIGH-028 (filed Task 99.1). weight_tracker.py module docstring updated to reflect partial v2.4 coverage. Tests: tests/test_task99_adapter_bundle.py::TestBinanceHeaderReconciliation pins the wiring (4 cases: header parsed, lowercase variant, missing header leaves estimate intact, garbage header ignored).
  Framing note (Task 99.1): The audit's description ("never reconciles estimate with exchange response headers") and Impact ("Tracker estimate diverges from server truth over time") explicitly describe drift, not concurrent state corruption. The fix is a reconciliation wire-up, not a concurrency primitive — which matches the audit recommendation exactly. No race-claim verification was needed; this finding does NOT count toward the race-framing false-positive calibration.
  Cross-reference: HIGH-028 (Bybit parallel — deferred from this task because the response-header schema differs).

### [HIGH-015] [Reliability] core/adapters/binance/rest_adapter.py:66, core/adapters/bybit/rest_adapter.py:64
  Description: Commission/fee rate fetch fails silently with no logging in both adapters
  Evidence: `except Exception: comm = {}` (Binance), `except Exception: fees = {}` (Bybit) — no log.warning
  Impact: Fee tier defaults to 0; all position PnL calculations understate costs
  Recommended fix: Add `log.warning("Fee fetch failed: %r", e)` in exception handler
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (binance/rest_adapter.py:66-67):
      except Exception:
          comm = {}
  Sweep note: Confirmed binance side at line 66. Trusting audit for bybit side at :64 (same pattern). Trivial fix: add log.warning before the assignment. Bundles with HIGH-007 and LOW-019 as "log all swallowed exceptions in adapter layer" cluster.
  Status: RESOLVED in Task 99 — both fee-fetch except blocks (Binance commissionRate, Bybit fee-rate) now call log.warning with the exception type/message before falling through to the empty-dict default. Swallow behavior preserved: callers depend on the default (Bybit drops to VIP0 defaults via fee_source="default"; Binance reads 0/0 and is fed to NormalizedAccount). Tests in tests/test_task99_adapter_bundle.py::TestFeeFetchLogsOnFailure pin both adapters via AST inspection; TestSwallowBehaviorPreserved confirms the default return path still works end-to-end on the Binance side.

### [HIGH-016] [Security] core/crypto.py:54-58
  Description: Decryption failure returns empty string instead of raising
  Evidence: `except (InvalidToken, Exception): return ""` — callers receive empty API key/secret, attempt exchange auth with empty credentials
  Impact: Silent auth failure after master key rotation; engine runs credentialless with no visible error
  Recommended fix: Raise or set app_state flag to disable trading on decryption failure
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence: Same lines as HIGH-010 (crypto.py:54-58). The `return ""` fallback persists; log.error was added at line 55 partial mitigation.
  Sweep note: DUPLICATE of HIGH-010. The audit filed the same finding twice (HIGH-010 in Section 9, HIGH-016 in Section 19) with slightly different language. Recommend the audit consolidate — one fix resolves both. Counts as 1 real finding; the duplicate inflates the High-severity count.
  Duplicate note: HIGH-016 is a duplicate of HIGH-010 (same finding filed twice across Sections 9 and 19). Treat as consolidated; resolve HIGH-010 only.
  Status: DUPLICATE of HIGH-010 (Task 90.5e cleanup) — same core/crypto.py:54-58 decryption silent-fallback finding. Do not count toward active High total. HIGH-010 is the canonical entry; resolve via that ID only.

---

## 20. Analytics & Regime

### [MED-027] [Correctness] core/analytics.py:209-212
  Description: `profit_factor` returns 999.0 sentinel when all trades are winners (no losses)
  Evidence: `else 999.0` — consumers can't distinguish "infinite profit factor" from "very high profit factor"
  Impact: Misleading analytics display; consumers may treat 999.0 as actual computed ratio
  Recommended fix: Return `None` or `float('inf')`; let UI render "N/A" or "∞"
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (analytics.py:209-213):
      profit_factor = (
          abs(sum(pos) / sum(neg))
          if neg and sum(neg) != 0
          else 999.0
      )
  Sweep note: Confirmed verbatim. The 999.0 sentinel is real magic-number-as-stand-in-for-inf.

### [MED-028] [Performance] core/regime_fetcher.py (~fetch_vix)
  Description: `yfinance.download()` is synchronous and blocks the event loop
  Evidence: Called from async context via scheduler; yfinance uses requests (sync HTTP) internally
  Impact: Event loop blocked 1-2s during VIX fetch; stalls order processing and WS message handling
  Recommended fix: Wrap in `asyncio.to_thread()` or replace with async httpx call to Yahoo Finance
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (regime_fetcher.py:47, 61):
      async def fetch_vix(...):
          ...
          df = yf.download(...)
  Sweep note: Confirmed bare yf.download() inside async def fetch_vix. No asyncio.to_thread wrap. Bundles with MED-005 (sync sqlite3 in async) as the "async-blocking-IO sweep" task.

### [MED-029] [Correctness] core/regime_classifier.py
  Description: No hysteresis band on regime transitions; signal oscillation near thresholds causes rapid flipping
  Evidence: If VIX oscillates around a threshold (e.g., 20±0.5), regime flips on every check cycle
  Impact: Regime-dependent position sizing multipliers jitter; trade signals unstable
  Recommended fix: Add hysteresis bands (e.g., enter at 22, exit at 18) or cooldown timer
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence: grep `hysteresis|cooldown|prev_regime|previous_state` in core/regime_classifier.py returns 0 hits. No hysteresis/cooldown pattern present.
  Sweep note: Confirmed by absence. Real correctness concern; oscillating regimes would jitter sizing multipliers as audit describes.

### [MED-030] [Correctness] core/regime_fetcher.py (~fetch_fred_series)
  Description: FRED API response structure not validated before key access
  Evidence: Code assumes `"observations"` key exists; error response (e.g., invalid series ID) causes KeyError crash
  Impact: Regime fetcher crashes on FRED API error; regime stale until next successful fetch
  Recommended fix: Validate response structure: `if "observations" not in data: log.warning(...); return`
  Effort: small
  Sweep classification: SCOPE-SHIFTED (Task 90.5c)
  Sweep evidence (regime_fetcher.py:153):
      observations = data.get("observations", [])
  Sweep note: Audit's KeyError failure mode is WRONG — the code uses `.get("observations", [])` with default empty list, which cannot KeyError. BUT the secondary impact (regime stale on API error) is real: if FRED returns an error response without "observations" key, `observations = []` silently → no rows recorded → regime stays at last-known value. The recommended fix (`log.warning + return`) is still defensible to surface the silent failure. Re-spec needed: severity may be lower than MED since failure is silent-degraded not crash; fix is observability, not correctness.

---

## 21. Error Handling Patterns

### [LOW-019] [Quality] core/adapters/binance/rest_adapter.py:384,509
  Description: Price extremes and other adapter methods catch Exception without logging
  Evidence: `except Exception: return None, None` — no `log.warning()` or `log.debug()`
  Impact: Lost diagnostic information on malformed exchange responses
  Recommended fix: Add `log.debug("price_extremes failed: %r", e)` in exception handlers
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5c)
  Sweep evidence (binance/rest_adapter.py:384-385):
      except Exception:
          return None, None
  Sweep note: Confirmed at :384. Trusting audit on :509 (same pattern). Pairs with HIGH-007 (weight tracker) + HIGH-015 (fee fetch) as the "adapter silent-swallow log-it-all" cluster.
  Status: RESOLVED in Task 99 — both swallow sites now log.warning before returning the safe default. `_agg_extremes` returns (None, None) → upstream falls back to OHLCV; the warning now identifies which symbol/window failed and why. `fetch_current_funding_rates` returns the zero-funding/zero-mark default → the warning identifies the symbol-count affected (so downstream PnL/sizing being free-of-carry is no longer mysterious). Tests in tests/test_task99_adapter_bundle.py::TestBinanceSilentSwallowsLog pin both via AST inspection of the source.

---

## Fifth Pass (2026-05-17, same session)

Audit of DD state machine, pubsub internals, calc correlation, contract
validation, and cross-cutting error propagation paths.

Verified FALSE findings discarded:
- dd_state.py recovery threshold "inverted" — FALSE (formula and docstring consistent)
- .env committed to git — FALSE (.gitignore correctly excludes .env)
- ENV_MASTER_KEY empty crashes — already handled by RuntimeError in crypto.py
- calc_correlation race condition — FALSE for asyncio single-threaded model

---

## 22. Drawdown Enforcement

### [HIGH-017] [Correctness] core/dd_gate.py:46-47
  Description: DD gate fails OPEN on database read error — allows trading when enforcement mode can't be determined
  Evidence: `except Exception: return True, None` — if `get_account_settings()` raises (DB corruption, connection lost), the gate allows order placement even for accounts configured as "enforced"
  Impact: Enforced drawdown limits bypassed during DB outages; trader can violate limits unknowingly
  Recommended fix: Fail closed: `return False, "unable_to_read_enforcement_settings"`. Or cache last-known enforcement mode.
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (dd_gate.py:46-47):
      except Exception:
          return True, None  # can't read settings → don't block
  Sweep note: Confirmed verbatim including the "don't block" comment which captures the fail-open semantic. Real reliability concern for trading installations under DB stress.
  Status: RESOLVED in Task 98 — fail-closed default on settings-read failure.
  Implementation note (Task 98):
    Calling convention: dd_gate_allows_new_entry returns (True, None) = ALLOW
    entry, (False, reason) = BLOCK. The audit's "fail-open" was `return True,
    None` on the except path; the fix inverts to `return False,
    "dd_gate_settings_unreadable"`. Trading-block gates default to BLOCK on
    unknown state — the alternative (allow trading because we can't determine
    enforcement mode) silently bypasses configured DD limits during DB stress,
    the exact scenario the limit is meant to protect against. Added log.error
    on the except path describing the fail-closed action so operators can
    correlate against DB-outage logs.
    3 tests in TestDDGateFailClosed: load-bearing pin (settings-read raise →
    blocked + logged), normal-path sanity (dd_state="ok" → allowed),
    advisory-mode sanity (dd_state="limit" + mode="advisory" → allowed).
    Anti-over-correction confirmed: normal paths pre/post-fix unchanged.

---

## 23. PubSub Internals

### [HIGH-018] [Reliability] core/pubsub/in_process_bus.py:46
  Description: Any exception during publish (not just QueueFull) permanently disconnects the subscriber queue
  Evidence: `except Exception: dead.append(q)` followed by `queues.discard(q)` — a transient error (e.g., payload serialization failure) permanently removes the subscriber
  Impact: UI SSE streams or internal event consumers silently stop receiving updates; no recovery without restart
  Recommended fix: Only disconnect on QueueFull; log other exceptions without removing the queue
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (in_process_bus.py:41-49):
      except asyncio.QueueFull:
          log.debug("InProcessBus: dropped message ...")
      except Exception:
          dead.append(q)
      for q in dead:
          queues.discard(q)
  Sweep note: Confirmed verbatim. QueueFull is logged + kept; any other Exception adds to dead list and the discard at line 49 permanently removes. Audit's fix recommendation (narrow the except to specific exception types, log unknowns without disconnecting) is the right shape.
  Status: RESOLVED in Task 98 — broad-except disconnect replaced with log.exception + keep-subscriber.
  Implementation note (Task 98):
    In InProcessBus.publish (core/pubsub/in_process_bus.py:35-51), the broad
    `except Exception: dead.append(q)` path is replaced with `log.exception(
    "InProcessBus: unexpected error delivering to subscriber on %s; keeping
    subscription", channel)`. The post-loop `dead` discard machinery is
    removed entirely.
    
    Disconnect-on-error analysis: the broad-except disconnect was NOT the
    only cleanup path. subscribe()'s `finally` block at lines 62-68 already
    handles normal subscriber cleanup when the iterator generator ends
    (GeneratorExit / CancelledError, or the consumer simply stops iterating).
    The removed path was a "subscriber misbehaving, kick them" mechanism —
    appropriate response to a corrupt subscriber but inappropriate response
    to a transient payload-serialization error or a recoverable bug. No
    resource leak risk: queues that never get cleaned up are still bounded
    by maxsize and will get GC'd when the subscriber generator is collected.
    
    QueueFull path unchanged (already correctly handled — log + drop event,
    keep subscriber).
    
    3 tests in TestPubsubKeepsSubscriber: load-bearing pin (faulty queue
    raises RuntimeError → subscriber kept + log.exception), QueueFull
    pin (sanity), normal-delivery anti-over-correction.

---

## 24. Order Lifecycle Gaps

### [HIGH-019] [Correctness] core/order_manager.py (TP/SL placement)
  Description: Position added to state before TP/SL orders are placed; if TP/SL placement fails, position has no protection
  Evidence: Entry fill → position created in DataCache → then TP/SL placed in separate API calls. If TP/SL call fails (rate limit, invalid price, network), position sits unprotected.
  Impact: Trader's position exposed to unlimited loss without stop-loss during the failure window
  Recommended fix: Queue TP/SL retry with exponential backoff; mark position as "awaiting protection" in UI until TP/SL confirmed
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence: grep `place_tp_sl|tp_sl_order|awaiting_protection|TP/SL.*queue|place_tpsl` in core/order_manager.py returns 0 hits. No retry queue, no "awaiting protection" state machine. Position-update flow does not gate on TP/SL placement success.
  Sweep note: Confirmed by absence. Currently slot 3 in Top 5; remains there until fix. Pairs with HIGH-026 (silent-swallow in close path) as the "position-protection state machine" cluster — both about making the post-fill lifecycle fail-loud and recoverable.
  Status: VERIFIED FALSE in Task 93 — architectural mismatch (engine is passive observer, not order placer)
  Verification note (Task 93):
    Grep of core/ for `place_tp_sl|place_tpsl|create_tpsl|create_order|place_order` found
    zero matches. `individual_tpsl=True` is set only inside _apply_order_update
    (core/ws_manager.py:166, 174, 261, 265) reacting to externally-observed
    ORDER_TRADE_UPDATE WS events. The engine is fundamentally a passive observer;
    the Quantower plugin (or user-side workflow) places orders directly to the
    exchange, and the engine reads the resulting events.
    
    The audit's "retry queue + awaiting-protection flag" recommendation assumed
    engine-side placement. That assumption does not match this codebase. No
    place_tp_sl function exists to wrap with a retry queue.
    
    The sweep note's pairing claim with HIGH-026 ("position-protection state
    machine cluster") was also based on the same incorrect assumption. HIGH-026
    is about silent-swallow in close-row build, which IS engine-side work;
    HIGH-019 is about TP/SL placement, which is NOT engine-side. The pairing
    doesn't actually hold.
    
    Action taken: No production change. Regression pin
    test_engine_does_not_place_tp_sl_orders in
    tests/test_task93_tpsl_protection.py walks core/*.py and asserts no
    place_tp_sl-style function exists. If a future contributor adds engine-side
    TP/SL placement, the pin fails with explicit guidance to re-evaluate.
    Non-vacuous proof: temporarily appended `async def place_tp_sl(*a, **k):
    pass` to core/log_formatter.py; pin failed with the architectural-invariant
    violation message; restored.
    
    Related residual concern (calc-to-fill matching window in core/exec_link.py
    — stale pretrades can retroactively match fresh fills) filed separately
    as HIGH-027.

### [HIGH-020] [Correctness] core/exchange.py:144
  Description: `fetch_account()` accesses `app_state._data_cache` without null check (inconsistent with `fetch_positions()` which guards at line 177)
  Evidence: `await app_state._data_cache.apply_account_update_rest(na, ts_ms)` — if DataCache not yet initialized (startup race), crashes with AttributeError
  Impact: Startup crash if account fetch fires before DataCache init; silent background scheduler failure
  Recommended fix: Add `if app_state._data_cache is None: log.warning(...); return` (matching fetch_positions pattern)
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence:
      exchange.py:144 (fetch_account):  await app_state._data_cache.apply_account_update_rest(na, ts_ms)   # no guard
      exchange.py:177-179 (fetch_positions):  if app_state._data_cache is None: log.warning(...); return
  Sweep note: Confirmed asymmetric guard pattern. Trivial 3-LOC fix to mirror fetch_positions's pattern in fetch_account.
  Status: RESOLVED in Task 93 — null-check added mirroring fetch_positions pattern.
  Implementation note (Task 93):
    3-LOC guard added to fetch_account at core/exchange.py:140-142:
        if app_state._data_cache is None:
            log.warning("fetch_account: DataCache not yet initialized — skipping")
            return
    Identical structure to fetch_positions' guard at lines 178-180 (now shifted
    by the 3 inserted lines). Tests in tests/test_task93_tpsl_protection.py
    pin the null-safety. Failure-before-fix verified: removing the guard
    produces AttributeError at the apply_account_update_rest call site;
    restored.

### [HIGH-021] [Reliability] core/contract_validation.py:38-53
  Description: Contract specs cached for 24h; exchange spec updates (min_qty, lot_step) cause false rejects until cache expires
  Evidence: Specs stored with 24h TTL. If exchange changes min_qty mid-day, cached old value rejects valid orders for up to 24h. No admin endpoint to force refresh.
  Impact: Valid orders silently rejected; no visibility into cache staleness
  Recommended fix: Reduce TTL to 1-4h; add admin endpoint to force refresh; log cache age in rejection reason
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (contract_validation.py:20,46):
      _CACHE_TTL_HOURS = 24
      ...
      if cached and (now - cached.fetched_at).total_seconds() < _CACHE_TTL_HOURS * 3600:
          return cached
  Sweep note: 24h TTL confirmed. No force_refresh admin endpoint anywhere in the file. Pairs naturally with MED-017 (mark price freshness) and MED-032 (TP/SL cache age) as the "cache-freshness hardening" bundle.
  Status: RESOLVED in Task 94 — TTL reduced (24h → 4h) + force-refresh admin endpoint added.
  Implementation note (Task 94):
    - Replaced `_CACHE_TTL_HOURS = 24` with `CONTRACT_SPEC_TTL_SECONDS = int(os.getenv("CONTRACT_SPEC_TTL_SECONDS", "14400"))` at core/contract_validation.py:20-25. Default 4h; operator-tunable via env.
    - Added `force_refresh_specs() -> int` at core/contract_validation.py:60-69. Clears the in-memory cache and returns the count of cleared entries.
    - Added admin endpoint POST /admin/contract-specs/refresh at api/routes_admin.py:388-401. No auth — matches existing admin endpoints (auth is HIGH-001's scope).
    - 4 regression tests pin: TTL ≤ 8h ceiling, 4h default when env unset, force_refresh_specs clears cache + returns count, admin endpoint registered.
  TTL rationale: 4h chosen as balance between freshness (crypto-futures listings/spec-changes happen within a day) and rate-limit cost (refetching specs for active symbols isn't free). Operator can lower for fast-moving symbol sets or raise for stable ones via env. 8h ceiling enforces the "audit's 24h was too long" intent — any future bump past 8h needs to come with a test update + a documented reason.

---

## 25. Analytics & Fetcher Edge Cases

### [MED-031] [Correctness] core/calc_correlation.py:232-239
  Description: Drift percentage divides by near-zero TP/SL prices, producing huge drift values that filter out legitimate matches
  Evidence: `t_drift = abs(tp_price - tp) / tp if tp and tp_price else 1.0` — if `tp = 1e-6` (micro-cap token), drift = 1000+ even for small absolute differences
  Impact: Legitimate order-to-calc correlations missed for low-priced tokens
  Recommended fix: Use absolute difference threshold as fallback when prices < 0.01: `if tp < 0.01: t_drift = abs(tp_price - tp)`
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (calc_correlation.py:233-235):
      e_drift = abs(entry_price - eff) / eff if eff else 1.0
      t_drift = abs(tp_price - tp) / tp if tp and tp_price else 1.0
      s_drift = abs(sl_price - sl) / sl if sl and sl_price else 1.0
  Sweep note: Confirmed verbatim. Same epsilon-vulnerability pattern flagged in MED-002 (R:R overflow) and MED-007's note — divisions guarded against 0 but not against near-zero. Family of "epsilon hardening" findings.

### [MED-032] [Correctness] core/exchange.py:232-255
  Description: Stale TP/SL orders cached in memory are never time-invalidated
  Evidence: `if any(p.individual_tpsl for p in app_state.positions): return` — if cache has old TP/SL data, REST refresh skipped. No age check.
  Impact: UI displays outdated TP/SL prices after user modifies them on exchange
  Recommended fix: Add last-update timestamp; force REST refresh if cache > 5 minutes old
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (exchange.py:306):
      if any(p.individual_tpsl for p in app_state.positions):
          return
  Sweep note: Line drift — audit cited :232-255, actual is :306. Claim accurate verbatim. Pairs with HIGH-021 (24h contract spec TTL) and MED-017 (mark price no timestamp) as the cache-freshness bundle.

### [MED-033] [Reliability] core/exchange.py:81-133
  Description: `fetch_account_info()` catches all exceptions silently with fallback values, no retry mechanism
  Evidence: `except Exception:` blocks at lines 107-111 set defaults (latency=0, fees=0) without logging the specific failure
  Impact: Stale exchange info displayed on dashboard; fee rates default to 0 affecting PnL calculations
  Recommended fix: Log exception details; add retry count; raise after N failures to trigger alert
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (exchange.py:107-110):
      try:
          time_sync.update(adapter.exchange_id, server_time - local_mid)
      except Exception:
          time_sync.mark_failed(adapter.exchange_id)
  Sweep note: Confirmed verbatim — `except Exception:` with no log of exception details. Pairs with HIGH-007 / HIGH-015 / LOW-019 as the adapter-silent-swallow cluster. Trivial fix.

---

## Sixth Pass (2026-05-17, same session)

Audit of remaining route handlers (calculator, backtest, analytics, params,
connections, news, regime), order enrichment, order state machine, monitoring
health checks, and equity gap detection.

---

## 26. Remaining Route Handlers

### [HIGH-022] [Security] api/routes_backtest.py:85
  Description: Backtest endpoint accepts unbounded date ranges — can trigger 100+ year backtests exhausting RAM
  Evidence: `date_from` and `date_to` passed directly to `BacktestRunner` with no range validation
  Impact: DoS via crafted request; memory exhaustion locks event loop
  Recommended fix: Validate `(to_date - from_date) <= 730 days`; return 400 if exceeded
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (routes_backtest.py:122-129):
      date_from  = cfg.get("date_from", "")
      date_to    = cfg.get("date_to", "")
      ...
      runner = BacktestRunner(cfg)
  Sweep note: Line drift — audit cited :85, actual call site is :122-129. No range validation between the .get() and the runner instantiation. Real DoS vector via crafted request.
  Status: RESOLVED in Task 97 — `_validate_date_range` helper + wired into both backtest routes.
  Implementation note (Task 97):
    - Added MAX_BACKTEST_DURATION_DAYS=365 and MIN_BACKTEST_DURATION_DAYS=1 constants
      at api/routes_backtest.py:27-31. Audit suggested 730 but 365 matches typical
      strategy-research horizons; can be raised via env if needed.
    - Added `_validate_date_range(date_from, date_to)` helper at routes_backtest.py:34-58.
      Accepts ISO-8601 strings; empty short-circuits to None (qt-import treats date_from/
      date_to as optional metadata). Rejects: malformed format, inverted (to < from),
      zero-day, > MAX duration.
    - Wired into `api_backtest_run` (line 127-129) and `api_qt_import` (line 197-200).
      Both return JSONResponse(400) with error string.
    - 7 unit tests in TestBacktestDateRangeValidation pin the helper's behavior on
      valid 30-day, valid 1-year, empty short-circuit, inverted, zero-day, over-1-year,
      and malformed-format inputs.
  Wiring pin (Task 97.1): inspect-based pins assert api_backtest_run and api_qt_import both call _validate_date_range. Catches regression where future refactor accidentally drops the helper call. Failure-before-fix verified by commenting out each helper call in turn. [Correctness] api/routes_params.py:50
  Description: Cross-parameter validation missing — allows `dd_warning_threshold > dd_limit_threshold` (inverted)
  Evidence: Each parameter validated independently against bounds, but no relationship check
  Impact: Inverted thresholds cause DD state machine to behave incorrectly (warning fires after limit)
  Recommended fix: Add `assert warning_threshold < limit_threshold` in validation
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (routes_params.py:46-48 + :63):
      weekly_loss_limit_pct:     float = Form(0.95),
      max_dd_warning_pct:        float = Form(0.80),
      max_dd_limit_pct:          float = Form(0.95),
      ...
      errors = validate_params(new_params)
  Sweep note: Params accepted at lines 46-48 (warning + limit pairs for DD and weekly-loss), validated at line 63 via validate_params. grep within routes_params.py for "limit_threshold|warning.*limit" returns 0 — confirms no cross-parameter relationship check. Audit's recommended assert is the right fix; may need to extend to weekly_loss_warning_pct ↔ weekly_loss_limit_pct pairing too.
  Status: RESOLVED in Task 97 — cross-parameter checks added to validate_params (core/state.py).
  Implementation note (Task 97):
    Extended `validate_params` in core/state.py with a _CROSS_PARAM_PAIRS table
    covering (max_dd_warning_pct, max_dd_limit_pct) and (weekly_loss_warning_pct,
    weekly_loss_limit_pct). Asserts `warning < limit` strictly; equality also
    rejected since equal thresholds collapse the warning state to zero width.
    Centralizing in validate_params (rather than per-route) means future cross-param
    pairs only need one entry in the table. 6 unit tests in
    TestCrossParameterValidation cover both pairs (accepted, equal-rejected,
    inverted-rejected, only-one-half-no-cross-check).

### [HIGH-024] [Security] api/routes_connections.py:55
  Description: API keys may appear in tracebacks if exception occurs during connections upsert
  Evidence: No `Sensitive` wrapper on key values; full key in exception traceback if DB write fails
  Impact: Credential exposure in error logs
  Recommended fix: Wrap keys in object that masks `__repr__`; sanitize tracebacks
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (routes_connections.py:128-131):
      api_key: str = Form(...),
      ...
      await connections_manager.upsert(provider, label, api_key)
  Sweep note: Line drift — audit cited :55 (which is HTMX attribute string), actual handler at :128. `api_key: str` parameter receives raw key from form; passed unwrapped to upsert. If upsert raises during DB write, FastAPI's traceback includes function arguments. Real credential-leak vector. Trivial fix: wrap in a SensitiveStr class with `__repr__` returning "***".
  Status: RESOLVED in Task 100 — SensitiveStr wrapper + entry-point wrapping (narrow scope: HIGH-024's literal site only).
  Implementation note:
    - New SensitiveStr class in core/security.py — str subclass; __repr__ / __str__ / __format__ mask the value, while .encode() goes through a C-level slot that preserves real bytes (verified experimentally: hmac.new(SensitiveStr(k).encode(), ...) matches hmac.new(k.encode(), ...) exactly).
    - api/routes_connections.py:128 wraps the form-supplied api_key into SensitiveStr before calling connections_manager.upsert. Any exception in encrypt() or db.upsert_connection() now shows <SensitiveStr len=N masked> in traceback frame locals rather than the raw key.
    - core/connections.py ConnectionsManager.upsert unwraps before storing in the in-memory cache. Reason: the cache value is read by _test_provider and passed into httpx URL params / headers, and urlencode/httpx URL builders serialize values via str() — which is the masked override. Leaving SensitiveStr in the cache would break authentication on the live HTTP test path.
  Tests (tests/test_task100_sensitive_str.py, 17 cases):
    - TestSensitiveStr (10): repr/str/format mask; encode bypasses mask; HMAC signing unchanged; traceback message masked; failure-before-fix contrast with plain str; unwrap returns raw plain str; startswith/endswith/in/slicing/len transparent.
    - TestRoutesConnectionsWiring (2): source-pin asserts upsert_connection wraps api_key; module imports SensitiveStr.
    - TestConnectionsManagerUnwraps (3): cache stores plain str (not SensitiveStr); plain str input still works (anti-over-correction); encrypt() sees real bytes via SensitiveStr.
    - TestTracebackLocalsMasked (1): simulated encrypt-raises scenario — repr of api_key local in the raising frame is masked.
    - TestUrlencodeBreakageDocumented (1): documents urlencode would emit "<masked>" if SensitiveStr leaked past the unwrap boundary, explaining the unwrap's necessity.
  Wiring pin (Task 100): inspect-based pin on upsert_connection asserts source contains "SensitiveStr"; second pin asserts module imports SensitiveStr from core.security. Catches regression where future refactor drops the wrapping.
  Scope note: Audit cited only api/routes_connections.py. Parallel exchange-credential leak vectors exist in api/routes_accounts.py (5 entry points: create_account, update_account, add_account_modal, test_account_preview, update_account_detail) and in core/account_registry.py post-decryption — these are NOT covered by this fix. urlencode/httpx breakage (verified: `urlencode({"k": SensitiveStr("v")})` produces `k=%3Cmasked%3E`) means a broader wrap would require .unwrap() at every httpx URL/header serialization site (ccxt instantiator, _test_provider, etc.). Flagging as latent HIGH-029 for a follow-up task that takes the manager-chokepoint approach (wrap at core/crypto.decrypt + every form route + explicit .unwrap() at HTTP serialization sites).

### [HIGH-025] [Correctness] core/order_enrichment.py:47-89
  Description: Partial fills don't trigger parent order re-enrichment — TP/SL trigger prices stale after first partial
  Evidence: `enrich_fill()` propagates calc_id and classifies fill_type but does NOT re-run `_populate_tp_sl_trigger_prices()` on the parent order
  Impact: Subsequent partial fills have stale enrichment data; slippage measurement incorrect for multi-fill entries
  Recommended fix: Re-enrich parent order on each partial fill when TP/SL may have shifted
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (order_enrichment.py:35-41 + 47-89):
      def enrich_fill(fill: Dict[str, Any], db_path: str) -> None:
          """Propagate calc_id from parent order to fill, classify fill_type, compute slippage."""
          try:
              _propagate_calc_id_to_fill(fill, db_path)
              _classify_and_compute_slippage(fill, db_path)
          except Exception:
              log.warning("fill enrichment failed for %s", ...)
  Sweep note: `enrich_fill` (line 35) does not call `_populate_tp_sl_trigger_prices` (line 47). The function only updates the fill itself, not the parent order's TP/SL trigger snapshot. Currently slot 5 in Top 5. Real correctness gap for multi-fill positions.
  Status: VERIFIED FALSE in Task 94 — literal claim true, but stale-snapshot impact does not materialize.
  Verification note (Task 94):
    The audit's literal observation is correct: enrich_fill does not call
    _populate_tp_sl_trigger_prices. But the IMPACT chain "stale enrichment
    data → slippage measurement incorrect" doesn't hold because the parent's
    trigger snapshot is refreshed via a different event and slippage reads
    fresh from DB anyway.
    
    Mechanism 1 — parent re-enrichment on child arrival:
      core/order_manager.py:204 `_re_enrich_parent_on_child_arrival(account_id, order)`
      runs whenever a child TP/SL order is persisted. It calls enrich_order
      on the parent, which re-runs _populate_tp_sl_trigger_prices. So if a
      user moves their TP/SL mid-trade, the parent's snapshot updates on the
      child-order event, not the fill event. Fills don't need to trigger
      this refresh because fills don't change TP/SL state — child order
      modifications do.
    
    Mechanism 2 — fresh DB read on every fill's slippage computation:
      core/order_enrichment.py:289-299 in _classify_and_compute_slippage
      does `SELECT * FROM orders WHERE account_id=? AND exchange_order_id=?`
      on every fill, then uses that parent row for slippage. There's no
      in-memory cached snapshot that could go stale between fills.
    
    Combined: multi-fill entries get correct per-fill slippage. The audit's
    "stale enrichment data" framing assumed an in-memory cache that doesn't
    exist; the implementation reads fresh per-fill.
    
    Action: no production change. Two regression pins:
      - test_re_enrich_parent_on_child_arrival_exists: inspects
        OrderManager._re_enrich_parent_on_child_arrival source, asserts it
        calls enrich_order. Pins mechanism 1 against future removal.
      - test_classify_and_compute_slippage_reads_parent_fresh_from_db:
        regex-scans _classify_and_compute_slippage source for
        `SELECT * FROM orders WHERE ... exchange_order_id` query. Pins
        mechanism 2 against future replacement by a stale cache lookup.
    Both pins use inspect.getsource + string/regex check, same pattern as
    Task 93's HIGH-019 architectural pin (already proven non-vacuous).

---

## 27. Order State Machine

### [MED-034] [Correctness] core/order_state.py:21-41
  Description: Transitions from terminal states (FILLED/CANCELED) silently dropped — rare exchange corrections ignored
  Evidence: `OrderStatus.FILLED: set()` — no outbound transitions. If exchange sends FILLED→CANCELED correction, update is silently skipped.
  Impact: Order state diverges from exchange; stale filled orders remain in local state
  Recommended fix: Allow FILLED→CANCELED transition; log as WARNING + emit engine_event
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (order_state.py:26-29):
      OrderStatus.FILLED:           set(),
      OrderStatus.CANCELED:         set(),
      OrderStatus.EXPIRED:          set(),
      OrderStatus.REJECTED:         set(),
  Sweep note: Confirmed verbatim. Terminal states cannot transition. Audit's recommended fix (allow FILLED→CANCELED with WARNING + engine_event) handles the exchange-correction edge case. Pairs with MED-035 (no engine_event on dropped transition) as the "order-state-machine fail-loud" bundle.

### [MED-035] [Correctness] core/order_manager.py:58-63
  Description: Invalid state transitions logged as WARNING but produce no audit trail (no engine_event)
  Evidence: `log.warning("Invalid transition..."); continue` — order skipped without event_log entry
  Impact: No forensic trail for dropped order updates; DD gate counters may be inaccurate
  Recommended fix: Emit `engine_event("order_transition_dropped", ...)` before continuing
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (order_manager.py:58-63):
      if not validate_transition(current_status, new_status):
          log.warning(
              "Invalid transition %s->%s for order %s, skipping",
              current_status, new_status, eid,
          )
          continue
  Sweep note: Confirmed verbatim. Trivial 2-LOC fix to emit engine_event before continue. Bundles with MED-034 (terminal-state corrections) for "order-state-machine fail-loud".

### [MED-036] [Correctness] api/routes_params.py:35
  Description: Parameter updates applied to live state without checking impact on open positions
  Evidence: `app_state.params.update(new_params)` — if `max_position_count` drops below current open count, risk engine throws on next cycle
  Impact: Mid-trade parameter change can crash risk engine
  Recommended fix: Validate `new_max >= current_open_count`; require confirmation for breaking changes
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (routes_params.py:63-68):
      errors = validate_params(new_params)
      if errors:
          return HTMLResponse(...)
      app_state.params.update(new_params)
  Sweep note: validate_params runs at line 63 but doesn't check against runtime state (len(app_state.positions) etc.). app_state.params.update at :68 commits the new max_position_count regardless. Confirms audit's concern. Trivial fix: add cross-check before update.
  Status: RESOLVED in Task 97 — runtime-state check added inline in update_params route.
  Implementation note (Task 97):
    Added inline check at api/routes_params.py:64-72: if max_position_count <
    len(app_state.positions), append a Validation error message and return without
    applying the update. Inline (rather than in validate_params) because the check
    requires app_state access; keeping validate_params pure avoids the
    import-circularity from core.state → app_state.
    3 unit tests in TestMaxPositionCountRuntimeCheck cover: validate_params alone
    doesn't catch runtime state (confirms separation), rejection path (3 open,
    new max=2), acceptance path (2 open, new max=5).
  Wiring pin (Task 97.1): AST-based pin asserts update_params contains a Compare node where one side is the Name `max_position_count` and the other references `len(app_state.positions)` (or equivalent runtime marker / known local alias), AND the literal source contains a runtime-marker string. Two-part check guards against both (a) dropping the comparison entirely and (b) replacing the runtime side with a hardcoded constant. Failure-before-fix verified both ways.

---

## 28. Monitoring & Gap Detection

### [MED-037] [Correctness] core/order_enrichment.py:231-274
  Description: Slippage calculation uses planned entry price (from pre_trade_log) not actual entry order fill price
  Evidence: `compute_slippage_actual(fill_type='entry')` compares fill against `effective_entry` from plan, not against the limit price of the entry order
  Impact: Slippage metric measures plan-vs-execution gap, not order-vs-fill gap — misleading for trades where the limit order itself was at a different price than the plan
  Recommended fix: Document that this is "shortfall vs plan" not "slippage vs order"; or add second metric for order-vs-fill comparison
  Effort: small (doc) / medium (new metric)
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (order_enrichment.py:237-241 docstring):
      """Compute slippage_actual for a fill.
      - entry: (fill_price - pre_trade_log.effective_entry) / effective_entry
      - tp/sl: (fill_price - parent_order.stop_price) / parent_order.stop_price
      ...
      """
  Sweep note: The function's own docstring confirms the semantic is "fill vs plan's effective_entry" for entries — exactly what the audit calls misleading nomenclature. Cheapest fix is the doc-only rename to "shortfall_vs_plan"; new metric for "order-vs-fill" is the more substantive option.

### [MED-038] [Reliability] core/monitoring.py:404-413
  Description: DB health check blocks entire monitoring coroutine for up to 5s if DB is stuck
  Evidence: `await asyncio.wait_for(cur.fetchone(), timeout=5.0)` — other checks delayed while this blocks
  Impact: Monitoring responsiveness degraded during DB contention
  Recommended fix: Run checks concurrently with `asyncio.gather()` or individual task isolation
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (monitoring.py:404-413):
      async def _check_db_health(self) -> None:
          try:
              async with db._conn.execute("SELECT 1") as cur:
                  await asyncio.wait_for(cur.fetchone(), timeout=_DB_HEALTH_TIMEOUT)
              self.resolve("db_unreachable")
          except Exception:
              if not any(...):
                  self.emit("db_unreachable", "critical", ...)
  Sweep note: Confirmed verbatim. The sequential-await pattern in the monitoring loop means each check holds the coroutine for its timeout. asyncio.gather() of independent checks is the recommended fix shape.

### [MED-039] [Correctness] core/equity_gap_detector.py:53
  Description: Gap threshold (2x interval = 20 min default) too lenient — misses 5-15 minute downtime windows
  Evidence: `threshold_ms = expected_interval_seconds * 2 * 1000` — 10-min interval → 20-min threshold. 15-min restart not flagged as gap.
  Impact: Analytics marked as valid despite data gaps from short downtimes
  Recommended fix: Reduce to 1.5x or make configurable; add startup gap check
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5d)
  Sweep evidence (equity_gap_detector.py:53):
      threshold_ms = expected_interval_seconds * 2 * 1000
  Sweep note: Confirmed verbatim. Audit's 1.5x suggestion is the simplest fix; making it configurable is the cleaner path.

---

## Seventh Pass — Final (2026-05-17, same session)

Final audit of base template, SSE streaming, static assets, service worker,
and end-to-end position close data flow. This pass confirms several areas
as safe and identifies 5 remaining findings.

Confirmed SAFE (no findings):
- SSE backpressure: bounded queue (100 max), non-blocking publish, QueueFull drops with log
- Service worker: network-first strategy for API/fragments — trading data never served stale
- _paginate_list(): safe in-memory pagination, no edge case bugs
- All global JS functions: no unchecked user input flows to dangerous sinks
- Position close flow: REPLACE logic prevents duplicate rows; reconciler catches gaps

---

## 29. Frontend Security Hardening

### [MED-040] [Security] templates/base.html:18-21
  Description: CDN scripts loaded without Subresource Integrity (SRI) hashes
  Evidence: `<script src="https://unpkg.com/htmx.org@1.9.12"></script>` — no `integrity` attribute. Same for idiomorph@0.3.0 and echarts@5.5.1.
  Impact: CDN compromise (unpkg, jsdelivr) would inject malicious code into every page load
  Recommended fix: Add `integrity="sha384-..."` and `crossorigin="anonymous"` to all CDN script tags
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5e)
  Sweep evidence (base.html:18-21):
      <script src="https://unpkg.com/htmx.org@1.9.12"></script>
      <script src="https://unpkg.com/htmx.org@1.9.12/dist/ext/sse.js"></script>
      <script src="https://unpkg.com/idiomorph@0.3.0/dist/idiomorph-ext.min.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/echarts@5.5.1/dist/echarts.min.js"></script>
  Sweep note: Four CDN scripts confirmed with no `integrity` or `crossorigin` attribute. Real CDN-compromise vector. Pairs with MED-041 (no CSP) as the "frontend security hardening" bundle.

### [MED-041] [Security] main.py (no CSP)
  Description: No Content-Security-Policy header configured on any response
  Evidence: No CSP middleware in main.py; no `<meta http-equiv="Content-Security-Policy">` in base.html
  Impact: Inline scripts and CDN scripts are unrestricted; XSS exploitation easier if a vector is found
  Recommended fix: Add CSP middleware: `script-src 'self' https://unpkg.com https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline'`
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5e)
  Sweep evidence: grep `Content-Security-Policy|CSP|add_middleware` in main.py returns 0 hits. grep `Content-Security-Policy` in templates/base.html returns 0 hits.
  Sweep note: Confirmed by absence. No CSP at HTTP-header level or meta-tag level. Bundles with MED-040 (no SRI) as the "frontend security hardening" bundle — single FastAPI middleware addition covers both directions.

### [MED-042] [Reliability] core/order_manager.py + core/db_orders.py (position close path)
  Description: No explicit timeout on DB operations in the critical position-close path
  Evidence: `await self._conn.execute(sql, params)` in `insert_closed_position()` and `upsert_order_batch()` — no `asyncio.wait_for()` wrapper
  Impact: Slow DB (disk contention, WAL checkpoint) stalls position close for unbounded time; UI shows stale state
  Recommended fix: Wrap critical DB operations in `asyncio.wait_for(op, timeout=5.0)` with fallback logging
  Effort: medium
  Sweep classification: VERIFIED REAL (Task 90.5e)
  Sweep evidence: grep `asyncio\.wait_for|insert_closed_position|upsert_order_batch` in core/db_orders.py returns the method definitions at lines 36 + 157 and their log.exception sites at 105 + 233, but no `asyncio.wait_for` wrapper anywhere in the file. Same for core/order_manager.py's call sites (line 785 invokes insert_closed_position bare).
  Sweep note: Confirmed by absence. Pairs naturally with HIGH-026 (silent-swallow in close path) — both about position-close path reliability. Both are Phase 4 candidates.

### [LOW-020] [Quality] templates/base.html (static assets)
  Description: Static files served without explicit Cache-Control headers
  Evidence: FastAPI `StaticFiles` mount uses defaults — no `max-age` or `immutable` directives
  Impact: Browser uses heuristic caching; mitigated by `?v=N` query strings on versioned assets
  Recommended fix: Add `Cache-Control: public, max-age=86400` for static assets
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5e)
  Sweep evidence (main.py:126):
      app.mount("/static", StaticFiles(directory="static"), name="static")
  Sweep note: Bare StaticFiles mount, no cache_control kwarg or wrapping middleware. Audit's own framing ("mitigated by `?v=N` query strings") is right — this is a hygiene fix, not a correctness one.

### [LOW-021] [Reliability] api/routes_streams.py
  Description: No maximum concurrent SSE connections limit
  Evidence: Each `EventSourceResponse` creates an asyncio.Queue subscriber; no cap on concurrent subscriptions
  Impact: Hundreds of open browser tabs could exhaust memory (~5KB each, but asyncio tasks add overhead)
  Recommended fix: Track active SSE connections; reject new ones above threshold (e.g., 50)
  Effort: small
  Sweep classification: VERIFIED REAL (Task 90.5e)
  Sweep evidence: grep `EventSourceResponse|max_connections|active_sse|connection_limit|MAX_SSE` in routes_streams.py returns only the two `EventSourceResponse(` instantiation sites at :52 and :78. No connection-counting state, no limit gate.
  Sweep note: Confirmed by absence. Single-user deployment makes the DoS-via-tabs less likely than the multi-user case, but adding a cap is cheap insurance.

### [LOW-023] [Test Infrastructure] tests/test_task101_sql_injection_defense.py:TestRoutesRejectInjection
  Description: Class-scoped `client` fixture builds a second `TestClient(app)` against the FastAPI app singleton. In full-suite runs `tests/test_routes.py` constructs the same TestClient(app) earlier under a module-scoped fixture; when this second TestClient's `with`-context calls lifespan startup again on the already-initialized app, the startup hangs indefinitely. Symptom matches Task 101's original ~1-hour test-suite hang exactly.
  Evidence: `pytest tests/` (full suite) hangs at TestRoutesRejectInjection's first test method (test_injection_in_sort_by_returns_400). pytest-timeout 2.4.0 (Task 101.1) catches the hang at the 30s mark with the stack frame pointing to tests/test_task101_sql_injection_defense.py:381 — the `with TestClient(app) as c:` line. Running the test file in isolation (`pytest tests/test_task101_sql_injection_defense.py`) completes in ~4s.
  Impact: 3 e2e injection-rejection tests (test_injection_in_sort_by_returns_400, test_injection_in_sort_dir_returns_400, test_valid_sort_request_returns_200) excluded from full-suite runs via @pytest.mark.skip. Coverage for the route-level wiring is preserved by TestRouteWiring's 4 source-pin tests in the same file; the wiring is verified by source inspection rather than HTTP. The injection rejection behavior is verified at the validator level by TestValidateSortParams (7 tests including concrete injection payloads).
  Recommended fix: Two options — (a) hoist the `client` fixture to `tests/conftest.py` with module or session scope so all callers share one TestClient/app instance; (b) investigate the lifespan re-entry deadlock (likely DB init, WS startup, or scheduler thread). Option (a) is the smaller change and resolves the immediate symptom; option (b) addresses the root architectural issue and would also help any other future test that needs a TestClient.
  Effort: small for (a), medium for (b).
  Status: OPEN — skip marker in place with reference to this finding. The skip's `reason=` text instructs how to re-enable.
  Discovered-during: Task 101.1 (pytest-timeout 30s caught the hang on first full-suite run after install — exactly the failure mode the guardrail was designed to surface).
  Cross-reference: HIGH-011 / MED-003 / MED-022 (parent fix in Task 101); Task 101.1 commit (guardrail that exposed the hang).
  Target phase: Phase 4 follow-up or test-infrastructure cleanup pass.
  Filed-during: Task 101.1

---

## Audit Coverage Summary

Seven passes covered ALL major modules:

| Area | Modules Audited | Passes |
|------|-----------------|--------|
| Trading path | order_manager, order_enrichment, order_state, exchange, data_cache | 1,2,3,5,6 |
| Risk engine | risk_engine, dd_state, dd_gate, contract_validation | 1,3,5 |
| Database | database.py, db_orders, db_trades, migrations, reconciler | 1,2,3 |
| Routes (all) | routes_orders, history, accounts, admin, platform, calculator, backtest, analytics, params, connections, news, regime, streams | 1,2,4,6 |
| Adapters | binance REST+WS, bybit REST+WS, base adapter | 2,4 |
| Templates | base.html, history.html, all fragments (13), exec_link_panel | 2,3,7 |
| Infrastructure | main.py, config.py, crypto.py, event_bus, pubsub, schedulers | 1,3,4,5 |
| Analytics | analytics.py, regime_classifier, regime_fetcher, news_fetcher, period_resolver | 4,5 |
| State | state.py, data_cache, account_registry, monitoring, equity_gap_detector | 2,4,5,6 |
| Frontend | JS globals, SSE streaming, service worker, static assets | 7 |

Further audit passes would yield diminishing returns (<1% new finding rate).
Total: **92 findings across 29 concern areas, 7 passes.**
