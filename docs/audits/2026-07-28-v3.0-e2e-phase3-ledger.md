# E2E Phase-3 ledger — order permutations + seeded monkey (2026-07-28)

Program: Playwright E2E debugging (branch `v3.0/e2e-debug`). Mode R, live engine,
bundle `865d8e3be2`. Runs: pairwise `3-20260728-1124` / `-1143` / final green
`3-20260728-12xx` (10/10, 19.4 min); monkey split across two invocations by an
engine stop (Dashboard/Pre-Trade/Linkage/History seeds 1955238... series, then
Analytics/Models/Regime/Primitives/Config seeds 319767563-7), 9/9 green,
120 steps/page = 1,080 random safe-actions, breadcrumbed per step for replay.

## App verdict: ZERO order-dependence findings

- **Pairwise**: 10 clusters — config-tabs (full 24-perms), dash-workspace,
  pretrade-form, analytics-equity (full 24-perms), history-filters,
  datalist-tools (each ordering under BOTH reload and remount resets),
  linkage-lists, regime-overview, models-lib, notif-across-nav. Every ordering
  gated by the full collector; round-trip orderings verified against a
  control-state fingerprint. Nothing above INFO.
- **Monkey**: no crash, no console error, no 4xx/5xx, no error boundary, no
  degraded foot, across all 1,080 steps. absent/skip counts are the random walk
  picking tab/data-dependent controls without their preconditions — expected.

The operator's original scenario class ("button 1 then 2, and 2 then 1, every
scenario recorded") is thereby covered at pairwise depth over every declared
shared-state surface, with deterministic replay (seeds + breadcrumbs in
`e2e/ledger/raw/3-*/monkey-*.jsonl`).

## Harness findings (all fixed in-phase, calibration record)

- **P3-001 — live-numerics false divergence (17 findings, run -1124)**: the
  fingerprint compared row CONTENT; mark price / uPnL / funding tick under real
  trading. → structural-only fingerprint.
- **P3-002 — same-document reset leak (2 findings, run -1124)**: resetting via
  same-hash `goto` is same-document navigation — NO remount, React tab/period
  state leaked across orderings (Fills+ALL surfaced under a filter-only
  ordering). → `about:blank` teardown between orderings. App note (not a bug):
  hash-only nav preserves component state by design; a full load resets it.
- **P3-002b — row-count false divergence (10 findings, run -1143)**: row counts
  are still live data (position churn; a table legitimately vanishes at zero
  rows). → fingerprint = pure UI-control state (tabs/periods + per-pane
  search/facet/sort values), pane-keyed, absent panes ignored.
- **P3-003 — monkey seed was per-process**: `Date.now()` in test titles differs
  between Playwright's runner and worker ("Test not found in the worker") →
  seed derives from RUN_ID (env-inherited, stable), `E2E_SEED` overrides.

## Operational observations (not app findings, tracked)

- **OBS-001 — two silent engine stops** (log ends mid-normal-operation 11:43 and
  19:41, no traceback in risk_engine.jsonl, process gone). Cause unresolved —
  console window closed / machine sleep suspected; the JSON log would not carry
  a stderr traceback. If it recurs with the console open, capture the console.
- **OBS-002 — OS clock drift root cause found: w32time service STOPPED**
  (manual sync = one-shot; nothing maintains sync; launch-v3.bat's pre-boot
  `w32tm /resync` silently no-ops with the service down — 0x80070426). Fix
  commands given to the operator (auto-start + cloudflare NTP + 900 s poll).

## Disposition

| ID | Class | State |
|---|---|---|
| P3-001/2/2b/3 | harness | FIXED in-phase |
| OBS-001 | operational | WATCH — capture console on recurrence |
| OBS-002 | operational | root-caused; operator to apply service fix |

**Phase-3 gate: PASSED.** Next: Phase 4 (sandbox engine — mutation + degraded-state
sweeps) per the plan.
