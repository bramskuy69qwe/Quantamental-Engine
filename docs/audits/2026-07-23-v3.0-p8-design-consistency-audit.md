# v3.0 P8 — Directive-#7 design-consistency audit (built frontend vs the Meridian reference)

**Date**: 2026-07-23 · **Branch**: `v3.0/ui-plan-audit` @ `3e6bb45` (bundle `c434b727ad`)
**Charge** (plan §5 row P8 / directive #7): re-audit the plan + the Meridian design
reference (`docs/design/meridian_v3/`) against the **BUILT** React frontend
(`frontend/src/`), catching drift introduced during the P0-P7 build: mock devices
that slipped through, panes that diverged from the design, endpoints wired to the
wrong shape, DESIGN.md carve-outs not applied, stale plan claims.

**Method**: 9 parallel read-only agents (direct Agent calls — the Workflow
orchestrator is avoided per the machine-sleep incident), one lens each:

| # | Lens | Scope |
|---|---|---|
| L1 | Dashboard fidelity | dash-tiled.jsx vs design twin |
| L2 | Pre-Trade fidelity | pages-pretrade.jsx vs design pages.jsx (+ the 3 ratified corrections verified POSITIVELY) |
| L3 | Linkage+History fidelity | pages-linkage/-history/link-primitives vs design linkage family |
| L4 | Analytics fidelity | pages-analytics.jsx 11 tabs vs design (+ the 5 settled semantics verified POSITIVELY) |
| L5 | Regime fidelity | pages-regime.jsx vs design twin (+ classifier-copy re-verify) |
| L6 | Models fidelity | 6 pages-models-* vs design 7-file family |
| L7 | Config+chrome | pages-config/app-shell/nav-and-data/notifications/grid-workspace + §9 nav rules + chrome mock inventory |
| L8 | DESIGN.md compliance sweep | hex/border-radius/rgba/typography/primitive-reuse/gutter/foot contract + tokens.css diff |
| L9 | Mocks · JS-drift · plan-truth | final mock sweep + CLAUDE.md old-class grep + plan/HANDOFF claim verification |

**Calibration** (given to every agent): deviations NAMED in the plan's P0-P7
SHIPPED-STATE notes are RATIFIED — only un-named divergence files; ~50%
historical FP rate ⇒ adversarial refute-first; coverage tables mandatory (a bare
"clean" without the table is rejected — the false-clean tell).

---

## Findings ledger

### L2 — Pre-Trade fidelity: **CLEAN** (0 CRIT/HIGH/MED · 1 LOW · 5 NIT)

All THREE ratified rulings POSITIVELY verified: (a) real `/api/state`
halt banner + §1.3 blur/freeze overlay (scrim card is a SIBLING of the
blurred workspace — actually renders); "positions frozen" = **0 occurrences
in the emitted bundle**; localStorage halt authority neutered. (b) the
Task-139/146 countdown machine — 7 states + correct cadences + receipt-
anchored skew-immune tick + Jinja-fragment literal copy. (c) auto-refresh
calcs compute-only (backend skips the publish; client caches/persists/
countdown-targets ONLY manual calcs — the HIGH-1 regression cannot recur).
Full coverage table: all 7 panes + strip + header at parity or ratified
deviation; pane geometry identical; every bound backend field verified to
exist. All compliance dimensions clean.

- **L2-LOW-1 — plan-annotation gap**: 4 file-header deviations missing from
  the plan's P3 named-residues list (restore-renders-cached-result;
  PRICE/TICKS toggle dropped per Task-152; Leverage→Exposure×; orderbook
  polls on ticker entry). Fix: append to the P3 SHIPPED-STATE note
  (doc-only; the deviation-discipline class).
- **L2-NIT-2** — pct-mode StepperInput swap to plain inputs undocumented
  (price-mode swap IS ratified via Task-152; pct-mode has no tick-size
  dependency). Use StepperInput step=0.1 in pct mode or extend the note.
- **L2-NIT-3** — `est_exposure` bound twice in Position Result ("Exposure ×"
  + "Portfolio Exp." — design had distinct values). Drop one row.
- **L2-NIT-4** — per-sector "50.0% cap" meta absent in Correlated Exposure —
  no cap field in the calc payload (no-fabrication held); could ride
  `/api/calculator/context` if the operator wants it visible.
- **L2-NIT-5** — After-Costs fee label binds `form.orderType` not the calc's
  `c.order_type` — transient label-only desync until next auto-refresh.
- **L2-NIT-6** — regime labels render raw snake_case ("RISK_ON_CHOPPY") at 4
  display sites; a display-site `replace(/_/g,' ')` restores design copy
  without reintroducing the ratified-out tone map.

### L3 — Linkage + History fidelity: **DRIFT-FOUND** (0 CRIT/HIGH · 3 MED · 1 LOW · 2 NIT)

Load-bearing mechanisms sound: choke-pointed writes VERIFIED (no direct
link_status write anywhere), all ratified deviations honored, mock purge
complete, drift-units/deadband/price-format conventions independently
verified against the engine. 8 candidates refuted (incl. lpCritRows ×100 —
correct, fraction despite the suffix; DevBadge deadband = engine tolerance;
local-naive date presets = pre-existing Jinja-shared semantics).

- **L3-F1 · MED — hedge-blind SSE uPnL merge** (pages-linkage.jsx:217-230):
  keys `position_update` by SYMBOL only; in HEDGE mode (the operator's
  actual mode) the last leg per symbol stamps its uPnL onto BOTH legs; the
  SSE cadence beats the 5s poll so the wrong value dominates screen time on
  exactly the diverging pairs. Payload carries `side`, rows carry
  `direction` — fix: composite `symbol|side` key. **Same class in
  dash-tiled.jsx:100-108** (P1's merge — the P5 hedge-composite discipline
  never swept the SSE merges).
- **L3-F2 · MED — History closed-positions parity trims + a FALSE header
  claim**: the header claims client-side M·R "per the door contract" but
  M·R is nowhere in the file; Jinja+design's MFE/MAE/M·R/heat-bar/Open/TP/
  SL columns unported though the rows carry the data (SELECT *). Smaller
  same-class: orders Trigger/TIF, fills Order-No./Slippage. Fix: render
  M·R + the heat cell, or correct the header + name the residues (the
  false claim must be fixed either way).
- **L3-F3 · MED — Trade-Events tab lost its substance**: TIME/SYM/TYPE/
  CALC/SRC only; both references render the event detail (design SUMMARY
  col; Jinja detail + expandable payload grid = the FE-HIGH-006 fix); the
  door already parses `_payload` per row — the page consumes only
  `_symbol`. Fix: port the per-type one-liner summary (+optional expand).
- **L3-F4 · LOW** — collected linkage trims, individually defensible,
  collectively UN-NAMED: inbox strip (dir badge/ts/context line), resolver
  (Hold/Closed-at/Later button), Recent Closes (Time/Hold/%), funding
  countdown static between polls + net_next label swap, LINK header dot.
  Fix: name in the file header (+optionally tick the countdown locally).
- **L3-F5 · NIT** — legacy-row drilldown probes ctx with the integer PK
  when `terminal_position_id` is '' → guaranteed 404 degrades the foot;
  skip the leg when empty.
- **L3-F6 · NIT** — history search fires a round-trip per keystroke (no
  debounce).

### L9 — Mocks · JS-drift · plan-truth: **PASS / CLEAN / PASS-with-2-MED**

**C1 mocks**: zero UNNAMED mocks in any production render path (bundle-
verified per segment; `_synthReport`/MOCK_MODELS absent from the bundle).
- **L9-C1-MED — the chrome residual is UNDER-DESCRIBED in the plan**: named
  as "the random-walk residual", but STATIC fabrications ride the same
  chrome (fake REGIME 'chop' badge, OPEN 5/20, EXP 2.93×, DD 0.00%,
  always-green WS/bus/db dots, fake uptime/tasks, hardcoded account picker)
  — arguably worse than jitter. The P8 chrome adapter must replace these +
  the G-O9 footer ornaments too, not just QE_LIVE.
- **L9-C1-LOW** — N_SEED ships while `/notifications/poll` exists unbound
  (= the L7/L8 consensus HIGH, third corroboration).
- **L9-C1-LOW** — dead mock machinery in the bundle: mockSpark, LiveNumber,
  LivePct, QE_POS walk + hooks (idle 1100ms timer on every page),
  MOCK_WATCHLIST, load-time-computed-never-rendered MOCK.ohlc/equity
  (keeps the frozen Date.UTC alive). Delete with the chrome wire.
- **L9-C1-NIT** — NewsTickerBar's never-defined MOCK_REGIME fallback.

**C2 JS-drift**: CLEAN both directions — full queried-selector inventory
(7 callsites, all resolve); NO querySelector/getElementsByClassName
anywhere in frontend/src (the Task-125 incident class is structurally
gone); old Jinja classes (.global-range-btn et al.) = zero hits in
frontend/src + static/v3. NIT: 3 semantic marker classes with no CSS rule
(qe-lockbtn/qe-page-header/qe-spinner) — harmless, noted to prevent
re-flagging.

**C3 plan-truth**: every endpoint/module existence claim TRUE (all G-gap
route decorators verified; manifest/bundle hash matches; ECharts 5.5.1).
- **L9-C3-MED-1 — the plan's P4 note is stale pre-execution text**: ":481
  BACKEND SHIPPED-STATE (frontend pending)" + ":506 Frontend charge (next
  unit)" — but the P4 frontend SHIPPED (`b55d317`). The exact F13 class.
- **L9-C3-MED-2 — G-O3 half-shipped, three inconsistent doc sites**: §2
  G-O3 reads pre-execution; the P1 note is silent on it; §4 still promises
  N_SEED "→ real producers P1"; backend half genuinely landed in P1
  (pinned), UI wire never did. Annotate all three + add N_SEED to the P8
  residual list.
- **L9-C3-LOW** — notifications.jsx:231 comment describes P1 as future.
- **L9-C3-NIT** — HANDOFF ★-block's superseded bundle hash could mislead
  a skimmer.

### L8 — DESIGN.md compliance sweep: mechanical dimensions **CLEAN**; live-binding **DRIFT**

All 8 mechanical dimensions: 0 CRIT/HIGH/MED. **tokens.css is BYTE-IDENTICAL
to the design reference** (header comment only) — zero token drift, zero
class renames (no stale-selector surface from tokens). border-radius: all 5
occurrences inside the §1 carve-out. rgba/color-mix: all ~30 within §2
classes. Typography sampled clean. Foot contract clean (hand-written id/ms
only in the DEV proving ground). Gutter clean.

- **L8-LOW-a — 11-site inline `'#000'` ECharts cluster** (charts.jsx ×2,
  pages-regime ×6, pages-analytics ×3) vs §2's "never inline literals"
  letter; identical value to `QE_ECHARTS_THEME.bg`. One-sweep fix
  (= L4-NIT-5, now with the full site list).
- **L8-LOW-b — WorkspaceBar preset selector hand-rolls `.qe-period` DOM**
  (nav-and-data.jsx:283-287); PeriodSelector is a drop-in.
- **L8-LOW-c — DevBadge hand-styles a `.qe-badge` span** (link-primitives.
  jsx:81) instead of Badge (wants an outline variant).
- **L8-NIT** — six single-pane analytics wrappers hardcode `padding: 4`
  instead of `var(--qe-pane-gap)`; calendar-heat + ANA_LINK_META washes are
  un-enumerated §2 carve-out instances (add to the list); §2's PaneFoot
  citation line drifted (=L7-F4).
- **Beyond-dimension (live-binding doctrine)** — corroborates L7
  independently: the QE_LIVE/MOCK chrome (fabricated REGIME 'chop' badge,
  DD 0.00%, EXP 2.93×, latency walks, uptime, always-green dots, hardcoded
  account select) rendered by 22 TopNavStd/StatusFooter callsites, whose
  OWN in-file comments say "must NOT ship past P1" — an EXPIRED deferral;
  + N_SEED (= L7-F1). Dead-deletable: QE_POS walks, useQeUnrealSum,
  MOCK_WATCHLIST, most MOCK members, the MOCK_REGIME fallback.

**Cross-lens reconciliation (chrome)**: the plan/HANDOFF DO name the chrome
random-walk as a standing follow-up ("a small shared /api/state+SSE adapter
feeding the chrome on every page"), so L7 inventoried it rather than filing;
L8 files it as HIGH because the module's own contract expired at P1. Both
are right: it is NAMED at plan level AND overdue by its own in-file terms.
Disposition: a first-class P8 work item (chrome rebind + strip), not
ambient debt. N_SEED is named NOWHERE as a residual → it is the audit's
consensus HIGH (filed by both L7 and L8).

### L6 — Models fidelity: **CLEAN** (0 CRIT/HIGH/MED · 1 LOW · 4 NIT)

Full coverage table: every design screen/pane/field-group matched or
ratified-deviation; `_synthReport`/MOCK_MODELS/MC_ES_REPORT correctly absent;
all 9 endpoint bindings field-verified; all compliance dimensions clean
(sole border-radius = the toast dot carve-out; sole rgba = the dialog scrim).
7 candidates adversarially refuted (incl. the combined-import bare-dry_run
lane — honored BY DESIGN, the 400 fail-loud applies only to the htmx lane).

- **L6-LOW-1 — delete-failure message rendered in the success-styled green
  toast** (pages-models.jsx:540 routes an error into hardwired green chrome;
  the design's toast only ever received successes). Fix: `flash(msg, tone)`
  + red border/dot on failures.
- **L6-NIT-1** — create-form blank defaults diverge from the design
  (regimeMult false, tp/sl/costs as placeholders not values) — defensible
  mock-strip honesty, but UN-NAMED. One-line plan executed-note.
- **L6-NIT-2** — import preview drops the design's backtest-window line
  (period_start/end already ride the dry-run payload; unrendered).
- **L6-NIT-3** — transient cross-run stale frame on run-selector switch
  (keep-last-good under a tier-1 foot for one RTT; shared-hook behavior,
  record-only).
- **L6-NIT-4** — dead guard in buildBody (`source` always has ≥2 keys).

### L7 — Config + chrome: **DRIFT-FOUND** (0 CRIT · 1 HIGH · 1 MED · 1 LOW · 1 NIT) + the chrome inventory

The Config PAGE itself is CLEAN — every deviation ratified and honored
exactly (DD-enforcement select display-only verified against the writer's
signature — the CRIT class is absent; Add/Delete disabled; preset Apply
confirm-gated FULL; backend contract parity across all 5 endpoints; §5
foots compliant). app-shell/grid-workspace/sse-adapter clean.

- **L7-F1 · HIGH — notification fiction renders bundle-wide.** N_SEED (7
  fabricated events: fills/regime/DD-breach…) seeds the provider at boot →
  every page's bell shows 3 unread FICTIONAL notifications; N_CHANNELS
  still the 6-channel fiction (REGIME/NEWS have no producers). The G-O3
  BACKEND shipped in P1 (`core/notifications.py` `_TYPE_UI` 4-type map +
  `GET /notifications/poll`) but `frontend/src` has ZERO references to the
  endpoint — the wire never landed, and plan §4 marked N_SEED "must NOT
  ship past their phase". Fix: seed `[]`, poll the endpoint (already emits
  the exact `{ch,pri,head,detail,ts}` shape), scope channels to the real 4.
- **L7-F2 · MED — DESIGN.md §9/§10 contradict shipped NAV_ITEMS on
  Primitives** (doc-vs-code: built NAV faithfully matches the DESIGN — both
  carry the Primitives DEV-chip tab — but the ported DESIGN.md claims it
  isn't in the nav and forbids adding it). P8 decision: strip it from
  NAV_ITEMS for production (hash routing keeps it reachable) OR amend §9.
- **L7-F3 · LOW** — DESIGN.md §6 claims layouts persist "namespaced by
  account"; `qe.ws.layout.${id}` has no account component (deferral is
  ratified — the DOC overstates). One-line correction or implement at P8.
- **L7-F4 · NIT** — stale §2 citation (PaneFoot washes moved to
  primitives.jsx:625-628 in the v2 arc).
- **CHROME INVENTORY (line-level, the P8 retirement scope)**: nav-and-data
  MOCK dataset (4 live chrome consumers + 10 DEAD members) · QE_LIVE walk
  (WorkspaceBar LAT/P&L + TopNav WS dot always-green + StatusFooter
  events/ms) · **QE_POS walk + useQePosMark/useQeUnrealSum + MOCK_WATCHLIST
  + qeMockClockFmt = fully DEAD, deletable now** · hardcoded account select
  (not bound to QE_BOOTSTRAP) · WorkspaceBar EXCH/DD hardcodes ·
  StatusFooter tasks/uptime/bus/db hardcodes (NB `/api/system` already
  serves `uptime_s` — unwired) · notifications demo machinery verified
  UNREACHABLE (demo=false) · halt remnants dormant · CTA buttons toast but
  never navigate (becomes real routing with F1).

### L1 — Dashboard fidelity: **DRIFT-FOUND** (0 CRIT · 1 HIGH · 3 MED · 5 LOW · 7 NIT)

The port is mock-free, token-compliant, wire-shape-verified real-data wiring
of the 8-pane grid with exact geometry — but four UN-NAMED content omissions
+ five mechanical/truthfulness LOWs. Zero mock devices in the built page.

- **L1-F1 · HIGH — Monthly daily-PnL bar chart dropped END-TO-END.** Design
  pane 6's right 60% is a 30-bar daily-PnL chart; plan §4:329-331 mandated
  SUBSTITUTION with real data ("→ real daily-PnL array from journal_stats"),
  not deletion. Neither the backend array nor the chart shipped; the wide
  tile renders mostly empty. Fix: `daily_pnl` array in
  `_journal_stats_context` + the design's BarChart right-half; annotate §4.
- **L1-F2 · MED — Macro Signals sparklines omitted; the endpoint ALREADY
  fetches 30d of points per signal and keeps only the last two**
  (routes_regime.py:191-209). Fix: return `series` + re-add the Sparkline
  column (near-zero cost).
- **L1-F3 · MED — Risk Monitor SECTOR readout dropped in the frontend while
  the backend computes + ships `sector_lines` EVERY snapshot**
  (routes_dashboard.py:414-418, 458; zero frontend references). Dead
  per-request compute + missing design element. Fix: render like
  funding_lines, or strip the field with annotation.
- **L1-F4 · MED — "Weekly Loss" gauge dropped (design 4 gauges → built 3)**
  despite `weekly_pnl_pct` + `max_weekly_loss_pct` existing in the snapshot.
  One of the two P&L guardrails has no magnitude readout. Fix: restore.
- **L1-F5 · LOW** — Open Positions AGE column dropped; `entry_ms` shipped
  per row, never read. Derive client-side.
- **L1-F6 · LOW** — Equity-Curve header degraded to a lone "C" + an
  UNCONDITIONAL "streaming" label on a 5s POLL (stays lit while the foot
  flips warn/err — body contradicts foot; fabrication-class). Derive
  O/H/L/Chg from the fetched candles; gate/rename the label.
- **L1-F7 · LOW** — §5 letter violation: Equity-Curve foot omits
  `retrying: true` on an interval-driven fetch. One-word fix.
- **L1-F8 · LOW — `_ptJson` cross-module dependency points BACKWARD in load
  order** (dash-tiled.jsx:244 uses pages-pretrade.jsx's non-window-exported
  top-level const; works only via flat concatenation; the parse-only guard
  can't catch a future break). Fix: window-export `_ptJson` or hoist to
  primitives.jsx (F8 also lets QE_DASH._json — N7's duplicate — collapse).
- **L1-F9 · LOW** — SSE-minimal position rows fabricate MARK/tape "0.00"
  for up to 15s until the next snapshot (siblings dash correctly).
  Null-guard the format.
- **NITs N1-N7**: preset badges (no engine field — no-fabrication held,
  un-named), funding reshape (fine), dead-mock PeriodSelectors dropped
  (fine), "April"→"Available" cell, Engine-Log dot tone static, DD-gauge
  hardcoded ticks vs dynamic cap, QE_DASH._json duplicate of _ptJson.

### L5 — Regime fidelity: **CLEAN** (0 CRIT/HIGH/MED · 1 LOW · 3 NIT)

Decision-tree copy re-verified LANE-BY-LANE against `core/regime_classifier.py`
(the panic reduction, both HY-gated risk-on lanes, full-mode trending, the
neutral floor) — the corrected copy matches the code; the design's wrong
version is absent. Multipliers: server 1.2/1.0/1.0/0.7/0.4 confirmed; NO
hardcoded multiplier anywhere (design's 0.6×/0.25× absent). 6-card signal set
= ALL_SIGNALS; Task-136 {v,n} + Task-124 tri-state present. 12/12 panes
footed post-`3e6bb45`. Full coverage table at parity/ratified-deviation.
Mock/hex/radius/rgba/primitives/backend-wiring dimensions all clean; 5
additional candidates adversarially refuted as FPs.

- **L5-LOW-1 — stale file-header comment**: pages-regime.jsx:18-20 claims
  Reclassify is "no confirm, Jinja parity" but the shipped code confirm-gates
  it (ratified, audit L4). The header is the map future sessions read first —
  the exact rot class the deviations discipline targets. Fix: one-line edit.
- **L5-NIT-2** — backfill progress bar re-implements `.qe-gauge-track/-fill`
  inline (tokens.css provides both incl. the `.err` variant). Swap classes.
- **L5-NIT-3** — signal-card current-value color `--qe-text` vs design's
  `--qe-text-dim`; single-token cosmetic, unlisted.
- **L5-NIT-4** — multi-pipe panes foot only their primary pipe (Macro Signals
  foots covFoot; Thresholds foots thrFoot while rendering mults too) —
  consistent with the house primary-pipe pattern; observation-tier.

### L4 — Analytics fidelity: **CLEAN** (0 CRIT/HIGH/MED · 2 LOW · 3 NIT)

All 5 settled semantics POSITIVELY verified in the build (residual-vs-plan
slippage w/ y=0 reference + `bias_bp` consumed server-side; historical_var
used raw-negative; HEDGE composite `ticker·direction` keys on funding+beta;
est_slippage entry-only pairing incl. the L5 coverage numerator; TTF
order→fill with honest disclosure). FE-MED-018 dim rule set-identical to the
design. 25-row coverage table: no design pane missing, no built pane without
a design ancestor, zero un-ratified semantic drift. Mock/foot/radius/rgba/
primitive/mono/bundle dimensions all CLEAN (34/34 foots post-`3e6bb45`).

- **L4-LOW-1 — `tools={false}` on all 5 analytics DataLists** suppresses the
  design's auto search/sort/filter (costliest on the 500-row Per-Fill Log).
  Refuted as P5 drift — it is a UNIFORM cross-page convention (History,
  Linkage, Config, Regime, Models all do it) — but the convention contradicts
  the design AND DESIGN.md §5's advertised auto-tools, and is UNRECORDED.
  Fix shape: ratify with one DESIGN.md §5 usage-rule line, or lift on the
  Per-Fill Log only.
- **L4-LOW-2 — period ‹ › nav has no future clamp** (`AnaPeriodNav`
  pages-analytics.jsx:150-152): `›` walks into empty future windows
  indefinitely; the SAME page's Calendar tab clamps exactly this (`atCurrent`,
  audit L7). Internally inconsistent. Fix: `disabled={offset >= 0}` + title.
- **L4-NIT-3** — two truthful-but-unlisted label renames ("Est. Broker Fee"→
  "Fees Paid"; "Avg R / R"→"Avg W / L ×") — annotate in the plan note.
- **L4-NIT-4** — execution scatter's fill-type color legend not ported
  (right-slot now carries the y=0 explainer); colors only in tooltip.
- **L4-NIT-5 (fleet-wide)** — inline `'#000'` ECharts tooltip backgroundColor
  ×3 here (+5 more in charts.jsx/pages-regime.jsx) vs §2's no-inline-literal
  ECharts rule; `QE_ECHARTS_THEME.bg` exists. One-sweep cleanup.

---

## Synthesis (all 9 lenses in)

**Headline: the port is structurally faithful.** Five of six pages audit
CLEAN at the material tiers (Pre-Trade, Analytics, Regime, Models, the
Config page). tokens.css is byte-identical to the design. Zero dangling JS
selectors (the Task-125 incident class is structurally gone in React).
Zero unnamed mocks in page render paths. All ratified corrections verified
POSITIVELY present (halt rewrite, countdown machine, classifier copy, the
5 analytics semantics, choke-pointed writes).

**The drift concentrates in three mechanisms:**

| # | Mechanism | Findings | Tier |
|---|---|---|---|
| D1 | **Fabricated chrome + notifications outlived their P1 expiry** — N_SEED fake events render on every page (consensus: L7-HIGH, L8-MED, L9-LOW; `/notifications/poll` shipped in P1, never bound); the QE_LIVE/MOCK chrome (fake REGIME/DD/EXP/uptime/health dots + hardcoded account picker) is plan-named but under-described and expired by its own in-file contract | L7-F1 · L8-beyond · L9-C1-MED | **HIGH** |
| D2 | **Dashboard content drops, un-named, data already available** — Monthly daily-PnL chart (against §4's own map), signal sparklines (endpoint discards a fetched series), sector_lines (computed+shipped, never read), Weekly-Loss gauge | L1-F1 (HIGH) · F2 · F3 · F4 | **HIGH+3 MED** |
| D3 | **History/Linkage substance trims + one real display bug** — hedge-blind SSE uPnL merge (also in dash-tiled); History column parity + FALSE M·R header claim; Trade-Events detail loss | L3-F1 · F2 · F3 | **3 MED** |

**Doc-truth debt (4 MED, all cheap)**: plan P4 note stale pre-execution
text (F13 class); G-O3 half-shipped across 3 inconsistent doc sites;
DESIGN.md §9 contradicts shipped NAV_ITEMS on Primitives (a P8 decision:
strip the tab or amend the doc); DESIGN.md §6 "namespaced by account"
overstates.

**LOW/NIT tail (~18 LOW / ~25 NIT)**: the 11-site `'#000'` ECharts cluster;
`tools={false}` fleet convention unrecorded; period-nav future clamp;
`_ptJson` backward cross-module dependency (+ QE_DASH duplicate); mark-0.00
fabrication on SSE-minimal rows; missing `retrying` on the equity foot;
dead mock machinery deletable now (QE_POS, MOCK_WATCHLIST, LiveNumber/
LivePct, mockSpark); per-lens cosmetic items as filed above.

## Disposition — EXECUTION STATE

**Wave 1 SHIPPED (`37d012e`)** — D1 closed: chrome-live.js QE_CHROME store,
chrome rebound real, NotificationProvider wired to /notifications/poll
(real-4 channels, N_SEED gone), whole mock family deleted. 2 audits
SHIP-WITH-NITS 0 CRIT/HIGH/MED, all folded.

**Wave 2 SHIPPED (the follow-up commit)** — D2+D3 closed: L1-F1 daily-PnL
chart end-to-end (backend `daily_pnl` array via get_daily_equity_series +
the Monthly bar chart) · F2 signal sparklines (backend `series` + the
4-col rows) · F3 sector_lines render · F4 Weekly-Loss gauge · F5 AGE col ·
F6/F7 OHLC header + honest poll label + retrying · F8 _ptJson hoisted to
primitives (QE_DASH duplicate collapsed) · F9 mark '—' guards · N6
proportional DD ticks · L3-F1 hedge-safe symbol|side SSE merges (linkage +
dash, incl. composite row keys/live-ids — the id-collision sibling) ·
L3-F2 M·R + HistHeat + the FALSE header claim corrected · L3-F3
Trade-Events SUMMARY (Jinja one-liners ported exact) · L4-LOW-2 period
future-clamp · L6-LOW-1 err-tone toast · L2-NIT-6 label de-snaking (5
sites). 3 backend pins (tests/test_p8_wave2.py). 2 audits: #1 caught a
REAL HIGH pre-commit — `entry_ms` is a MISNAMED ISO-string field (the P1
test stub had baked a numeric type), the new AGE column would have
rendered NaNm on every live row; fixed parse-tolerant + '—' guard. #1's
F2 (M·R ∞-on-null fabricating certainty) also folded ('—' on unknown, ∞
only on measured-zero). #2 render-executed the bundle: 131/131 assertions
incl. the hedge-pair no-cross-stamp proof on the compiled handler and an
exact-match _hEvtSummary table vs the Jinja twin. Residues recorded:
sideKey duplicated (linkage-local + QE_DASH-scoped — export candidates);
DD-gauge ticks decorative (not the 0.80/0.95 state thresholds); the P1
test stub's numeric entry_timestamp + the `entry_ms` MISNOMER are backend
cleanup candidates; EquityHero locale-mixing observation (pre-existing,
P1-era).

**Remaining**: the doc wave (§3 below) + the operator decision points (§4)
+ acceptance-gated retirement.

## Disposition (original proposal — kept for the record)

1. **P8 fix wave 1 (the fabrication mechanism, D1)**: bind the chrome to
   real `/api/state`+SSE via a small shared adapter (the named follow-up),
   wire NotificationProvider to `/notifications/poll`, scope channels to
   the real 4, strip N_SEED/QE_LIVE/QE_POS/MOCK/MOCK_WATCHLIST + dead
   machinery. This is also the retirement precondition — promoting `/v3`
   → `/` with fabricated chrome would ship fiction to the default surface.
2. **P8 fix wave 2 (D2+D3 code)**: Dashboard restores (daily-PnL array+
   chart, sparkline series, sector_lines render, weekly gauge, AGE col,
   OHLC header + honest label); hedge-safe SSE merges (linkage + dash);
   History M·R/heat + Trade-Events summary; the LOW tail worth taking
   (future clamp, _ptJson export, mark null-guard, retrying flag, toast
   tone, delete-failure tone).
3. **Doc wave (same commit as its code or standalone)**: the 4 doc MEDs +
   named-residue backfills (L2-LOW-1's four items, L6-NIT-1, L4-NIT-3) +
   stale citations.
4. **Retirement decision points for the operator**: Primitives in
   production nav (strip vs amend §9); `tools={false}` convention (ratify
   in DESIGN.md §5 vs lift on large tables); account-namespacing of
   workspace layouts (implement vs correct the doc).
