# v2.7 — Model Library (Implementation Plan)

**Status:** design rev 2 — pending execution.
**Created:** 2026-06-24. **Rev 2:** 2026-07-16 — 6-agent plan audit folded (24 findings: §2
re-verified at HEAD `e05e359`; attribution seam redesigned around `core/position_identity.py`;
Phase 6 retirement bundle added; §6 open items resolved).
**Branch:** dedicated `v2.7/model-library`, forked off **`e05e359`** (the v2.6 tip — land
**after** v2.6, see [v2.6_remove_quantower_plugin_plan.md](v2.6_remove_quantower_plugin_plan.md)).
Do **NOT** fork off `main`: it is 258 commits behind (a strict ancestor, 0 unique commits —
fast-forwarding main first is fine, but the fork base is the v2.6 tip either way).
**Scope:** a DB-backed library of reusable, exchange-agnostic *models*, plus a per-app adapter
framework for importing 3rd-party backtest results. **Minimal UI now**; full UX in the v3 redesign.

---

## 1. Context

A **model** is a reusable, exchange-agnostic trading definition the operator stores once and uses
anywhere. It carries:

1. **Identity / metadata** — name, type (`macro` / `micro` / `both`), description.
2. **Risk / sizing preset** — the calculator coefficients that don't change per-trade
   (risk %, sizing rule, window, TP/SL methodology, regime-multiplier toggle, size override).
3. **Strategy definition** — freeform notes + an opaque JSON config blob (entry/exit logic,
   target universe, regime config). Engine treats it as documentation; no execution semantics.
4. **Imported 3rd-party backtest runs** — performance results imported from external apps
   (MultiCharts first), 1 model : N runs, each with summary + trade list + equity curve.

**How a model is used** (operator decisions):
- **Pre-fill the calculator** — pick a model → its risk-preset loads into the risk calculator for
  any symbol/exchange.
- **Tag trades / linkage** — a plan records which model it came from; the model name surfaces in
  position history via the existing calc-linkage chain.
- **Reference catalog** — browse models + their imported backtest performance.

**What the engine does NOT do** (operator decision): it does **not** run backtests. Backtesting
is delegated to external apps; their results are **imported** (file upload, one adapter per app).
Import is **one-directional** (no export/round-trip to the external app). Models stay
**exchange-agnostic** — no symbol/exchange columns; exchange context is the active account at
calculator-use time.

---

## 2. Foundation already present (reuse — do not reinvent)

Verified at HEAD `1fe4178`; **re-verified at HEAD `e05e359` (2026-07-16 audit) — every item below
still holds, line-refs exact.** (Line-refs still drift; re-grep at implementation time.)

- **`potential_models`** table (`core/database.py:307`) + **`ModelsMixin`** (`core/db_models.py`):
  `create_potential_model` / `list_potential_models` / `update_potential_model` /
  `delete_potential_model` + the `/api/models` JSON CRUD (`api/routes_models.py`, registered in
  `api/router.py`). Columns: `id, created_at, name, type, description, config_json`.
- **`backtest_sessions` / `backtest_trades` / `backtest_equity`** (`core/database.py:269–305`) +
  **`BacktestMixin`** (`core/db_backtest.py`): `create_backtest_session`,
  `finish_backtest_session`, `insert_backtest_trades`, `insert_backtest_equity`. The two child
  tables are `ON DELETE CASCADE` on `session_id`. `backtest_sessions.summary_json` already stores
  per-run summary stats. **This is exactly the per-run storage the import needs.**
- **Calc seam:** `pre_trade_log.model_name` / `model_desc` are free-text today
  (`core/database.py:89–90`), flowing calc-form → `run_risk_calculator(model_name=…)`
  (`core/risk_engine.py:437`) → `insert_pre_trade_log` (`core/db_trades.py:103`).
  `closed_positions.model_name` (`:448`) exists too. This is the FK opportunity for tagging.
- **Adapter pattern to mirror:** `core/adapters/{registry,protocols,base}.py`.
- **UI pattern to mirror:** `templates/config.html` (two-column list+detail) +
  `templates/fragments/account_list.html` + `account_config.html` + `api/routes_accounts.py`
  fragment routes; primitives in `templates/primitives/` (NB `card` is a call/caller-slot macro,
  `empty_state` must not render inside a `<tbody>` — see `primitives/README.md`).
- **Already-existing Models UI (retired in Phase 6):** `templates/backtest.html` ships a
  client-side Models sub-tab (tab `:16`, panel `:169–244`, CRUD JS `:406–492` against
  `/api/models`). The Phase-4 library page supersedes it; Phase 6 removes it together with
  qt-import. Until then two editors coexist — the old form knows nothing of the new JSON columns.
- **Attribution owner (post-reconciler — this plan predates it):** junction/lifecycle writes live
  in `core/position_identity.py`, NOT "the matcher". The primary-calc selection rule is
  `PositionIdentity.position_primary_calc` / `_most_contributing_calc_id`
  (`core/position_identity.py:62–98, 423–480`): per-calc **summed** `contributed_qty`,
  earliest-fill tie-break, **unsealed rows preferred** (`sealed_ts IS NULL` — a venue-reused tpid
  must not weld the closed trade's model onto the live one). Any position→model resolution MUST
  route through this rule (tasks 1.7 / 5.3 / 5.4) — a naive `positions_calcs` join returns
  multiple/arbitrary rows on scale-ins and mis-attributes on reused tpids.

### Storage decision (operator deferred to me): EXTEND `potential_models`, REUSE `backtest_*`
Avoids a duplicate "model" concept and a duplicated 3-table + mixin + renderer stack. The
MultiCharts adapter's normalized output maps **directly** onto the existing `backtest_trades` /
`backtest_equity` columns, so the existing insert helpers are reused unchanged.

Schema additions (idempotent `ALTER TABLE ... ADD COLUMN` in `database.initialize()`, using the
house **duplicate-column check-and-reraise** idiom — catch `sqlite3.OperationalError`, pass ONLY
when `"duplicate column name"` is in the message, log + re-raise anything else; see
`core/database.py:857–865`. NOT a blanket except):
```
ALTER TABLE potential_models  ADD COLUMN risk_preset_json TEXT NOT NULL DEFAULT '{}'
ALTER TABLE potential_models  ADD COLUMN strategy_json    TEXT NOT NULL DEFAULT '{}'
ALTER TABLE potential_models  ADD COLUMN updated_at       TEXT
ALTER TABLE backtest_sessions ADD COLUMN model_id         INTEGER DEFAULT NULL
ALTER TABLE backtest_sessions ADD COLUMN source_app       TEXT NOT NULL DEFAULT ''
ALTER TABLE pre_trade_log     ADD COLUMN model_id         INTEGER DEFAULT NULL
ALTER TABLE closed_positions  ADD COLUMN model_id         INTEGER DEFAULT NULL
CREATE INDEX IF NOT EXISTS idx_bt_sessions_model ON backtest_sessions(model_id)
```
- `risk_preset_json` — `{risk_pct, sizing_rule, window_seconds, tp_methodology, sl_methodology,
  apply_regime_multiplier, size_override_default}` (calculator pre-fill reads known keys).
- `strategy_json` — `{entry_logic, exit_logic, target_universe, regime_config, notes}`.
  (`config_json` stays for back-compat.)
- `updated_at` — **nullable TEXT in BOTH the CREATE statement and the ALTER** (identical shapes;
  SQLite forbids non-constant defaults in `ADD COLUMN`, so the house
  `NOT NULL DEFAULT (datetime('now'))` shape is unreachable here). Stamped explicitly with
  `datetime('now')` in `update_potential_model`; legacy rows stay NULL.
- **Imported runs (1 model : N): reuse `backtest_sessions`** with nullable `model_id` +
  `source_app`. Imported runs set `model_id`; existing engine backtests + `qt-import` leave it
  NULL. Trades + equity reuse `backtest_trades` / `backtest_equity` unchanged; summary stats live
  in `summary_json`. Dedicated stat columns are deferred to v3 (only needed if cross-run SQL
  analytics is wanted — open item #4, confirmed).
- **No double-surface:** `list_backtest_sessions` gains `WHERE model_id IS NULL` (task 1.8) so
  imported model runs render ONLY in the model library, never in the engine's Backtest tab —
  whose templates read summary keys imported runs don't carry (`total_return_pct`,
  `r_stats.*`, `sortino`) and treat `max_drawdown` as a fraction (an abs-$ value would render
  as e.g. "500000.00%").
- **Calc FK:** `pre_trade_log.model_id` is the FK the free-text `model_name` becomes; the
  free-text columns stay (a model need not be selected). `closed_positions.model_id` is its
  close-row twin, stamped from the PRIMARY calc at close (task 5.4).
- **FK policy — dangling by design:** NO `REFERENCES` clause on any `model_id` column. A real FK
  is impossible for `pre_trade_log`/`closed_positions` anyway (per-account DB files vs
  `potential_models` in `global.db` — SQLite FKs can't span files, and per-account files hold an
  empty shadow `potential_models`). After a model delete, ids dangle permanently; **every read
  path is a LEFT JOIN with a "(deleted model)" fallback.** Phase 1.6's cascade assumption is
  TRUE on the main connection (`PRAGMA foreign_keys=ON`, `core/database.py:633`; precedent
  `db_backtest.py:64–67`) — but raw `sqlite3.connect()` operator scripts touching
  `backtest_sessions` must set the pragma themselves or they silently orphan trades/equity.
- **Dual-track migrations (house precedent, migration 011):** the inline ALTERs only reach the DB
  the singleton opens (`data/risk_engine.db` — the live data today). Also ship a numbered `.sql`
  pair for the split-DB runner: global scope (`potential_models` + `backtest_sessions` columns)
  and per_account scope (`pre_trade_log.model_id` only — `closed_positions` lives only in the
  legacy DB, 008 precedent), so `global.db`/`per_account/*.db` don't schema-drift (task 1.9).

---

## 3. Phasing rationale

Schema/DB first (no behavior change), then the parser, then API → UI → calculator wiring. Critical
path is **Phase 1 → 3 → 4**. Phase 2 (adapter) depends only on Phase 1's storage wrappers and can
proceed in parallel. Phase 5 (calculator pre-fill + tagging) depends on the `model_id` FK (Phase 1)
and the prefill route (Phase 3). Phase 6 (retire the superseded backtest.html surfaces) runs
**LAST** — remove a surface only with/after its replacement is live (v2.6 precedent).

```
Phase 1: Schema + DB layer (extend potential_models, reuse backtest_*, model_id FKs)
  ├─> Phase 2: 3rd-party backtest adapter framework + MultiCharts (.xml + .xlsx)
  │
  └─> Phase 3: REST API (model CRUD + backtest import + prefill)   [needs 1 + 2]
        └─> Phase 4: Minimal UI (library page + fragments)
        └─> Phase 5: Calculator pre-fill + linkage tagging          [needs 1 + 3]
              └─> Phase 6: Retire qt-import + old Models sub-tab    [needs 4 + 5 LIVE]
```

Effort tiers: **S** (≤1 day), **M** (2–4 days), **L** (>4 days).

---

## Phase 1 — Schema + DB layer (no behavior change)

**Goal:** all new columns exist; DB helpers in place; `/api/models` still works.
**Dependencies:** none. **Effort:** M.

### Tasks
| # | Task | File(s) |
|---|---|---|
| 1.1 | Add `risk_preset_json`, `strategy_json`, `updated_at` to `potential_models` via idempotent `ALTER TABLE` (duplicate-column check-and-reraise idiom, `database.py:857–865`; `updated_at` nullable TEXT in BOTH CREATE and ALTER — see §2; stamp `datetime('now')` in `update_potential_model`). | `core/database.py`, `core/db_models.py` |
| 1.2 | Add `model_id` + `source_app` to `backtest_sessions`; add index `idx_bt_sessions_model` (in the post-ALTER index block, `database.py:867–906`). | `core/database.py` |
| 1.3 | Add `model_id` to `pre_trade_log` (the FK the free-text `model_name` becomes) AND to `closed_positions` (the close-stamp target for 5.4). | `core/database.py` |
| 1.4 | Extend `ModelsMixin` create/update to accept `risk_preset`/`strategy` dicts (JSON-encode; set `updated_at`); extend get/list to decode the new JSON columns (mirror the `config_json → config` pattern). Keep `config` for back-compat. | `core/db_models.py` |
| 1.5 | Thin wrappers over `BacktestMixin`: `create_model_backtest(model_id, source_app, normalized)` (create_backtest_session + insert_backtest_trades + insert_backtest_equity + finish_backtest_session in one path) and `list_model_backtests(model_id)`. | `core/db_models.py` (uses `core/db_backtest.py`) |
| 1.6 | `delete_potential_model` also runs `DELETE FROM backtest_sessions WHERE model_id=?` (CASCADE clears trades + equity — holds: `PRAGMA foreign_keys=ON` on the main conn, `database.py:633`; precedent `db_backtest.py:64–67`). `pre_trade_log`/`closed_positions.model_id` then dangle BY DESIGN — reads are LEFT JOIN + "(deleted model)" fallback (§2 FK policy). | `core/db_models.py` |
| 1.7 | `get_model_for_calc(calc_id)` — plain LEFT-JOIN lookup `pre_trade_log.model_id → potential_models`, returning `(model_id, name)` or the "(deleted model)" fallback. Callers resolve WHICH calc via the existing `PositionIdentity.position_primary_calc` (unsealed-first, summed-contribution rule — §2 attribution owner). Do NOT reimplement the selection rule in the DB mixin, and do NOT ship a naive `positions_calcs` join (mis-attributes on scale-ins / reused tpids). | `core/db_models.py` |
| 1.8 | `list_backtest_sessions` gains `WHERE model_id IS NULL` — imported model runs must not double-surface in the engine's Backtest tab (§2 no-double-surface). + regression test. | `core/db_backtest.py` |
| 1.9 | Dual-track migration: numbered `.sql` pair mirroring migration 011 — global scope (`potential_models` + `backtest_sessions`), per_account scope (**`pre_trade_log.model_id` only** — `closed_positions` lives only in the legacy DB, so its column is inline-ALTER-only, per the 008 placeholder precedent). | `core/migrations/` |

### Tests
- Migration idempotency: boot twice on an existing DB; assert new columns via `PRAGMA table_info`
  (mirror `tests/test_indexes_and_calc_id_high003_crit008.py:77–93`; column-shape assertions per
  `tests/test_task157_pretrade_regime_columns.py`). If 1.9's `.sql` pair ships, mirror
  `tests/test_migration_runner.py` (apply twice, `migrations_log` skip).
- Model round-trips with `risk_preset` + `strategy` decoded; `create_model_backtest` →
  `list_model_backtests`; existing engine backtest sessions have `model_id IS NULL`.
- 1.8 filter: an imported (model_id-set) session is invisible to `list_backtest_sessions`, visible
  to `list_model_backtests`.
- `get_model_for_calc`: hit, miss, and dangling-model_id ("(deleted model)" fallback) cases.

### Acceptance criteria
- All migrations run idempotently; existing tests pass; `/api/models` JSON CRUD still works.

---

## Phase 2 — 3rd-party backtest adapter framework + MultiCharts

**Goal:** a pluggable per-app importer; a MultiCharts reference adapter for **both `.xml` and
`.xlsx`** producing a normalized result that drops into the Phase-1 wrappers.
**Dependencies:** Phase 1 (for the insert wrappers; parsing itself is standalone). **Effort:** M.

### Tasks
| # | Task | File(s) |
|---|---|---|
| 2.1 | `protocols.py` — dataclasses field-compatible with the `backtest_*` columns so `db.insert_backtest_trades`/`insert_backtest_equity` accept `asdict()` (verified exact vs schema 2026-07-16): `BacktestTrade{symbol, side, entry_dt, exit_dt, entry_price, exit_price, size_usdt, r_multiple, pnl_usdt, regime_label, exit_reason}`, `EquityPoint{dt, equity, drawdown}`, `BacktestSummary{net_profit, win_rate, profit_factor, max_drawdown, max_drawdown_pct, sharpe, total_trades, period_start, period_end}`, `NormalizedBacktestResult{summary, trades, equity_curve, source_app, session_name}`. **Units:** `max_drawdown` = absolute $, `max_drawdown_pct` = fraction 0–1 (per §6-#3); `win_rate` = fraction. `BacktestSummary` is consumed ONLY by the model-library fragments (imported runs are filtered out of the Backtest tab, task 1.8) — no `r_stats` nesting mimicry (MC has no R-multiples). NB `insert_backtest_equity` is non-tolerant (direct subscript, KeyError on missing keys) — dataclass fields with defaults guarantee the keys by construction; keep `EquityPoint` a dataclass, don't "optimize" it into a raw dict. | `core/backtest_adapters/protocols.py` (new) |
| 2.2 | `base.py` — `BacktestAdapter` protocol: `app_id`, `display_name`, `accepted_extensions`, `parse(file_bytes, filename) -> NormalizedBacktestResult`. Tolerant by contract — default missing fields; raise only when the file is unrecognizable for the adapter. | `core/backtest_adapters/base.py` (new) |
| 2.3 | `registry.py` — `@register_backtest_adapter("multicharts")` + `get_backtest_adapter(app_id)` + `list_backtest_adapters()` (drives the upload dropdown). Mirrors `core/adapters/registry.py`. | `core/backtest_adapters/registry.py` (new) |
| 2.4 | `multicharts.py` — MultiCharts **Strategy Performance Report** adapter. **Sniff by magic bytes** (`PK` prefix = OOXML → `openpyxl`; verified that MC's `.xml` export is a zipped `.xlsx`, 8 sheets); keep an `xml.etree.ElementTree` fallback only for a possible true flat-XML variant. Read the three data sheets **by name** — `Strategy Analysis` (summary), `List of Trades` (trades), `Settings` (metadata); the `* Graphs` sheets are images. Pair the **two-rows-per-trade** entry/exit; convert **Excel-serial dates** (epoch 1899-12-30); compute `profit_factor = gross_profit/abs(gross_loss)` (MC reports it signed); build the equity curve from the trade list's `Cum. Profit ($)` + `Drawdown ($)`. **Full mapping in the § below.** | `core/backtest_adapters/multicharts.py` (new) |
| 2.5 | `__init__.py` imports `multicharts` so the `@register_backtest_adapter` decorator fires at import time. | `core/backtest_adapters/__init__.py` (new) |
| 2.6 | ~~Add `openpyxl`~~ **Already shipped** (`requirements.txt:10`, `openpyxl>=3.1.2`; live use: `core/data_logger.py` XLSX export). Verify version only — NO new dependency; the adapter is merely the first .xlsx *reader*. | — |
| 2.7 | Commit a **truncated synthetic fixture** (~20 trades, all 8 sheets' structure) derived from the real `@ES - 1 Minute` sample, under `tests/fixtures/multicharts/` — for both the `.xlsx` and (if the flat-XML variant materializes) `.xml` paths. Do NOT commit the 1.17 MB original (it lives outside the repo). | `tests/fixtures/multicharts/` (new) |

### Tests
- `tests/test_backtest_adapters_multicharts.py`: the task-2.7 fixture
  (`tests/fixtures/multicharts/`) for **both `.xml` and `.xlsx`** → assert summary/trades/equity;
  tolerance test (renamed/reordered/missing columns don't crash); negative test (unrecognizable
  file → raises). Registry resolution test (mirrors `core/adapters/registry` coverage).

### Acceptance criteria
- Both formats parse to a `NormalizedBacktestResult` whose `trades`/`equity_curve` insert via the
  Phase-1 wrappers unchanged (asdict keys == table columns).

### MultiCharts Strategy Performance Report — verified layout (sample: `@ES - 1 Minute`, 2026-06-24)
The export is an **OOXML workbook** (8 sheets, 273 shared strings). Detect by `PK` magic bytes →
`openpyxl`. Only three sheets carry the data the adapter needs:

| Sheet | Use |
|---|---|
| `Strategy Analysis` | summary stats (key in col A; **col B = All Trades**, C = Long, D = Short) |
| `List of Trades` | trade-by-trade list (header on **row 3** — title row, blank row, header; the adapter finds it by scanning, not hardcoding) |
| `Settings` | symbol, point value, compression, start/end date, initial capital + strategy params |
| `Trade Analysis` / `Periodical Analysis` | extra aggregates — optional |
| `Strategy/Trade/Periodical Analysis Graphs` (3) | chart images only — **no cell data** |

**Summary** (`Strategy Analysis`, col-A label → col-B value): `Net Profit` → net_profit;
`Gross Profit`/`Gross Loss` → gross; `Total # of Trades` → total_trades; `% Profitable` → win_rate
(fraction 0–1); `Max Strategy Drawdown` → max_drawdown **(absolute $)** + `Max Strategy Drawdown (%)`
→ max_drawdown_pct (fraction); `(Annualized) Sharpe Ratio` → sharpe (often `0` — MC didn't compute).
**Profit Factor gotcha:** MC reports it **signed-negative** (gross_profit ÷ *negative* gross_loss) —
compute `gross_profit/abs(gross_loss)`, don't trust the cell sign.

**Trade list** (`List of Trades`, header row 3 — corrected at Phase-2 implementation; row 2 is blank):
`Trade # | Order # | Type | Signal | Date | Time | Price | Contracts | Profit ($) | Profit (%) | Cum. Profit ($) | Cum. Profit (%) | Run-up ($) | Run-up (%) | Drawdown ($) | Drawdown (%)`
- **Two rows per trade**: Entry row (`Type`=`EntryLong`/`EntryShort`; carries `Trade #` + the trade's
  `Profit ($)`/`Cum. Profit`/`Run-up`/`Drawdown`) then Exit row (`Type`=`ExitLong`/`ExitShort`; carries
  `Order #` + exit `Price`; profit cols blank). **Pair entry+exit** into one `BacktestTrade`.
- `side` ← entry `Type`; `entry_price`/`exit_price` ← the two `Price` cells; `pnl_usdt` ← entry
  `Profit ($)`; `exit_reason` ← exit `Signal` (`L_TP`→take_profit, `S_SL`→stop_loss, …);
  `r_multiple` not in the report (leave 0); `regime_label` n/a (MC has no regime).
- `size_usdt` ← **notional = Price × Contracts × Point Value** (Point Value from the `Settings`
  sheet, e.g. $50 for @ES). **DECIDED 2026-07-16 (was open item 3a)** — dollar notional matches the
  column's semantics; raw contracts would be meaningless next to engine-run rows.
- **Excel-serial dates** (epoch 1899-12-30); `Date` and `Time` hold the **same** datetime serial — use
  either, convert to ISO for `entry_dt`/`exit_dt`.

**Equity curve:** no dedicated equity *data* sheet (the `* Graphs` are images). Build it from the trade
list — per closed trade: `dt = exit_dt`, `equity = InitialCapital + Cum. Profit ($)`,
`drawdown = Drawdown ($)`.

**Settings sheet (bonus):** `Symbol Name` (@ES), `Point Value` ($50), `Compression` (1 Minute),
`Start/End Date`, `Initial Capital`, plus strategy params (`RiskPctOfEquity=1.0`, `RewardRiskMult=1.5`,
`ORMinutes=30`, …). These can optionally **seed** the model's `risk_preset_json` / `strategy_json` on
first import (open item #3a).

---

## Phase 3 — REST API (model CRUD + backtest import + prefill)

**Goal:** extend `/api/models` with the new fields + import + UI-serving fragment routes.
**Dependencies:** Phases 1, 2. **Effort:** S–M.

### Tasks
| # | Task | File(s) |
|---|---|---|
| 3.1 | Existing `/api/models` JSON CRUD gains the new fields (defaults keep it back-compat). Fold in: **PUT/update gains the same `macro/micro/both` membership validation POST already has** (pre-existing hole — update currently persists arbitrary `type` values). | `api/routes_models.py` |
| 3.2 | Fragment routes `/fragments/models/list`, `/fragments/models/detail/{id}`, `/fragments/models/form[/{id}]`, `/fragments/models/backtests/{id}` (**slash-namespaced** — the dominant new-tab convention, cf. `/fragments/backtest/*`; template files keep underscore names per the `account-list`→`account_list.html` precedent). Co-locating page + fragments + JSON in one route file mirrors `routes_backtest.py`. **Executed deviation (P3):** the `GET /models` PAGE route moved to task 4.1 — its template (`model_library.html`) is a Phase-4 artifact and the route would 500 without it; conversely the five fragment TEMPLATES (4.2–4.5) shipped in Phase 3 with their routes (a fragment route can't ship template-less — the v2.6 pull-dependencies-into-phase precedent). | `api/routes_models.py` |
| 3.3 | `POST /models` (Form) create; `POST /models/{id}/update` (Form) edit; `DELETE /models/{id}` — each re-renders the relevant fragment. | `api/routes_models.py` |
| 3.4 | `POST /models/{id}/backtest-upload` (`UploadFile` + `app_id` Form): bytes → `get_backtest_adapter(app_id).parse(...)` → `create_model_backtest` → re-render the backtest-list fragment. On parse error return a **200 error-styled fragment** (htmx swallows non-2xx bodies; canonical precedent `routes_config.py:save_account_config` — note this deliberately bypasses the global `htmx:responseError` toast surface). **This is the codebase's FIRST multipart route** (qt-import is JSON — nothing to mirror); `python-multipart` already shipped (`requirements.txt:4`). Reject files > 20 MB before parsing (the upload lands in memory). Catch the base `ValueError`, not just `BacktestAdapterError` — an unknown `app_id` raises plain `ValueError` from the registry, and both must render the error fragment rather than 500 (P2 audit note). | `api/routes_models.py` |
| 3.5 | `GET /calculator/prefill/{id}` — returns the model's `risk_preset` for the calc form. | `api/routes_models.py` |

### Tests
- Route round-trips (create/update/delete re-render the right fragment); import route happy-path +
  parse-error fragment; prefill returns the known risk-preset keys.
- **Mechanism (mandatory — LOW-023):** GET smokes = append the new page/fragment paths to
  `tests/test_routes.py`'s parametrized `PAGE_ROUTES`/`FRAGMENT_ROUTES` lists (reuse the single
  module-scoped TestClient). POST/DELETE round-trips = call the async handlers **directly** (or via
  that same client) — NEVER construct a second `TestClient(main.app)`; it hangs the suite (only
  one in-process lifespan per pytest process, owned by `test_routes.py`).

### Acceptance criteria
- CRUD + import functional via API; the existing JSON `/api/models` contract is intact.

---

## Phase 4 — Minimal UI (library page; styling deferred to v3)

**Goal:** a bare list / create / edit / delete + import UI mirroring `config.html`.
**Dependencies:** Phase 3. **Effort:** M.

### Tasks
| # | Task | File(s) |
|---|---|---|
| 4.1 | Two-column list+detail page; nav entry in `base.html` `nav_items` **AND `page_meta`** (`base.html:563–573` — without it the title bar won't render); the `GET /models` handler passes `active_page="models"` through `_ctx` (mirror `routes_backtest.py:68`). | `templates/model_library.html` (new), `templates/base.html` |
| 4.2 | List fragment — loop + "+ New Model"; use `primitives/card` + `primitives/empty_state`. | `templates/fragments/model_list.html` (new) |
| 4.3 | Detail fragment — metadata + risk-preset + strategy; "Edit" + "Load into Calculator" actions; a backtest section embedding 4.5. | `templates/fragments/model_detail.html` (new) |
| 4.4 | Form fragment — name, type (`macro`/`micro`/`both`), description; risk-preset fields; strategy textareas. Mirror `account_config.html` grid. | `templates/fragments/model_form.html` (new) |
| 4.5 | Backtest list + upload fragments — run list (name, source_app, net profit, win rate, # trades, period) + a multipart upload form (file input + `app_id` `<select>` from `list_backtest_adapters()`). | `templates/fragments/model_backtest_list.html` + `model_backtest_upload.html` (new) |

### Tests
- Compile-render every new template/fragment via `jinja2.Environment` (template-wiring discipline —
  catches Jinja syntax / nested-comment bugs).

### Acceptance criteria
- Operator can create / edit / delete a model and import a backtest entirely from the UI.

---

## Phase 5 — Calculator pre-fill + linkage tagging

**Goal:** a model pre-fills the calculator and rides the existing linkage chain for attribution.
**Dependencies:** Phases 1, 3. **Effort:** S–M.

### Tasks
| # | Task | File(s) |
|---|---|---|
| 5.1 | Add a model picker + `model_id` as a **regular persistent form field** to the calculator; selecting a model HTMX-GETs `risk_preset` (3.5) and fills the form (symbol / avg / side stay operator-entered). Prefill must set the **VISIBLE regime-multiplier toggle state**, not just its hidden input (`calculator.html:62`) — or the UI and the submitted value diverge. Because `model_id` is a form field it re-rides every recalc automatically, so a **superseding** calc keeps its model (supersede inserts a fresh `pre_trade_log` row from the new submit; nothing clones fields server-side) — pin with a test. | `templates/calculator.html` |
| 5.2 | `POST /calculator/calculate` accepts `model_id: str = Form("")` and **POST-ATTACHES** it (`calc["model_id"] = …`) after `run_risk_calculator` returns — the existing `tp_levels`/`link_window_seconds_override` idiom (`routes_calculator.py:244–250`). Do **NOT** extend `run_risk_calculator`'s signature (unnecessary). The REAL persistence edit: `insert_pre_trade_log` is an **explicit column-list INSERT** (`db_trades.py:121–252`) — add `model_id` to the column list + `:model_id` placeholder + values dict, or it is **silently dropped** (dict-passthrough only holds at the `handlers.py:543` boundary). Do NOT change the pinned corr-log `db_write` tap payload (a payload change needs a spec erratum; untouched = none needed). Optional: forward `model_id` on the `calc:created` event (`handlers.py:612–620`, which already forwards `model_name`). | `api/routes_calculator.py`, `core/db_trades.py` |
| 5.3 | Surface the model name on a position: resolve the **primary calc** via `PositionIdentity.position_primary_calc` (unsealed-first, summed-contribution — §2 attribution owner), then `get_model_for_calc` (1.7). `model_id` rides the existing chain unchanged (owner = `core/position_identity.py`, post-reconciler; no new junction) — verified safe 2026-07-16: nothing rewrites `pre_trade_log.model_id`, and the R2 re-key pass moves `position_id` only, never `calc_id`, so the join key is stable across reconciliation. | `core/db_models.py`, history/position templates |
| 5.4 | **Stamp the model at CLOSE from the primary calc** (closes the two-sources-of-truth split): the close-row builder already computes `primary_calc_id` (`order_manager.py:2773`) — resolve `(model_id, model_name)` from that calc's `pre_trade_log` row and write BOTH onto the close row (`closed_positions.model_id` from 1.3 + the existing `model_name` text). Fall back to the current `get_pre_trade_for_shortfall` symbol+300s-window heuristic ONLY when no primary calc exists (it is model-blind and can mis-attribute — the in-code "T234 known limit"). Fix the fills-rebuild path identically: `position_grouping.py:396` hard-codes `model_name=""` while holding `calc_id`. History templates keep reading `model_name` unchanged — a picker-only trade is no longer invisible. | `core/order_manager.py`, `core/position_grouping.py`, `core/db_orders.py` |

### Tests
- Prefill fragment fills the form (incl. the visible regime-toggle state); `calculate` persists
  `model_id` (assert the actual `pre_trade_log` row — this catches the column-list drop);
  primary-calc → `get_model_for_calc` resolves the model via the junction rule.
- 5.4 close-stamp: model-tagged trade → close row carries `model_id` + `model_name` from the
  PRIMARY calc (multi-calc scale-in scenario: largest summed contribution wins, not the
  shortfall-window heuristic); no-calc fallback still works; rebuild path re-stamps.
- Picker persistence: recalc/supersede keeps `model_id` on the superseding calc row.

### Acceptance criteria
- Pick a model → calc pre-filled → submit a plan → `pre_trade_log.model_id` set → the model name
  surfaces in position history / linkage — including on the CLOSED row (5.4), with the free-text
  `model_name` field left empty.

---

## Phase 6 — Retire superseded backtest.html surfaces (AFTER Phases 4+5 are live)

**Goal:** one bundled subtraction — `/api/backtest/qt-import` (landmine L5; the HANDOFF v2.7
commitment) + the old client-side Models sub-tab, both in `templates/backtest.html`. Remove a
surface only **with/after** its replacement is live and operator-verified (v2.6 precedent).
**Dependencies:** Phases 4 + 5 shipped. **Effort:** S.

### Tasks
| # | Task | File(s) |
|---|---|---|
| 6.1 | Delete `POST /api/backtest/qt-import` (`api/routes_backtest.py:213–271`) + its wiring-pin test (`tests/test_task97_1_wiring_pins.py:59–65`). `_validate_date_range` STAYS (also used by `api_backtest_run`) — prose-update its qt-import docstring mention (`routes_backtest.py:40`; also `tests/test_task97_route_validation.py:33` docstring). | `api/routes_backtest.py`, tests |
| 6.2 | Delete the QT Import UI: tab button `backtest.html:15`, `panel-qt` `:150–165`, `submitQtImport()` `:344–354`, `'qt'` in the panel-toggle list `:253`. | `templates/backtest.html` |
| 6.3 | Delete the old Models sub-tab (superseded by `/models`): tab `:16`, `panel-models` `:169–244`, CRUD JS `:406–492`, `loadModels()` boot call `:564`. Update the stale `page_meta['backtest']` "model management" subtitle (`base.html:571`). | `templates/backtest.html`, `templates/base.html` |
| 6.4 | **KEEP** the `results.html` "quantower microstructure" render branch (`fragments/backtest/results.html:27`) — historical `source:"quantower"` sessions remain in the DB and must still render. Anchor-comment why. | `templates/fragments/backtest/results.html` |
| 6.5 | Living-docs sweep: HANDOFF L5 → closed (and correct its "anchored in-file" claim — no anchor ever existed at the route); provenance note: the `source_app` column is canonical going forward, historical rows keep their JSON `"source"` key (no migration). | `HANDOFF.md`, docs |

### Acceptance criteria
- `grep -ri "qt-import\|qt_import"` clean across `api/ templates/ tests/ core/` (docs keep
  historical mentions); the Backtest tab still renders engine sessions AND historical quantower
  rows; models are managed ONLY via `/models`.

---

## 4. Out of scope (this plan)
- Engine-side backtesting (delegated to external apps).
- Export / round-trip to 3rd-party apps (import-only).
- Adapters beyond MultiCharts (the framework makes them additive — one file per app).
- v3 styling + equity-curve charting polish (the minimal UI shows a data table now).

---

## 5. Verification (end-to-end)
1. **Migration idempotency:** boot twice on an existing DB; new ALTERs no-op the second run.
2. **Adapter units:** fixture MultiCharts report for **both `.xml` and `.xlsx`** → summary/trades/
   equity; tolerance + negative tests; registry resolution.
3. **DB layer:** model with risk_preset+strategy round-trips decoded; `create_model_backtest` →
   `list_model_backtests`; existing engine backtests have `model_id IS NULL` (back-compat).
4. **Template compile-render** all new templates/fragments via `jinja2.Environment`.
5. **Routes/UI + calculator linkage (manual):** create a model → import a real MultiCharts
   `.xml`/`.xlsx` report → verify summary/trades/equity render → pre-fill the calculator → submit a
   plan and confirm `pre_trade_log.model_id` is tagged and surfaces in position history/linkage —
   **including on the closed row with the free-text `model_name` left empty** (5.4).
6. **Back-compat + no double-surface:** `/api/models` JSON CRUD still works; `backtest.html` still
   renders non-model sessions AND imported model runs do **NOT** appear there (1.8 filter).
7. **Phase 6 (after 4+5 live):** qt-import + old Models sub-tab gone; historical quantower
   sessions still render; models managed only via `/models`.

---

## 6. Open items — ALL RESOLVED (rev 2, 2026-07-16)
1. **MultiCharts layout — RESOLVED (2026-06-24).** A real `@ES - 1 Minute` export was inspected on
   disk; the exact sheets + columns + mapping are locked in **§ MultiCharts report layout** above.
   The MC `.xml` export is an OOXML `.xlsx` (8 sheets). Fixture-commit is now **task 2.7**
   (truncated synthetic copy — the 1.17 MB original stays outside the repo).
2. **Excel dependency — RESOLVED (superseded):** `openpyxl>=3.1.2` is **already** in
   `requirements.txt:10` (live use: data-logger XLSX export) — no new dependency; task 2.6 is
   verify-only. `xml.etree.ElementTree` stays only as a fallback for a possible true
   flat-XML/SpreadsheetML variant.
3. **`max_drawdown` units — RESOLVED:** the report carries **both** `Max Strategy Drawdown`
   (absolute $) and `Max Strategy Drawdown (%)`. Store both (`max_drawdown` = absolute $ +
   `max_drawdown_pct` = fraction) — both are now in the `BacktestSummary` field list (2.1).
3a. **`size_usdt` — DECIDED (2026-07-16):** store **notional = Price × Contracts × Point Value**
   (Point Value from the `Settings` sheet). Dollar notional matches the column's semantics; raw
   contracts would be meaningless next to engine-run rows. Mapped in § MultiCharts layout.
3b. **Settings-sheet seeding — DECIDED (2026-07-16): skip for v2.7.** `risk_preset_json` /
   `strategy_json` stay operator-entered; show the Settings params read-only in the run detail.
   Auto-seeding adds a mapping surface for marginal benefit in a minimal-UI release — note as a
   v3 enhancement.
4. **Storage — CONFIRMED (2026-07-16):** reuse `backtest_sessions` + `model_id` FK + JSON summary
   (the audit verified exact `asdict()` ↔ column parity for trades/equity). Dedicated stat-column
   tables stay deferred to v3.
5. **qt-import — CONVERTED TO A TASK (2026-07-16):** no longer a "confirm"; retirement is
   **Phase 6** (bundled with the old Models sub-tab removal), per the HANDOFF v2.7 commitment.

---

## 7. Coordination notes
- **Land v2.6 first** (the Quantower removal — DONE 2026-07-16, `e05e359`) — a clean subtraction
  that yields a single data path before this layers on new tables/routes. The two initiatives are
  otherwise independent; this plan adds only new files/columns and touches the calculator seam.
- **Fork base:** `v2.7/model-library` off `e05e359` (the v2.6 tip) — NOT off `main` (258 behind;
  see header). Fast-forwarding main first is an optional operator step.
- **Corr-log:** model CRUD + import inserts are NOT money-path writers — no `db_write` taps needed
  (spec §5.5 / E22: the chokepoint list is named and closed). No new registry categories. The one
  corr-log-adjacent touch is 5.2's `insert_pre_trade_log` edit — its pinned tap payload must stay
  byte-identical (see 5.2).
- Per the standing "one task, then wait" rule, each phase is executed + committed in small steps
  with explicit go-ahead between them.
