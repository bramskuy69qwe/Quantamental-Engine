# Handoff — next Claude Code session

**Date**: 2026-05-24
**Current branch**: `v2.5/post-rewind-drop-regime-infra` @ commit `86b19fe`
**Tests**: 2191 passed, 7 skipped

## What just happened (T170)

Rolled back the regime infrastructure built in T163-T169. Trigger: running T169's counterfactual replay against the operator's live DB returned 175 closed trades with **0 calc_id links**. Operator clarified post-screen: the test sessions weren't using the calculator path (trades came from terminal-side / manual entry), so the calc-id chain went untested end-to-end rather than failing. Either way, the regime work was elaborate infrastructure built on a foundation that hadn't been validated against real calc-backed trades. Operator decision: validate the calc_linkage end-to-end first (build out per the spec/plan that already exists), then rebuild regime on a proven foundation.

T170 mechanics: ONE rollback commit on a new branch off T169 tip. Old branches preserved — everything dropped is recoverable via `git checkout v2.5/regime-2a-counterfactual` (or earlier `v2.5/*` task branches).

## Direction change

**Plan from here** (operator-set):

1. **Build calc_linkage** per `docs/design/calc_linkage_spec.md` + `docs/design/calc_linkage_implementation_plan.md`. Close the fill → calc_id → closed_positions chain so trades actually link back to their plan-time calc rows.
2. **Then rebuild regime** per `v2.5_regime-plan.md` — but lighter than T163-T169 was. With real linked data, the simpler shape may suffice; reassess against actual signal once it exists. Don't reproduce T163's JSON interpreter or T165/T166's hysteresis until evidence shows they're needed.

## What survived the rewind (keepers — don't re-revert)

Independently load-bearing fixes, all preserved:
- **T148** MED-004 PBKDF2-SHA256 KDF upgrade (security)
- **T151** `_user_data_loop` local-shadow fix (real bug)
- **T154** Calculator first-submit 422 backend race (real bug)
- **T157** 4 regime columns on `pre_trade_log` (additive, NULL-default)
- **T159** risk-engine clamps — MED-002 + MED-016 + MED-019 + MED-021 (real correctness)
- **T160** MED-024 fill-misrouting UNIQUE (multi-account safety)
- **T161** contract-validation NameError reactivation (LIVE DEAD-CODE BUG — most consequential keeper)
- **T162** broad-except narrowing sweep (regression guardrail)
- **T165 MED-017 half** mark-price freshness (timestamps + stale-flag surface; `config.MARK_PRICE_STALE_SECONDS`)
- **T168 pollution guards** at `insert_closed_position` + `log_trade_event` (`position_closed`)

## What's gone (don't re-add without re-discussion)

- `core/regime/` (interface + JSON interpreter + HysteresisWrapper + v1_rules.json) — T163
- `core/regime_counterfactual.py` — T169
- `regime_stale` column + migration 013 — T165
- `config.REGIME_CONFIRMATIONS_DERISK/RERISK` — T165
- T164 missing-not-zero guards on FRED/VIX/RVOL fetchers
- T167 `get_dual_pnl_trades` / `get_dual_pnl_summary`
- Hysteresis singleton + reading_id wiring in `compute_current_regime`
- 7 regime test files; T168 trimmed to `test_task168_pollution_guards.py`

## Memory (auto-loaded — but worth knowing)

Three feedback memories in `~/.claude/projects/.../memory/`:
- **untracked-files-discipline**: call out `??` files explicitly when staging; don't silently filter
- **branch-off-cherry-pick**: new task branches must fork off the actual tip including cherry-picks, not the named-task commit alone
- **verify-first-default-mode**: cheap state-check before scoping regime/data-readiness work (opportunistic post-T169; full re-sync only if divergence found)

## Live-DB diagnostic (the test data calc_linkage needs to be exercised against)

- DB: `data/risk_engine.db` (pre-split layout — has `closed_positions`)
- 175 closed trades, 2026-03-13 to 2026-05-20
- **ALL have NULL/empty calc_id** — operator clarification: these trades came from terminal-side / manual entry, NOT through the engine's calculator. Calc-id is generated by the calculator path; trades initiated outside it have no calc_id by design. So the 0-link observation is data-absence (calc path wasn't exercised), NOT evidence the chain is broken.
- 3 `pre_trade_log` rows have `regime_multiplier` (post-T157 plumbing fired on 3 actual calculator runs), but none of those calc_ids link to a closed trade — those 3 calc runs didn't lead to fills that closed within the test window.
- Implication for calc_linkage validation: need to drive trades END-TO-END through the calculator (calc → order → fill → close) to verify the linkage works on real data. The lack of existing linked rows means the linkage is untested, not broken.
- `regime_signals` (relevant later for the regime rebuild): vix/us10y/hy/btc_rvol cover the trade window; `avg_funding` has 20 days only (2026-04-18→2026-05-07); `agg_oi_change` is missing entirely.

## Useful files for context

- `docs/audits/2026-05-17-v2.4-backend-audit.md` — Stat Summary section at the top has the T170 rollback notes + the full kept/dropped manifest
- `v2.5_regime-plan.md` — regime architecture plan (preserved across rewind)
- `docs/design/calc_linkage_spec.md` + `calc_linkage_implementation_plan.md` — the next direction
- `CLAUDE.md` — project test/audit/Jinja/deployment discipline (no T170-relevant changes; everything still applies)

## Recoverable branches (in case you need to peek)

```
v2.5/regime-2a-counterfactual         (T169 tip — full pre-rewind state)
v2.5/regime-1c-dual-pnl               (T167)
v2.5/regime-1b-live-sizing            (T165)
v2.5/regime-1a-classifier-interface   (T163)
v2.5/fix-t168-counterfactual-data-readiness
v2.5/fix-t166-hysteresis-reading-unit
v2.5/fix-t164-fred-error-conservative
v2.5/audit-t162-broad-except-sweep    (last pre-regime state)
```

## Starting hint

When the operator says "let's build calc_linkage", first action: read `docs/design/calc_linkage_implementation_plan.md` end-to-end + skim `calc_linkage_spec.md`. Then verify-first per the memory discipline — trace what's already implemented vs what the plan claims is missing. Specifically: T87 wired calc_id into orders, T104a/T104b wired the calc-to-fill matching window + per-calc override on `pre_trade_log`. The chain exists in code; the live DB just hasn't exercised it (operator's test trades came from terminal-side, not through the calculator). Open question for the plan: are there structural gaps that prevent end-to-end flow, or is the work "drive a calculator-initiated trade through to close + verify each hop persists calc_id"?
